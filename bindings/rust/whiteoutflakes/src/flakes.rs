// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026 Fernando Sahmkow
// AUTOGENERATED by tools/codegen/emit_rust.py — do not edit.
// Regenerate via:  python -m tools.codegen.codegen flakes --backend rust

#![allow(clippy::too_many_arguments)]

// Which of these a module needs depends on its shapes; the modules that
// have no span accessors would otherwise trip the unused-import lint.
#[allow(unused_imports)]
use crate::support::{BorrowedSlice, Bytes};

/// Selects the GPU backend at device-init time.
///
/// The viewer picks via CLI flag, INI setting, or the per-platform default. D3D11 / D3D12 are Windows-only; Vulkan is cross-platform (MoltenVK on macOS). WebGPU is opt-in (CMake -DWDX_ENABLE_WEBGPU=ON) and uses Dawn's native runtime — see `WebGPU.md` for the design. Metal is macOS-only and opt-in (-DWDX_ENABLE_METAL=ON, default ON on Apple platforms). It is the preferred native backend on macOS.
#[repr(i32)]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum GfxApi {
    D3D11 = 0,
    D3D12 = 1,
    Vulkan = 2,
    WebGPU = 3,
    Metal = 4,
}

impl TryFrom<i32> for GfxApi {
    type Error = crate::Error;
    fn try_from(v: i32) -> Result<Self, crate::Error> {
        match v {
            0 => Ok(GfxApi::D3D11),
            1 => Ok(GfxApi::D3D12),
            2 => Ok(GfxApi::Vulkan),
            3 => Ok(GfxApi::WebGPU),
            4 => Ok(GfxApi::Metal),
            other => Err(crate::Error::UnknownEnum {
                name: "GfxApi",
                value: other,
            }),
        }
    }
}

/// Material-rendering path: legacy fixed-function-style (SD) vs Reforged PBR (HD).
#[repr(i32)]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum RenderMode {
    /// Standard-Definition: classic WC3 materials.
    SD = 0,
    /// High-Definition: Reforged PBR materials with IBL.
    HD = 1,
}

impl TryFrom<i32> for RenderMode {
    type Error = crate::Error;
    fn try_from(v: i32) -> Result<Self, crate::Error> {
        match v {
            0 => Ok(RenderMode::SD),
            1 => Ok(RenderMode::HD),
            other => Err(crate::Error::UnknownEnum {
                name: "RenderMode",
                value: other,
            }),
        }
    }
}

/// Transport state of a scene's playback clock.
///
/// `Paused` and `Stopped` both hold time still; they differ in where the clock sits. Pausing keeps the current instant, so resuming continues mid-animation with particles still in flight. Stopping rewinds to the start and discards transient effect state, so resuming plays the scene from the top.
#[repr(i32)]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum PlaybackState {
    /// Time advances normally (scaled by the scene's time scale).
    Playing = 0,
    /// Time held at the current instant.
    Paused = 1,
    /// Time held at zero, transient effect state discarded.
    Stopped = 2,
}

impl TryFrom<i32> for PlaybackState {
    type Error = crate::Error;
    fn try_from(v: i32) -> Result<Self, crate::Error> {
        match v {
            0 => Ok(PlaybackState::Playing),
            1 => Ok(PlaybackState::Paused),
            2 => Ok(PlaybackState::Stopped),
            other => Err(crate::Error::UnknownEnum {
                name: "PlaybackState",
                value: other,
            }),
        }
    }
}

/// Light-rig preset selectable from the host UI.
#[repr(i32)]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum LightingMode {
    /// Engine-runtime rig (game look).
    InGame = 0,
    /// Loading-screen / portrait rig (stronger key light).
    Glue = 1,
    /// Animated DNC-driven rig.
    Dynamic = 2,
}

impl TryFrom<i32> for LightingMode {
    type Error = crate::Error;
    fn try_from(v: i32) -> Result<Self, crate::Error> {
        match v {
            0 => Ok(LightingMode::InGame),
            1 => Ok(LightingMode::Glue),
            2 => Ok(LightingMode::Dynamic),
            other => Err(crate::Error::UnknownEnum {
                name: "LightingMode",
                value: other,
            }),
        }
    }
}

/// Image-Based-Lighting probe set selection.
#[repr(i32)]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum IblMode {
    Portrait = 0,
    DayNight = 1,
    Dungeon = 2,
    Sunset = 3,
}

impl TryFrom<i32> for IblMode {
    type Error = crate::Error;
    fn try_from(v: i32) -> Result<Self, crate::Error> {
        match v {
            0 => Ok(IblMode::Portrait),
            1 => Ok(IblMode::DayNight),
            2 => Ok(IblMode::Dungeon),
            3 => Ok(IblMode::Sunset),
            other => Err(crate::Error::UnknownEnum {
                name: "IblMode",
                value: other,
            }),
        }
    }
}

/// Role of an actor within the scene.
///
/// Exposed to host code (e.g. the Max plugin) so it can mark an actor as host-driven (`External`) and suppress automatic per-frame evaluation.
#[repr(i32)]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum ActorRole {
    /// Top-level model the engine animates.
    Unit = 0,
    /// Host pushes time + evaluates manually.
    External = 1,
    /// Slot-bound child of another actor.
    Attachment = 2,
    /// Legacy PE1 sub-emitter actor.
    PE1 = 3,
    /// Engine-spawned (SND / SPL / SPN events).
    SPN = 4,
}

impl TryFrom<i32> for ActorRole {
    type Error = crate::Error;
    fn try_from(v: i32) -> Result<Self, crate::Error> {
        match v {
            0 => Ok(ActorRole::Unit),
            1 => Ok(ActorRole::External),
            2 => Ok(ActorRole::Attachment),
            3 => Ok(ActorRole::PE1),
            4 => Ok(ActorRole::SPN),
            other => Err(crate::Error::UnknownEnum {
                name: "ActorRole",
                value: other,
            }),
        }
    }
}

/// The 16 canonical WC3 tilesets, ordered to match the in-game World Editor enum so persisted indices round-trip.
#[repr(i32)]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum Tileset {
    LordaeronSummer = 0,
    Ashenvale = 1,
    Barrens = 2,
    Northrend = 3,
    Felwood = 4,
    Dungeon = 5,
    Cityscape = 6,
    LordaeronFall = 7,
    LordaeronWinter = 8,
    Outland = 9,
    SunkenRuins = 10,
    IcecrownGlacier = 11,
    DalaranRuins = 12,
    BlackCitadel = 13,
    Underground = 14,
    Village = 15,
    /// Sentinel: number of valid tilesets, not a value itself.
    Count = 16,
}

impl TryFrom<i32> for Tileset {
    type Error = crate::Error;
    fn try_from(v: i32) -> Result<Self, crate::Error> {
        match v {
            0 => Ok(Tileset::LordaeronSummer),
            1 => Ok(Tileset::Ashenvale),
            2 => Ok(Tileset::Barrens),
            3 => Ok(Tileset::Northrend),
            4 => Ok(Tileset::Felwood),
            5 => Ok(Tileset::Dungeon),
            6 => Ok(Tileset::Cityscape),
            7 => Ok(Tileset::LordaeronFall),
            8 => Ok(Tileset::LordaeronWinter),
            9 => Ok(Tileset::Outland),
            10 => Ok(Tileset::SunkenRuins),
            11 => Ok(Tileset::IcecrownGlacier),
            12 => Ok(Tileset::DalaranRuins),
            13 => Ok(Tileset::BlackCitadel),
            14 => Ok(Tileset::Underground),
            15 => Ok(Tileset::Village),
            16 => Ok(Tileset::Count),
            other => Err(crate::Error::UnknownEnum {
                name: "Tileset",
                value: other,
            }),
        }
    }
}

/// Camera control mode.
#[repr(i32)]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum CameraViewMode {
    /// Pitch/yaw/distance + target follow.
    Orbital = 0,
    /// Position/target/roll set by a `CameraPreset` animator.
    Direct = 1,
}

impl TryFrom<i32> for CameraViewMode {
    type Error = crate::Error;
    fn try_from(v: i32) -> Result<Self, crate::Error> {
        match v {
            0 => Ok(CameraViewMode::Orbital),
            1 => Ok(CameraViewMode::Direct),
            other => Err(crate::Error::UnknownEnum {
                name: "CameraViewMode",
                value: other,
            }),
        }
    }
}

/// Asset categories the manager tracks. Keep in sync with `renderer::assets::AssetKind`.
#[repr(i32)]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum AssetsViewKind {
    Texture = 0,
    Particle = 1,
    ChildModel = 2,
}

impl TryFrom<i32> for AssetsViewKind {
    type Error = crate::Error;
    fn try_from(v: i32) -> Result<Self, crate::Error> {
        match v {
            0 => Ok(AssetsViewKind::Texture),
            1 => Ok(AssetsViewKind::Particle),
            2 => Ok(AssetsViewKind::ChildModel),
            other => Err(crate::Error::UnknownEnum {
                name: "AssetsViewKind",
                value: other,
            }),
        }
    }
}

/// Where a @ref StorageBrowser reads its entries from.
#[repr(i32)]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum StorageKind {
    /// An installed game's CASC storage.
    Casc = 0,
    /// A single `.mpq` / `.w3x` / `.w3m` archive.
    Mpq = 1,
    /// A directory on disk, walked recursively.
    Folder = 2,
}

impl TryFrom<i32> for StorageKind {
    type Error = crate::Error;
    fn try_from(v: i32) -> Result<Self, crate::Error> {
        match v {
            0 => Ok(StorageKind::Casc),
            1 => Ok(StorageKind::Mpq),
            2 => Ok(StorageKind::Folder),
            other => Err(crate::Error::UnknownEnum {
                name: "StorageKind",
                value: other,
            }),
        }
    }
}

/// Which file types @ref StorageBrowser::Files reports.
///
/// Naming matches `StorageFileFilter` in the explorer panel, which has had the same three-way choice since before the browser was bindable.
#[repr(i32)]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum StorageFileFilter {
    /// Models and effects both.
    All = 0,
    /// `.mdx` / `.mdl`.
    ModelsOnly = 1,
    /// `.pkb`
    EffectsOnly = 2,
}

impl TryFrom<i32> for StorageFileFilter {
    type Error = crate::Error;
    fn try_from(v: i32) -> Result<Self, crate::Error> {
        match v {
            0 => Ok(StorageFileFilter::All),
            1 => Ok(StorageFileFilter::ModelsOnly),
            2 => Ok(StorageFileFilter::EffectsOnly),
            other => Err(crate::Error::UnknownEnum {
                name: "StorageFileFilter",
                value: other,
            }),
        }
    }
}

/// An owned list of [`SequenceInfo`] produced by the library.
pub struct SequenceInfoList {
    raw: core::ptr::NonNull<ffi::whiteout_SequenceInfoList>,
}

impl SequenceInfoList {
    /// # Safety
    /// `raw` must be a live list this value takes ownership of.
    #[allow(dead_code)]
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_SequenceInfoList) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| SequenceInfoList { raw })
    }

    pub fn len(&self) -> usize {
        // SAFETY: the list is live for `&self`.
        unsafe { ffi::whiteout_flakes_SequenceInfoList_size(self.raw.as_ptr()) }
    }

    pub fn is_empty(&self) -> bool {
        self.len() == 0
    }

    /// Borrow element `index`. `None` when out of range.
    pub fn get(&self, index: usize) -> Option<crate::support::Ref<'_, SequenceInfo>> {
        if index >= self.len() {
            return None;
        }
        // SAFETY: index checked; the pointer is interior to the list and
        // is never freed by the `Ref`.
        unsafe {
            Some(crate::support::Ref::new(SequenceInfo {
                raw: core::ptr::NonNull::new_unchecked(ffi::whiteout_flakes_SequenceInfoList_at(
                    self.raw.as_ptr(),
                    index,
                )),
            }))
        }
    }

    pub fn iter(&self) -> impl ExactSizeIterator<Item = crate::support::Ref<'_, SequenceInfo>> {
        (0..self.len()).map(move |i| self.get(i).expect("index below len"))
    }
}

impl Drop for SequenceInfoList {
    fn drop(&mut self) {
        // SAFETY: the list was transferred to us and is freed once.
        unsafe { ffi::whiteout_flakes_SequenceInfoList_delete(self.raw.as_ptr()) }
    }
}

impl core::fmt::Debug for SequenceInfoList {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("SequenceInfoList")
            .field("len", &self.len())
            .finish()
    }
}

// SAFETY: a plain heap vector with no thread affinity, owned exclusively.
unsafe impl Send for SequenceInfoList {}

/// An owned list of [`CameraPreset`] produced by the library.
pub struct CameraPresetList {
    raw: core::ptr::NonNull<ffi::whiteout_CameraPresetList>,
}

impl CameraPresetList {
    /// # Safety
    /// `raw` must be a live list this value takes ownership of.
    #[allow(dead_code)]
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_CameraPresetList) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| CameraPresetList { raw })
    }

    pub fn len(&self) -> usize {
        // SAFETY: the list is live for `&self`.
        unsafe { ffi::whiteout_flakes_CameraPresetList_size(self.raw.as_ptr()) }
    }

    pub fn is_empty(&self) -> bool {
        self.len() == 0
    }

    /// Borrow element `index`. `None` when out of range.
    pub fn get(&self, index: usize) -> Option<crate::support::Ref<'_, CameraPreset>> {
        if index >= self.len() {
            return None;
        }
        // SAFETY: index checked; the pointer is interior to the list and
        // is never freed by the `Ref`.
        unsafe {
            Some(crate::support::Ref::new(CameraPreset {
                raw: core::ptr::NonNull::new_unchecked(ffi::whiteout_flakes_CameraPresetList_at(
                    self.raw.as_ptr(),
                    index,
                )),
            }))
        }
    }

    pub fn iter(&self) -> impl ExactSizeIterator<Item = crate::support::Ref<'_, CameraPreset>> {
        (0..self.len()).map(move |i| self.get(i).expect("index below len"))
    }
}

impl Drop for CameraPresetList {
    fn drop(&mut self) {
        // SAFETY: the list was transferred to us and is freed once.
        unsafe { ffi::whiteout_flakes_CameraPresetList_delete(self.raw.as_ptr()) }
    }
}

impl core::fmt::Debug for CameraPresetList {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("CameraPresetList")
            .field("len", &self.len())
            .finish()
    }
}

// SAFETY: a plain heap vector with no thread affinity, owned exclusively.
unsafe impl Send for CameraPresetList {}

/// Integer-coordinate screen-space rectangle (top-down, pixel units).
///
/// Used by host frontends for laying out UI overlays.
pub struct Rect {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesRect>,
}

impl Drop for Rect {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesRect_delete(self.raw.as_ptr()) }
    }
}

impl Rect {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesRect) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| Rect { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for Rect {}

impl core::fmt::Debug for Rect {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("Rect").finish_non_exhaustive()
    }
}

impl Rect {
    /// # Panics
    /// Panics if the native allocation fails.
    pub fn new() -> Self {
        // SAFETY: the native constructor returns a live handle; a null here
        // means the library is unusable.
        unsafe {
            let raw = ffi::whiteout_flakes_FlakesRect_new();
            Self::from_raw(raw).expect("native Rect allocation failed")
        }
    }

    pub fn left(&self) -> i32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesRect_get_left(self.raw.as_ptr()) }
    }

    pub fn set_left(&mut self, value: i32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesRect_set_left(self.raw.as_ptr(), value) }
    }

    pub fn top(&self) -> i32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesRect_get_top(self.raw.as_ptr()) }
    }

    pub fn set_top(&mut self, value: i32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesRect_set_top(self.raw.as_ptr(), value) }
    }

    pub fn right(&self) -> i32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesRect_get_right(self.raw.as_ptr()) }
    }

    pub fn set_right(&mut self, value: i32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesRect_set_right(self.raw.as_ptr(), value) }
    }

    pub fn bottom(&self) -> i32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesRect_get_bottom(self.raw.as_ptr()) }
    }

    pub fn set_bottom(&mut self, value: i32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesRect_set_bottom(self.raw.as_ptr(), value) }
    }
}

impl Default for Rect {
    fn default() -> Self {
        Self::new()
    }
}

/// Cascade-shadow-map tuning consumed by `ShadowService`.
///
/// `enabled = false` disables shadows entirely (no shadow pass, no sampler binds). When enabled, `cascadeCount` cascades of `cascadeResolution × cascadeResolution` each are rendered every frame; `lambdaSplit` mixes uniform and logarithmic cascade distribution (0 = uniform, 1 = log).
#[derive(Clone, Debug, PartialEq)]
pub struct ShadowParams {
    /// Number of cascades; the renderer accepts 1..3.
    pub cascade_count: i32,
    /// Per-cascade shadow-map side length in texels.
    pub cascade_resolution: i32,
    /// Extruded receiver-height padding for the cascade frustums; larger values include taller casters in the cascade at a quality cost.
    pub caster_height: f32,
    /// Cascade-distribution lambda: 0 = uniform splits, 1 = logarithmic.
    pub lambda_split: f32,
    /// @name Depth-bias tuning (passed to the rasterizer state). Defaults sized for a D32_FLOAT cascade with the LessEqual PCF compare op. Zero biases let rasterizer floating-point noise swing the depth comparison both ways on near-coplanar fragments; the 3×3 PCF averages that salt-and-pepper into ~50% lit and the scene looks uniformly darker than it should. A small constant offset + slope-scaled term keeps lit fragments lit while still catching actual occluders. @{
    pub depth_bias: i32,
    pub slope_scaled_bias: f32,
    pub depth_bias_clamp: f32,
    /// Snap cascade origin to texel grid to suppress shimmering as the camera moves.
    pub texel_snap: bool,
    /// Master enable. Off ⇒ no shadow pass; the lit pass binds a 1×1 white sampler so PCF reads return "lit".
    pub enabled: bool,
}

impl Default for ShadowParams {
    fn default() -> Self {
        // SAFETY: `_new` always returns a live handle; freed before return.
        unsafe {
            let h = ffi::whiteout_flakes_FlakesShadowParams_new();
            let out = ShadowParams {
                cascade_count: ffi::whiteout_flakes_FlakesShadowParams_get_cascadeCount(h),
                cascade_resolution: ffi::whiteout_flakes_FlakesShadowParams_get_cascadeResolution(
                    h,
                ),
                caster_height: ffi::whiteout_flakes_FlakesShadowParams_get_casterHeight(h),
                lambda_split: ffi::whiteout_flakes_FlakesShadowParams_get_lambdaSplit(h),
                depth_bias: ffi::whiteout_flakes_FlakesShadowParams_get_depthBias(h),
                slope_scaled_bias: ffi::whiteout_flakes_FlakesShadowParams_get_slopeScaledBias(h),
                depth_bias_clamp: ffi::whiteout_flakes_FlakesShadowParams_get_depthBiasClamp(h),
                texel_snap: ffi::whiteout_flakes_FlakesShadowParams_get_texelSnap(h) != 0,
                enabled: ffi::whiteout_flakes_FlakesShadowParams_get_enabled(h) != 0,
            };
            ffi::whiteout_flakes_FlakesShadowParams_delete(h);
            out
        }
    }
}

impl ShadowParams {
    /// Build a native handle carrying these values. Caller frees it.
    #[allow(dead_code)] // consumed once the methods taking these options bind
    pub(crate) unsafe fn to_native(&self) -> *mut ffi::whiteout_FlakesShadowParams {
        unsafe {
            let h = ffi::whiteout_flakes_FlakesShadowParams_new();
            ffi::whiteout_flakes_FlakesShadowParams_set_cascadeCount(h, self.cascade_count);
            ffi::whiteout_flakes_FlakesShadowParams_set_cascadeResolution(
                h,
                self.cascade_resolution,
            );
            ffi::whiteout_flakes_FlakesShadowParams_set_casterHeight(h, self.caster_height);
            ffi::whiteout_flakes_FlakesShadowParams_set_lambdaSplit(h, self.lambda_split);
            ffi::whiteout_flakes_FlakesShadowParams_set_depthBias(h, self.depth_bias);
            ffi::whiteout_flakes_FlakesShadowParams_set_slopeScaledBias(h, self.slope_scaled_bias);
            ffi::whiteout_flakes_FlakesShadowParams_set_depthBiasClamp(h, self.depth_bias_clamp);
            ffi::whiteout_flakes_FlakesShadowParams_set_texelSnap(
                h,
                if self.texel_snap { 1 } else { 0 },
            );
            ffi::whiteout_flakes_FlakesShadowParams_set_enabled(
                h,
                if self.enabled { 1 } else { 0 },
            );
            h
        }
    }

    /// Free a handle produced by [`Self::to_native`].
    ///
    /// # Safety
    /// `h` must have come from `to_native` and not been freed already.
    #[allow(dead_code)]
    pub(crate) unsafe fn free_native(h: *mut ffi::whiteout_FlakesShadowParams) {
        unsafe { ffi::whiteout_flakes_FlakesShadowParams_delete(h) }
    }
}

/// Toggles for what the renderer draws each frame.
///
/// Persisted by the host as a single block; defaults shown match the out-of-the-box viewer experience.
pub struct DisplayFlags {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesDisplayFlags>,
}

impl Drop for DisplayFlags {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesDisplayFlags_delete(self.raw.as_ptr()) }
    }
}

impl DisplayFlags {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesDisplayFlags) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| DisplayFlags { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for DisplayFlags {}

impl core::fmt::Debug for DisplayFlags {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("DisplayFlags").finish_non_exhaustive()
    }
}

impl DisplayFlags {
    /// # Panics
    /// Panics if the native allocation fails.
    pub fn new() -> Self {
        // SAFETY: the native constructor returns a live handle; a null here
        // means the library is unusable.
        unsafe {
            let raw = ffi::whiteout_flakes_FlakesDisplayFlags_new();
            Self::from_raw(raw).expect("native DisplayFlags allocation failed")
        }
    }

    /// Reference grid on the ground plane.
    pub fn show_grid(&self) -> bool {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesDisplayFlags_get_showGrid(self.raw.as_ptr()) != 0 }
    }

    pub fn set_show_grid(&mut self, value: bool) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesDisplayFlags_set_showGrid(
                self.raw.as_ptr(),
                if value { 1 } else { 0 },
            )
        }
    }

    /// MDX particle emitters.
    pub fn show_particles(&self) -> bool {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesDisplayFlags_get_showParticles(self.raw.as_ptr()) != 0 }
    }

    pub fn set_show_particles(&mut self, value: bool) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesDisplayFlags_set_showParticles(
                self.raw.as_ptr(),
                if value { 1 } else { 0 },
            )
        }
    }

    /// MDX ribbon emitters.
    pub fn show_ribbons(&self) -> bool {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesDisplayFlags_get_showRibbons(self.raw.as_ptr()) != 0 }
    }

    pub fn set_show_ribbons(&mut self, value: bool) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesDisplayFlags_set_showRibbons(
                self.raw.as_ptr(),
                if value { 1 } else { 0 },
            )
        }
    }

    /// Debug overlay for collision shapes.
    pub fn show_collisions(&self) -> bool {
        // SAFETY: plain scalar read through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesDisplayFlags_get_showCollisions(self.raw.as_ptr()) != 0
        }
    }

    pub fn set_show_collisions(&mut self, value: bool) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesDisplayFlags_set_showCollisions(
                self.raw.as_ptr(),
                if value { 1 } else { 0 },
            )
        }
    }

    /// Debug overlay for light positions.
    pub fn show_lights(&self) -> bool {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesDisplayFlags_get_showLights(self.raw.as_ptr()) != 0 }
    }

    pub fn set_show_lights(&mut self, value: bool) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesDisplayFlags_set_showLights(
                self.raw.as_ptr(),
                if value { 1 } else { 0 },
            )
        }
    }

    /// MDX event objects (SPN / SPL / SND).
    pub fn show_events(&self) -> bool {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesDisplayFlags_get_showEvents(self.raw.as_ptr()) != 0 }
    }

    pub fn set_show_events(&mut self, value: bool) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesDisplayFlags_set_showEvents(
                self.raw.as_ptr(),
                if value { 1 } else { 0 },
            )
        }
    }

    /// Material path (SD vs HD).
    pub fn render_mode(&self) -> RenderMode {
        // SAFETY: scalar read; the discriminant is validated below.
        unsafe { ffi::whiteout_flakes_FlakesDisplayFlags_get_renderMode(self.raw.as_ptr()) }
            .try_into()
            .expect("unknown enum discriminant from the native library")
    }

    pub fn set_render_mode(&mut self, value: RenderMode) {
        // SAFETY: scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesDisplayFlags_set_renderMode(self.raw.as_ptr(), value as i32)
        }
    }
}

impl Default for DisplayFlags {
    fn default() -> Self {
        Self::new()
    }
}

/// Per-frame statistics aggregated by the pipeline.
///
/// Names mirror the internal `RenderPipeline::GetFrameStats` out parameters. Hosts surface these in title-bar / overlay HUDs.
pub struct FrameStats {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesFrameStats>,
}

impl Drop for FrameStats {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_delete(self.raw.as_ptr()) }
    }
}

impl FrameStats {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesFrameStats) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| FrameStats { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for FrameStats {}

impl core::fmt::Debug for FrameStats {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("FrameStats").finish_non_exhaustive()
    }
}

impl FrameStats {
    /// # Panics
    /// Panics if the native allocation fails.
    pub fn new() -> Self {
        // SAFETY: the native constructor returns a live handle; a null here
        // means the library is unusable.
        unsafe {
            let raw = ffi::whiteout_flakes_FlakesFrameStats_new();
            Self::from_raw(raw).expect("native FrameStats allocation failed")
        }
    }

    pub fn geosets(&self) -> i32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_get_geosets(self.raw.as_ptr()) }
    }

    pub fn set_geosets(&mut self, value: i32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_set_geosets(self.raw.as_ptr(), value) }
    }

    pub fn textures(&self) -> i32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_get_textures(self.raw.as_ptr()) }
    }

    pub fn set_textures(&mut self, value: i32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_set_textures(self.raw.as_ptr(), value) }
    }

    pub fn nodes(&self) -> i32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_get_nodes(self.raw.as_ptr()) }
    }

    pub fn set_nodes(&mut self, value: i32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_set_nodes(self.raw.as_ptr(), value) }
    }

    /// Live legacy (PE2) particles. Does **not** include corn effects — that is a separate simulation, counted by @ref cornParticles.
    pub fn particles(&self) -> i32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_get_particles(self.raw.as_ptr()) }
    }

    pub fn set_particles(&mut self, value: i32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_set_particles(self.raw.as_ptr(), value) }
    }

    pub fn segments(&self) -> i32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_get_segments(self.raw.as_ptr()) }
    }

    pub fn set_segments(&mut self, value: i32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_set_segments(self.raw.as_ptr(), value) }
    }

    /// Live corn-effects particles across every registered emitter. Separate from @ref particles because the two are different simulations with different lifetimes, and a host watching one learns nothing about the other.
    pub fn corn_particles(&self) -> i32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_get_cornParticles(self.raw.as_ptr()) }
    }

    pub fn set_corn_particles(&mut self, value: i32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesFrameStats_set_cornParticles(self.raw.as_ptr(), value) }
    }
}

impl Default for FrameStats {
    fn default() -> Self {
        Self::new()
    }
}

/// Scripted camera the host may select for a loaded model.
///
/// Mirrors the engine's MDX camera-chunk semantics: a static pose (`position` / `target` / FoV / clip planes / roll) plus an optional per-frame animator that overrides them based on the active sequence's time cursor.
pub struct CameraPreset {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesCameraPreset>,
}

impl Drop for CameraPreset {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_delete(self.raw.as_ptr()) }
    }
}

impl CameraPreset {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesCameraPreset) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| CameraPreset { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for CameraPreset {}

impl core::fmt::Debug for CameraPreset {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("CameraPreset").finish_non_exhaustive()
    }
}

impl CameraPreset {
    /// # Panics
    /// Panics if the native allocation fails.
    pub fn new() -> Self {
        // SAFETY: the native constructor returns a live handle; a null here
        // means the library is unusable.
        unsafe {
            let raw = ffi::whiteout_flakes_FlakesCameraPreset_new();
            Self::from_raw(raw).expect("native CameraPreset allocation failed")
        }
    }

    /// UTF-8 preset name as authored by the artist (used by host UI dropdowns); was `std::wstring` historically but normalised to UTF-8 so the same string flows from the MDX adapter through the UI without per-platform wide-char roundtrips.
    pub fn name(&self) -> String {
        // SAFETY: the native side hands over an owned CString.
        unsafe {
            crate::support::take_string(ffi::whiteout_flakes_FlakesCameraPreset_get_name(
                self.raw.as_ptr(),
            ))
        }
    }

    pub fn set_name(&mut self, value: &str) {
        let value = std::ffi::CString::new(value).unwrap_or_default();
        // SAFETY: the pointer outlives the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraPreset_set_name(self.raw.as_ptr(), value.as_ptr())
        }
    }

    /// `true` if this preset should drive the camera every frame (i.e. the user can't free-orbit while it's active).
    pub fn is_live(&self) -> bool {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_get_isLive(self.raw.as_ptr()) != 0 }
    }

    pub fn set_is_live(&mut self, value: bool) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraPreset_set_isLive(
                self.raw.as_ptr(),
                if value { 1 } else { 0 },
            )
        }
    }

    /// Diagonal field of view (radians).
    pub fn fov_diagonal(&self) -> f32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_get_fovDiagonal(self.raw.as_ptr()) }
    }

    pub fn set_fov_diagonal(&mut self, value: f32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_set_fovDiagonal(self.raw.as_ptr(), value) }
    }

    /// Near clip plane.
    pub fn z_near(&self) -> f32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_get_zNear(self.raw.as_ptr()) }
    }

    pub fn set_z_near(&mut self, value: f32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_set_zNear(self.raw.as_ptr(), value) }
    }

    /// Far clip plane.
    pub fn z_far(&self) -> f32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_get_zFar(self.raw.as_ptr()) }
    }

    pub fn set_z_far(&mut self, value: f32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_set_zFar(self.raw.as_ptr(), value) }
    }

    /// Roll around the view axis (radians).
    pub fn static_roll(&self) -> f32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_get_staticRoll(self.raw.as_ptr()) }
    }

    pub fn set_static_roll(&mut self, value: f32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_set_staticRoll(self.raw.as_ptr(), value) }
    }

    /// @name Derived orbital pose @{ Convenience values populated by the MDX adapter for free-camera fallback (`pitch`/`yaw` in radians, `distance` from `target`).
    pub fn pitch(&self) -> f32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_get_pitch(self.raw.as_ptr()) }
    }

    pub fn set_pitch(&mut self, value: f32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_set_pitch(self.raw.as_ptr(), value) }
    }

    pub fn yaw(&self) -> f32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_get_yaw(self.raw.as_ptr()) }
    }

    pub fn set_yaw(&mut self, value: f32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_set_yaw(self.raw.as_ptr(), value) }
    }

    pub fn distance(&self) -> f32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_get_distance(self.raw.as_ptr()) }
    }

    pub fn set_distance(&mut self, value: f32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesCameraPreset_set_distance(self.raw.as_ptr(), value) }
    }
}

impl Default for CameraPreset {
    fn default() -> Self {
        Self::new()
    }
}

/// Description of one animation sequence (`SEQS` chunk entry).
///
/// Times are in milliseconds (matches MDX's internal unit). `moveSpeed` is the artist-authored locomotion speed in world-units / second, used by hosts to drive walk-cycle drift along the camera axis.
pub struct SequenceInfo {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesSequenceInfo>,
}

impl Drop for SequenceInfo {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesSequenceInfo_delete(self.raw.as_ptr()) }
    }
}

impl SequenceInfo {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesSequenceInfo) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| SequenceInfo { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for SequenceInfo {}

impl core::fmt::Debug for SequenceInfo {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("SequenceInfo").finish_non_exhaustive()
    }
}

impl SequenceInfo {
    /// # Panics
    /// Panics if the native allocation fails.
    pub fn new() -> Self {
        // SAFETY: the native constructor returns a live handle; a null here
        // means the library is unusable.
        unsafe {
            let raw = ffi::whiteout_flakes_FlakesSequenceInfo_new();
            Self::from_raw(raw).expect("native SequenceInfo allocation failed")
        }
    }

    pub fn name(&self) -> String {
        // SAFETY: the native side hands over an owned CString.
        unsafe {
            crate::support::take_string(ffi::whiteout_flakes_FlakesSequenceInfo_get_name(
                self.raw.as_ptr(),
            ))
        }
    }

    pub fn set_name(&mut self, value: &str) {
        let value = std::ffi::CString::new(value).unwrap_or_default();
        // SAFETY: the pointer outlives the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSequenceInfo_set_name(self.raw.as_ptr(), value.as_ptr())
        }
    }

    pub fn start_ms(&self) -> i32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesSequenceInfo_get_startMs(self.raw.as_ptr()) }
    }

    pub fn set_start_ms(&mut self, value: i32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesSequenceInfo_set_startMs(self.raw.as_ptr(), value) }
    }

    pub fn end_ms(&self) -> i32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesSequenceInfo_get_endMs(self.raw.as_ptr()) }
    }

    pub fn set_end_ms(&mut self, value: i32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesSequenceInfo_set_endMs(self.raw.as_ptr(), value) }
    }

    pub fn move_speed(&self) -> f32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesSequenceInfo_get_moveSpeed(self.raw.as_ptr()) }
    }

    pub fn set_move_speed(&mut self, value: f32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesSequenceInfo_set_moveSpeed(self.raw.as_ptr(), value) }
    }

    pub fn rarity(&self) -> f32 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesSequenceInfo_get_rarity(self.raw.as_ptr()) }
    }

    pub fn set_rarity(&mut self, value: f32) {
        // SAFETY: plain scalar write through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesSequenceInfo_set_rarity(self.raw.as_ptr(), value) }
    }

    pub fn non_looping(&self) -> bool {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesSequenceInfo_get_nonLooping(self.raw.as_ptr()) != 0 }
    }

    pub fn set_non_looping(&mut self, value: bool) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesSequenceInfo_set_nonLooping(
                self.raw.as_ptr(),
                if value { 1 } else { 0 },
            )
        }
    }
}

impl Default for SequenceInfo {
    fn default() -> Self {
        Self::new()
    }
}

/// Swap-chain + frame-submit surface.
pub struct PipelineView {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesPipelineView>,
}

impl Drop for PipelineView {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesPipelineView_delete(self.raw.as_ptr()) }
    }
}

impl PipelineView {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesPipelineView) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| PipelineView { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for PipelineView {}

impl core::fmt::Debug for PipelineView {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("PipelineView").finish_non_exhaustive()
    }
}

impl PipelineView {
    /// Create the gfx device. Must succeed before any other pipeline call. Picks the platform-default backend if @p api is `Vulkan` on non-Windows.
    pub fn init_device(&mut self, arg: GfxApi) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPipelineView_InitDevice(self.raw.as_ptr(), arg as i32);
        }
    }

    pub fn is_device_ready(&self) -> bool {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesPipelineView_IsDeviceReady(self.raw.as_ptr()) != 0 }
    }

    /// Create a headless (off-screen) target with no swap-chain. RenderFrame / RenderViewport draw into it; Present no-ops on it. Read the result back via the frame-capture ring. Use for thumbnail / render-to-texture viewports that aren't shown in a window.
    pub fn create_offscreen_target(&mut self, width: i32, height: i32) -> u32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPipelineView_CreateOffscreenTarget(
                self.raw.as_ptr(),
                width,
                height,
            )
        }
    }

    /// Destroy a target (swap-chain or off-screen) and free its GPU memory. Safe to call on any target id; clears primary if it was.
    pub fn destroy_target(&mut self, arg: u32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPipelineView_DestroyTarget(self.raw.as_ptr(), arg);
        }
    }

    /// Mark a target as the main one (frame stats track this one).
    pub fn set_primary_target(&mut self, arg: u32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPipelineView_SetPrimaryTarget(self.raw.as_ptr(), arg);
        }
    }

    /// Resize the primary target's swap-chain.
    pub fn resize_primary_target(&mut self, width: i32, height: i32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPipelineView_ResizePrimaryTarget(
                self.raw.as_ptr(),
                width,
                height,
            );
        }
    }

    /// Render the scene's default camera to @p t.
    pub fn render_frame(&mut self, t: u32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPipelineView_RenderFrame(self.raw.as_ptr(), t);
        }
    }

    /// Render the scene seen through @p camera into target @p t. The unit of multi-viewport rendering: call once per (camera, target) pair in a frame, then Present the windowed targets. Cameras come from the scene's camera set (`Renderer::CreateCamera`).
    pub fn render_viewport(&mut self, t: u32, camera: u32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPipelineView_RenderViewport(self.raw.as_ptr(), t, camera);
        }
    }

    /// Present @p t to the window. No-op for headless targets.
    pub fn present(&mut self, t: u32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPipelineView_Present(self.raw.as_ptr(), t);
        }
    }

    /// Tear down the gfx device. Idempotent.
    pub fn shutdown(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPipelineView_Shutdown(self.raw.as_ptr());
        }
    }

    /// Last-frame stats (geoset / texture / node / particle / segment counts).
    pub fn frame_stats(&self) -> Option<FrameStats> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            FrameStats::from_raw(ffi::whiteout_flakes_FlakesPipelineView_FrameStats(
                self.raw.as_ptr(),
            ))
        }
    }

    /// Live GPU bytes currently allocated. WebGPU backend tracks every CreateTexture / CreateBuffer and subtracts on deferred-delete drain — diagnostic for memory growth. Returns 0 on backends without tracking.
    pub fn live_gpu_bytes(&self) -> u64 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesPipelineView_LiveGpuBytes(self.raw.as_ptr()) }
    }
}

/// Scene-clock + content-provider surface.
pub struct SceneView {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesSceneView>,
}

impl Drop for SceneView {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesSceneView_delete(self.raw.as_ptr()) }
    }
}

impl SceneView {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesSceneView) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| SceneView { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for SceneView {}

impl core::fmt::Debug for SceneView {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("SceneView").finish_non_exhaustive()
    }
}

impl SceneView {
    /// Master animation clock the renderer ticks (ms).
    pub fn animation_time_ms(&self) -> i32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesSceneView_AnimationTimeMs(self.raw.as_ptr()) }
    }

    pub fn set_animation_time_ms(&mut self, arg: i32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSceneView_SetAnimationTimeMs(self.raw.as_ptr(), arg);
        }
    }

    /// Advance per-frame scene state (DNC, deferred deletes, …).
    pub fn update(&mut self, dt: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSceneView_Update(self.raw.as_ptr(), dt);
        }
    }

    /// Point this scene's content provider at an installed Warcraft III, so archive paths resolve out of its CASC storage.
    ///
    /// The counterpart to @ref StorageBrowser: browse an install, then hand the same root here and `LoaderView::SpawnUnit` reads the paths the browser returns. Loose files on disk keep working — CASC is consulted for what is not found beside @ref SetPE1BasePath.
    ///
    /// No-op for scenes running on a host-supplied content provider, which resolve however the host chooses.
    pub fn set_casc_install_path(&mut self, root: &str) {
        let root_cstr = std::ffi::CString::new(root).unwrap_or_default();
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSceneView_SetCascInstallPath(
                self.raw.as_ptr(),
                root_cstr.as_ptr(),
            );
        }
    }

    /// Prefer the HD (Reforged) asset chain over SD when resolving archive paths.
    ///
    /// A CASC path exists in several mod layers; the provider tries them in order, and which order is right depends on the model. Set this from the actor's @ref ActorView::PreferredRenderMode, or textures resolve to the wrong layer and a Reforged model renders with classic art.
    pub fn set_hd_mode(&mut self, enabled: bool) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSceneView_SetHdMode(
                self.raw.as_ptr(),
                if enabled { 1 } else { 0 },
            );
        }
    }
}

/// Free-orbit + scripted camera surface.
pub struct CameraView {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesCameraView>,
}

impl Drop for CameraView {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesCameraView_delete(self.raw.as_ptr()) }
    }
}

impl CameraView {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesCameraView) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| CameraView { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for CameraView {}

impl core::fmt::Debug for CameraView {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("CameraView").finish_non_exhaustive()
    }
}

impl CameraView {
    /// Re-center on the world origin in orbital mode.
    pub fn reset(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraView_Reset(self.raw.as_ptr());
        }
    }

    pub fn set_pitch(&mut self, arg: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraView_SetPitch(self.raw.as_ptr(), arg);
        }
    }

    pub fn set_yaw(&mut self, arg: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraView_SetYaw(self.raw.as_ptr(), arg);
        }
    }

    pub fn set_distance(&mut self, arg: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraView_SetDistance(self.raw.as_ptr(), arg);
        }
    }

    pub fn set_target(&mut self, x: f32, y: f32, z: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraView_SetTarget(self.raw.as_ptr(), x, y, z);
        }
    }

    /// Apply a mouse-drag rotation (orbital mode).
    pub fn rotate(&mut self, dx: i32, dy: i32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraView_Rotate(self.raw.as_ptr(), dx, dy);
        }
    }

    /// Apply a mouse-drag pan (orbital mode).
    pub fn pan(&mut self, dx: i32, dy: i32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraView_Pan(self.raw.as_ptr(), dx, dy);
        }
    }

    /// Apply a wheel-step zoom.
    pub fn zoom(&mut self, wheel_delta: i32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraView_Zoom(self.raw.as_ptr(), wheel_delta);
        }
    }

    /// Apply a continuous-axis zoom (middle-mouse drag).
    pub fn zoom_smooth(&mut self, factor: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraView_ZoomSmooth(self.raw.as_ptr(), factor);
        }
    }

    /// Switch from `Direct` back to `Orbital`.
    pub fn set_orbital_mode(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraView_SetOrbitalMode(self.raw.as_ptr());
        }
    }

    /// Diagonal field of view (radians).
    pub fn set_fov_diagonal(&mut self, arg: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraView_SetFovDiagonal(self.raw.as_ptr(), arg);
        }
    }

    /// Near + far clip planes.
    pub fn set_clip(&mut self, nz: f32, fz: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesCameraView_SetClip(self.raw.as_ptr(), nz, fz);
        }
    }

    pub fn mode(&self) -> CameraViewMode {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            CameraViewMode::try_from(ffi::whiteout_flakes_FlakesCameraView_Mode(
                self.raw.as_ptr(),
            ))
            .expect("unknown enum discriminant from the native library (ABI version skew)")
        }
    }

    pub fn distance(&self) -> f32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesCameraView_Distance(self.raw.as_ptr()) }
    }
}

/// Display flags, post-processing tunables, and persistent host knobs that survive across runs.
pub struct SettingsView {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesSettingsView>,
}

impl Drop for SettingsView {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesSettingsView_delete(self.raw.as_ptr()) }
    }
}

impl SettingsView {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesSettingsView) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| SettingsView { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for SettingsView {}

impl core::fmt::Debug for SettingsView {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("SettingsView").finish_non_exhaustive()
    }
}

impl SettingsView {
    pub fn display_flags(&self) -> Option<DisplayFlags> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            DisplayFlags::from_raw(ffi::whiteout_flakes_FlakesSettingsView_DisplayFlags(
                self.raw.as_ptr(),
            ))
        }
    }

    pub fn set_display_flags(&mut self, arg: &DisplayFlags) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSettingsView_SetDisplayFlags(
                self.raw.as_ptr(),
                arg.raw.as_ptr(),
            );
        }
    }

    /// Returns `true` once (then resets) whenever the render mode was changed since the last call. Hosts gate model re-spawn / material refresh on this.
    pub fn consume_render_mode_dirty(&mut self) -> bool {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSettingsView_ConsumeRenderModeDirty(self.raw.as_ptr()) != 0
        }
    }

    pub fn lighting_mode(&self) -> LightingMode {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            LightingMode::try_from(ffi::whiteout_flakes_FlakesSettingsView_LightingMode(
                self.raw.as_ptr(),
            ))
            .expect("unknown enum discriminant from the native library (ABI version skew)")
        }
    }

    pub fn set_lighting_mode(&mut self, arg: LightingMode) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSettingsView_SetLightingMode(self.raw.as_ptr(), arg as i32);
        }
    }

    /// Background colour as packed 0x00BBGGRR.
    pub fn background_color_raw(&self) -> u32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesSettingsView_BackgroundColorRaw(self.raw.as_ptr()) }
    }

    pub fn set_background_color(&mut self, r: u8, g: u8, b: u8) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSettingsView_SetBackgroundColor(self.raw.as_ptr(), r, g, b);
        }
    }

    pub fn tonemap_exposure(&self) -> f32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesSettingsView_TonemapExposure(self.raw.as_ptr()) }
    }

    pub fn set_tonemap_exposure(&mut self, arg: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSettingsView_SetTonemapExposure(self.raw.as_ptr(), arg);
        }
    }

    /// HD-only bloom master switch. Off in SD, and inert if the backend has no bloom shader bundle.
    pub fn bloom_enabled(&self) -> bool {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesSettingsView_BloomEnabled(self.raw.as_ptr()) != 0 }
    }

    pub fn set_bloom_enabled(&mut self, arg: bool) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSettingsView_SetBloomEnabled(
                self.raw.as_ptr(),
                if arg { 1 } else { 0 },
            );
        }
    }

    pub fn ibl_mode(&self) -> IblMode {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            IblMode::try_from(ffi::whiteout_flakes_FlakesSettingsView_IblMode(
                self.raw.as_ptr(),
            ))
            .expect("unknown enum discriminant from the native library (ABI version skew)")
        }
    }

    pub fn set_ibl_mode(&mut self, arg: IblMode) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSettingsView_SetIblMode(self.raw.as_ptr(), arg as i32);
        }
    }

    /// HD-shader debug mode (0 = off, 1..7 = visualisations).
    pub fn hd_debug_mode(&self) -> i32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesSettingsView_HdDebugMode(self.raw.as_ptr()) }
    }

    pub fn set_hd_debug_mode(&mut self, arg: i32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSettingsView_SetHdDebugMode(self.raw.as_ptr(), arg);
        }
    }

    /// GTAO quality preset (0 = Low, 1 = Medium, 2 = High).
    pub fn ao_quality(&self) -> u32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesSettingsView_AoQuality(self.raw.as_ptr()) }
    }

    pub fn set_ao_quality(&mut self, arg: u32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSettingsView_SetAoQuality(self.raw.as_ptr(), arg);
        }
    }

    /// LOD override (`-1` = auto-pick by screen size, `0..3` = forced).
    pub fn lod_override(&self) -> i32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesSettingsView_LodOverride(self.raw.as_ptr()) }
    }

    pub fn set_lod_override(&mut self, arg: i32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSettingsView_SetLodOverride(self.raw.as_ptr(), arg);
        }
    }

    pub fn set_render_mode(&mut self, arg: RenderMode) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSettingsView_SetRenderMode(self.raw.as_ptr(), arg as i32);
        }
    }
}

/// Spawn / refresh / clear actors.
pub struct LoaderView {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesLoaderView>,
}

impl Drop for LoaderView {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesLoaderView_delete(self.raw.as_ptr()) }
    }
}

impl LoaderView {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesLoaderView) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| LoaderView { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for LoaderView {}

impl core::fmt::Debug for LoaderView {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("LoaderView").finish_non_exhaustive()
    }
}

impl LoaderView {
    /// Spawn an actor from a path resolvable by the content provider. @return New actor handle, or `0` on failure.
    pub fn spawn_unit(&mut self, path: &str) -> u32 {
        let path_cstr = std::ffi::CString::new(path).unwrap_or_default();
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesLoaderView_SpawnUnit(self.raw.as_ptr(), path_cstr.as_ptr())
        }
    }

    /// Spawn a standalone corn effect (`.pkb` / `.pkfx`) as its own actor, with no model around it.
    ///
    /// A corn effect is normally reached through a model: an MDX's `CORN` chunk names the `.pkb` and the loader stages an emitter for it. This plays one on its own — the same thing the viewer does when you open a `.pkb` directly.
    ///
    /// Equivalent to `SpawnUnitFromSource` with the library's `CornEffectSource`, and exists because bindings cannot implement @ref IModelSource themselves. The actor carries a single always-on emitter and one looping placeholder sequence to keep it ticking; the effect's own runtime decides whether it loops or plays once.
    ///
    /// @param path Resolvable by the content provider, exactly like @ref SpawnUnit.
    pub fn spawn_effect(&mut self, path: &str) -> u32 {
        let path_cstr = std::ffi::CString::new(path).unwrap_or_default();
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesLoaderView_SpawnEffect(self.raw.as_ptr(), path_cstr.as_ptr())
        }
    }

    /// Defer-destroy every actor currently in the scene.
    pub fn request_clear_all(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesLoaderView_RequestClearAll(self.raw.as_ptr());
        }
    }

    /// Destroy a single actor by handle. Used by JS wrappers that expose mdx-m3-viewer-style `instance.detach()` / `instance.hide()` semantics.
    pub fn destroy(&mut self, handle: u32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesLoaderView_Destroy(self.raw.as_ptr(), handle);
        }
    }
}

/// Push-based asset registry view.
///
/// Renderer subsystems call into `AssetManager` directly to Acquire slots; the host uses this view to: * pump the needs queue (paths the renderer wants the host to fetch), * push fetched bytes back in via `ApplyAsset`.
///
/// Stays valid for the renderer's lifetime; cheap to copy.
pub struct AssetsView {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesAssetsView>,
}

impl Drop for AssetsView {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesAssetsView_delete(self.raw.as_ptr()) }
    }
}

impl AssetsView {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesAssetsView) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| AssetsView { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for AssetsView {}

impl core::fmt::Debug for AssetsView {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("AssetsView").finish_non_exhaustive()
    }
}

impl AssetsView {
    pub fn stats(&self) -> Option<AssetsViewStats> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            AssetsViewStats::from_raw(ffi::whiteout_flakes_FlakesAssetsView_Stats(
                self.raw.as_ptr(),
            ))
        }
    }

    /// Acquire slots for every SPL/UBR texture and SPN child-model referenced by the loaded event-data SLKs. The slots are held by the event-data cache for the rest of the session, so the host pump fetches them eagerly and they survive animation changes. Call after `LoadEventDataFiles` finishes populating the splat tables.
    pub fn prefetch_event_assets(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesAssetsView_PrefetchEventAssets(self.raw.as_ptr());
        }
    }

    /// Per-actor variant — only Acquire slots for the SPL/UBR/SPN/FPT events the actor's template actually references. Use this instead of `PrefetchEventAssets()` on backends where every Acquire costs a network fetch (web viewer hitting Hive's CASC mirror); the global variant would pull in every entry in the SLK tables regardless of whether the loaded model fires them.
    pub fn prefetch_event_assets_for_actor(&mut self, actor: u32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesAssetsView_PrefetchEventAssetsForActor(
                self.raw.as_ptr(),
                actor,
            );
        }
    }
}

/// Snapshot diagnostic counters.
pub struct AssetsViewStats {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesAssetsViewStats>,
}

impl Drop for AssetsViewStats {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesAssetsViewStats_delete(self.raw.as_ptr()) }
    }
}

impl AssetsViewStats {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesAssetsViewStats) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| AssetsViewStats { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for AssetsViewStats {}

impl core::fmt::Debug for AssetsViewStats {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("AssetsViewStats").finish_non_exhaustive()
    }
}

impl AssetsViewStats {
    /// # Panics
    /// Panics if the native allocation fails.
    pub fn new() -> Self {
        // SAFETY: the native constructor returns a live handle; a null here
        // means the library is unusable.
        unsafe {
            let raw = ffi::whiteout_flakes_FlakesAssetsViewStats_new();
            Self::from_raw(raw).expect("native AssetsViewStats allocation failed")
        }
    }

    pub fn live_slots(&self) -> u64 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesAssetsViewStats_get_liveSlots(self.raw.as_ptr()) }
    }

    pub fn set_live_slots(&mut self, value: u64) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesAssetsViewStats_set_liveSlots(self.raw.as_ptr(), value)
        }
    }

    pub fn loaded_slots(&self) -> u64 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesAssetsViewStats_get_loadedSlots(self.raw.as_ptr()) }
    }

    pub fn set_loaded_slots(&mut self, value: u64) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesAssetsViewStats_set_loadedSlots(self.raw.as_ptr(), value)
        }
    }

    pub fn pending_needs(&self) -> u64 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesAssetsViewStats_get_pendingNeeds(self.raw.as_ptr()) }
    }

    pub fn set_pending_needs(&mut self, value: u64) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesAssetsViewStats_set_pendingNeeds(self.raw.as_ptr(), value)
        }
    }

    pub fn total_acquires(&self) -> u64 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesAssetsViewStats_get_totalAcquires(self.raw.as_ptr()) }
    }

    pub fn set_total_acquires(&mut self, value: u64) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesAssetsViewStats_set_totalAcquires(self.raw.as_ptr(), value)
        }
    }

    pub fn total_releases(&self) -> u64 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesAssetsViewStats_get_totalReleases(self.raw.as_ptr()) }
    }

    pub fn set_total_releases(&mut self, value: u64) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesAssetsViewStats_set_totalReleases(self.raw.as_ptr(), value)
        }
    }

    pub fn total_applies(&self) -> u64 {
        // SAFETY: plain scalar read through a live handle.
        unsafe { ffi::whiteout_flakes_FlakesAssetsViewStats_get_totalApplies(self.raw.as_ptr()) }
    }

    pub fn set_total_applies(&mut self, value: u64) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesAssetsViewStats_set_totalApplies(self.raw.as_ptr(), value)
        }
    }

    pub fn total_apply_misses(&self) -> u64 {
        // SAFETY: plain scalar read through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesAssetsViewStats_get_totalApplyMisses(self.raw.as_ptr())
        }
    }

    pub fn set_total_apply_misses(&mut self, value: u64) {
        // SAFETY: plain scalar write through a live handle.
        unsafe {
            ffi::whiteout_flakes_FlakesAssetsViewStats_set_totalApplyMisses(
                self.raw.as_ptr(),
                value,
            )
        }
    }
}

impl Default for AssetsViewStats {
    fn default() -> Self {
        Self::new()
    }
}

/// Day-night-cycle service.
pub struct DncView {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesDncView>,
}

impl Drop for DncView {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesDncView_delete(self.raw.as_ptr()) }
    }
}

impl DncView {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesDncView) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| DncView { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for DncView {}

impl core::fmt::Debug for DncView {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("DncView").finish_non_exhaustive()
    }
}

impl DncView {
    /// `true` once the host has set a unit-MDL path and the DNC service has loaded successfully.
    pub fn is_valid(&self) -> bool {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesDncView_IsValid(self.raw.as_ptr()) != 0 }
    }

    /// Time of day in hours (0..@ref GetHoursPerDay).
    pub fn time_of_day(&self) -> f32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesDncView_TimeOfDay(self.raw.as_ptr()) }
    }

    pub fn set_time_of_day(&mut self, arg: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesDncView_SetTimeOfDay(self.raw.as_ptr(), arg);
        }
    }

    /// TOD playback rate (`0` = paused; `1` = real-time-equivalent).
    pub fn tod_scale(&self) -> f32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesDncView_TodScale(self.raw.as_ptr()) }
    }

    pub fn set_tod_scale(&mut self, arg: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesDncView_SetTodScale(self.raw.as_ptr(), arg);
        }
    }

    /// Length of a full DNC cycle in hours (typically `24`).
    pub fn hours_per_day(&self) -> f32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesDncView_HoursPerDay(self.raw.as_ptr()) }
    }

    /// Replace the DNC unit-MDL path (re-loads from the content provider).
    pub fn set_unit_mdl(&mut self, arg: &str) {
        let arg_cstr = std::ffi::CString::new(arg).unwrap_or_default();
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesDncView_SetUnitMdl(self.raw.as_ptr(), arg_cstr.as_ptr());
        }
    }

    /// Advance the DNC clock by @p dt seconds.
    pub fn advance(&mut self, dt: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesDncView_Advance(self.raw.as_ptr(), dt);
        }
    }
}

/// Cascade-shadow-map service.
pub struct ShadowView {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesShadowView>,
}

impl Drop for ShadowView {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesShadowView_delete(self.raw.as_ptr()) }
    }
}

impl ShadowView {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesShadowView) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| ShadowView { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for ShadowView {}

impl core::fmt::Debug for ShadowView {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("ShadowView").finish_non_exhaustive()
    }
}

impl ShadowView {
    pub fn is_valid(&self) -> bool {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesShadowView_IsValid(self.raw.as_ptr()) != 0 }
    }

    pub fn is_enabled(&self) -> bool {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesShadowView_IsEnabled(self.raw.as_ptr()) != 0 }
    }

    pub fn set_enabled(&mut self, on: bool) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesShadowView_SetEnabled(
                self.raw.as_ptr(),
                if on { 1 } else { 0 },
            );
        }
    }

    pub fn set_params(&mut self, arg: &ShadowParams) {
        let arg_native = unsafe { arg.to_native() };
        // SAFETY: handle is live for the call; the staged
        // option handles are freed immediately after.
        unsafe {
            ffi::whiteout_flakes_FlakesShadowView_SetParams(self.raw.as_ptr(), arg_native);
            ShadowParams::free_native(arg_native);
        }
    }
}

/// Splat-decal service (engine-spawned SPL/UBR events).
pub struct SplatView {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesSplatView>,
}

impl Drop for SplatView {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesSplatView_delete(self.raw.as_ptr()) }
    }
}

impl SplatView {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesSplatView) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| SplatView { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for SplatView {}

impl core::fmt::Debug for SplatView {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("SplatView").finish_non_exhaustive()
    }
}

impl SplatView {
    /// Remove every live splat. Hosts call this on sequence change to avoid carrying decay-only splats across cuts.
    pub fn clear(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesSplatView_Clear(self.raw.as_ptr());
        }
    }
}

/// Replaceable-texture / tileset switching.
pub struct ReplaceablesView {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesReplaceablesView>,
}

impl Drop for ReplaceablesView {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesReplaceablesView_delete(self.raw.as_ptr()) }
    }
}

impl ReplaceablesView {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesReplaceablesView) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| ReplaceablesView { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for ReplaceablesView {}

impl core::fmt::Debug for ReplaceablesView {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("ReplaceablesView").finish_non_exhaustive()
    }
}

impl ReplaceablesView {
    /// Returns `true` once when the tileset has changed since last call (hosts use this to invalidate cached swatches).
    pub fn consume_dirty(&mut self) -> bool {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesReplaceablesView_ConsumeDirty(self.raw.as_ptr()) != 0 }
    }

    /// Switch the active tileset; in-scene replaceables re-resolve on the next frame.
    pub fn set_tileset(&mut self, arg: Tileset) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesReplaceablesView_SetTileset(self.raw.as_ptr(), arg as i32);
        }
    }
}

/// Transport controls for the scene clock — one switch that governs model animation and every effect system alike.
///
/// A frame advances in two halves: `SceneView::Update` moves the animation clocks, `Renderer::Tick` moves the particle, PE1, ribbon and corn effects simulations. Both read the state set here, so a host cannot pause one and leave the other running.
///
/// @code r.Playback().Pause();     // freeze mid-animation, particles hang in air r.Playback().Play();      // carry on from that instant r.Playback().Restart();   // rewind to the top and play r.Playback().Stop();      // rewind to the top and hold @endcode
pub struct PlaybackView {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesPlaybackView>,
}

impl Drop for PlaybackView {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesPlaybackView_delete(self.raw.as_ptr()) }
    }
}

impl PlaybackView {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesPlaybackView) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| PlaybackView { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for PlaybackView {}

impl core::fmt::Debug for PlaybackView {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("PlaybackView").finish_non_exhaustive()
    }
}

impl PlaybackView {
    /// Current transport state.
    pub fn state(&self) -> PlaybackState {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            PlaybackState::try_from(ffi::whiteout_flakes_FlakesPlaybackView_State(
                self.raw.as_ptr(),
            ))
            .expect("unknown enum discriminant from the native library (ABI version skew)")
        }
    }

    /// Set the transport state directly. Entering @ref PlaybackState::Stopped rewinds; the other transitions do not.
    pub fn set_state(&mut self, arg: PlaybackState) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPlaybackView_SetState(self.raw.as_ptr(), arg as i32);
        }
    }

    /// Resume (or continue) advancing time.
    pub fn play(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPlaybackView_Play(self.raw.as_ptr());
        }
    }

    /// Hold time at the current instant. Resuming continues from here.
    pub fn pause(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPlaybackView_Pause(self.raw.as_ptr());
        }
    }

    /// Rewind to the start and hold. Animation cursors return to zero and transient effect state — live particles, ribbon trails, PE1 instances, corn effects runtimes — is discarded, so nothing is left hanging in mid-air.
    pub fn stop(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPlaybackView_Stop(self.raw.as_ptr());
        }
    }

    /// Rewind to the start and play. Equivalent to `Stop()` then `Play()`, and the usual way to replay a one-shot effect.
    pub fn restart(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPlaybackView_Restart(self.raw.as_ptr());
        }
    }

    /// Drop transient effect state so the effect systems replay from the cursor's current position.
    ///
    /// For hosts that move the clock themselves — a timeline scrub — rather than through this view. Particles, ribbons and corn effects are forward simulations with no seek: jumping the animation cursor leaves them mid-flight from wherever they were, which reads as the effects ignoring the scrub. This restarts them. The clock is untouched.
    pub fn resync_effects(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPlaybackView_ResyncEffects(self.raw.as_ptr());
        }
    }

    /// `true` when time is not advancing (paused *or* stopped).
    pub fn is_paused(&self) -> bool {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesPlaybackView_IsPaused(self.raw.as_ptr()) != 0 }
    }

    /// Multiplier applied to every advancing frame. Survives a pause/resume, so slow-motion is not lost on Play().
    pub fn time_scale(&self) -> f32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesPlaybackView_TimeScale(self.raw.as_ptr()) }
    }

    pub fn set_time_scale(&mut self, arg: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesPlaybackView_SetTimeScale(self.raw.as_ptr(), arg);
        }
    }
}

/// Value-typed view onto one actor.
///
/// Constructed by `Renderer::Actor(handle)`. Cheap to copy; under the hood it's just `{ RendererImpl*, ActorHandle }`. Lifetime mirrors the renderer's — copies become invalid when the actor is removed.
pub struct ActorView {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesActorView>,
}

impl Drop for ActorView {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesActorView_delete(self.raw.as_ptr()) }
    }
}

impl ActorView {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesActorView) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| ActorView { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for ActorView {}

impl core::fmt::Debug for ActorView {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("ActorView").finish_non_exhaustive()
    }
}

impl ActorView {
    /// `true` if the underlying actor still exists in the scene.
    pub fn is_valid(&self) -> bool {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesActorView_IsValid(self.raw.as_ptr()) != 0 }
    }

    /// The handle this view was constructed with.
    pub fn handle(&self) -> u32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesActorView_Handle(self.raw.as_ptr()) }
    }

    /// Role assigned at spawn time (see @ref ActorRole).
    pub fn role(&self) -> ActorRole {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ActorRole::try_from(ffi::whiteout_flakes_FlakesActorView_Role(self.raw.as_ptr()))
                .expect("unknown enum discriminant from the native library (ABI version skew)")
        }
    }

    /// Animation playback rate (`1.0` = nominal).
    pub fn playback_speed(&self) -> f32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesActorView_PlaybackSpeed(self.raw.as_ptr()) }
    }

    pub fn set_playback_speed(&mut self, arg: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesActorView_SetPlaybackSpeed(self.raw.as_ptr(), arg);
        }
    }

    /// If `true`, non-looping sequences hold their last frame instead of restarting from the beginning.
    pub fn ignore_non_looping(&self) -> bool {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesActorView_IgnoreNonLooping(self.raw.as_ptr()) != 0 }
    }

    pub fn set_ignore_non_looping(&mut self, arg: bool) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesActorView_SetIgnoreNonLooping(
                self.raw.as_ptr(),
                if arg { 1 } else { 0 },
            );
        }
    }

    /// Packed 0x00BBGGRR team colour (low 24 bits used).
    pub fn team_color(&self) -> u32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesActorView_TeamColor(self.raw.as_ptr()) }
    }

    /// Set the team colour from sRGB byte components.
    pub fn set_team_color(&mut self, r: u8, g: u8, b: u8) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesActorView_SetTeamColor(self.raw.as_ptr(), r, g, b);
        }
    }

    /// Mark this actor as host-driven so `Renderer::Tick` skips evaluating it. Used by the Max plugin, which receives time scrubs from Max's timeline and evaluates manually via @ref EvaluateAndApply.
    pub fn set_role_external(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesActorView_SetRoleExternal(self.raw.as_ptr());
        }
    }

    /// @name Animation cursor @{
    pub fn sequences(&self) -> Option<SequenceInfoList> {
        // SAFETY: the native side transfers ownership of
        // the list; null means the operation produced none.
        unsafe {
            SequenceInfoList::from_raw(ffi::whiteout_flakes_FlakesActorView_Sequences(
                self.raw.as_ptr(),
            ))
        }
    }

    pub fn active_sequence_index(&self) -> i32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesActorView_ActiveSequenceIndex(self.raw.as_ptr()) }
    }

    pub fn set_active_sequence(&mut self, arg: i32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesActorView_SetActiveSequence(self.raw.as_ptr(), arg);
        }
    }

    pub fn animation_time_ms(&self) -> i32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesActorView_AnimationTimeMs(self.raw.as_ptr()) }
    }

    pub fn set_animation_time_ms(&mut self, arg: i32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesActorView_SetAnimationTimeMs(self.raw.as_ptr(), arg);
        }
    }

    /// `true` once the actor has an `IAnimationSource` bound (i.e. spawn-from-source completed successfully).
    pub fn has_animation_source(&self) -> bool {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesActorView_HasAnimationSource(self.raw.as_ptr()) != 0 }
    }

    /// Evaluate the animation at the actor's current cursor and push the result into the renderer state.
    ///
    /// Used by host-driven actors (Max-plugin timeline scrub). Call `SetAnimationTimeMs(t)` first if you want a specific time.
    pub fn evaluate_and_apply(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesActorView_EvaluateAndApply(self.raw.as_ptr());
        }
    }

    /// Convenience: `SetAnimationTimeMs(t)` + `EvaluateAndApply()`.
    pub fn evaluate_at(&mut self, time_ms: i32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesActorView_EvaluateAt(self.raw.as_ptr(), time_ms);
        }
    }

    /// @name Read-only counts for status displays. @{
    pub fn geoset_count(&self) -> i32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesActorView_GeosetCount(self.raw.as_ptr()) }
    }

    pub fn material_count(&self) -> i32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesActorView_MaterialCount(self.raw.as_ptr()) }
    }

    pub fn collision_shape_count(&self) -> i32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesActorView_CollisionShapeCount(self.raw.as_ptr()) }
    }

    /// Camera presets attached to this actor's source model.
    pub fn camera_presets(&self) -> Option<CameraPresetList> {
        // SAFETY: the native side transfers ownership of
        // the list; null means the operation produced none.
        unsafe {
            CameraPresetList::from_raw(ffi::whiteout_flakes_FlakesActorView_CameraPresets(
                self.raw.as_ptr(),
            ))
        }
    }

    /// Render mode the actor's template expects (`HD` if any material layer uses a non-zero BLS shaderId, else `SD`). Hosts call this after `SpawnUnit` and forward to `SettingsView::SetRenderMode` so SD models don't render through the HD pipeline (which mis-blends multi-layer SD materials) and vice-versa.
    pub fn preferred_render_mode(&self) -> RenderMode {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            RenderMode::try_from(ffi::whiteout_flakes_FlakesActorView_PreferredRenderMode(
                self.raw.as_ptr(),
            ))
            .expect("unknown enum discriminant from the native library (ABI version skew)")
        }
    }

    /// Every child-model path this actor's template will eventually need: attachment slots (`AttachmentConfig`) and legacy PE1 particle emitters (`PE1EmitterConfig`). Hosts running an async loader (web build) use this to eagerly prefetch the child MDX bytes so the first attachment spawn / first PE1 fire lands in a primed cache rather than triggering a miss.
    pub fn child_model_paths(&self) -> Vec<String> {
        // SAFETY: one call materialises the list; the
        // elements are borrowed out of it and it is freed
        // before returning. Reading is O(1) per element.
        unsafe {
            let list = ffi::whiteout_flakes_FlakesActorView_ChildModelPaths(self.raw.as_ptr());
            if list.is_null() {
                return Vec::new();
            }
            let n = ffi::whiteout_flakes_StringList_size(list);
            let out = (0..n)
                .map(|i| crate::support::take_string(ffi::whiteout_flakes_StringList_at(list, i)))
                .collect();
            ffi::whiteout_flakes_StringList_delete(list);
            out
        }
    }
}

/// Top-level WhiteoutFlakes renderer.
///
/// Construct one per process / per host. Holds every internal subsystem behind a stable PIMPL pointer; copies are disallowed.
pub struct Renderer {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesRenderer>,
}

impl Drop for Renderer {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesRenderer_delete(self.raw.as_ptr()) }
    }
}

impl Renderer {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesRenderer) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| Renderer { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for Renderer {}

impl core::fmt::Debug for Renderer {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("Renderer").finish_non_exhaustive()
    }
}

impl Renderer {
    /// # Panics
    /// Panics if the native allocation fails.
    pub fn new() -> Self {
        // SAFETY: the native constructor returns a live handle; a null here
        // means the library is unusable.
        unsafe {
            let raw = ffi::whiteout_flakes_FlakesRenderer_new();
            Self::from_raw(raw).expect("native Renderer allocation failed")
        }
    }

    /// @name Sub-service handles Each call returns a small value-typed view; the underlying impl is shared with the owning `Renderer`. Views are cheap to copy. @{
    pub fn pipeline(&mut self) -> Option<PipelineView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            PipelineView::from_raw(ffi::whiteout_flakes_FlakesRenderer_Pipeline(
                self.raw.as_ptr(),
            ))
        }
    }

    pub fn scene(&mut self) -> Option<SceneView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe { SceneView::from_raw(ffi::whiteout_flakes_FlakesRenderer_Scene(self.raw.as_ptr())) }
    }

    /// View onto the scene's default camera (handle 0).
    pub fn camera(&mut self) -> Option<CameraView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            CameraView::from_raw(ffi::whiteout_flakes_FlakesRenderer_Camera(
                self.raw.as_ptr(),
            ))
        }
    }

    /// Add a camera to the scene's set; returns its handle. Use for extra viewports onto the same scene (e.g. an editor's top/front/side/perspective layout). Pair with PipelineView::RenderViewport(target, handle).
    pub fn create_camera(&mut self) -> u32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesRenderer_CreateCamera(self.raw.as_ptr()) }
    }

    /// View onto a specific camera in the scene's set. An out-of-range handle drives the default camera.
    pub fn camera_at(&mut self, arg: u32) -> Option<CameraView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            CameraView::from_raw(ffi::whiteout_flakes_FlakesRenderer_CameraAt(
                self.raw.as_ptr(),
                arg,
            ))
        }
    }

    pub fn settings(&mut self) -> Option<SettingsView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            SettingsView::from_raw(ffi::whiteout_flakes_FlakesRenderer_Settings(
                self.raw.as_ptr(),
            ))
        }
    }

    pub fn loader(&mut self) -> Option<LoaderView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            LoaderView::from_raw(ffi::whiteout_flakes_FlakesRenderer_Loader(
                self.raw.as_ptr(),
            ))
        }
    }

    pub fn dnc(&mut self) -> Option<DncView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe { DncView::from_raw(ffi::whiteout_flakes_FlakesRenderer_Dnc(self.raw.as_ptr())) }
    }

    pub fn shadow(&mut self) -> Option<ShadowView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ShadowView::from_raw(ffi::whiteout_flakes_FlakesRenderer_Shadow(
                self.raw.as_ptr(),
            ))
        }
    }

    pub fn splats(&mut self) -> Option<SplatView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            SplatView::from_raw(ffi::whiteout_flakes_FlakesRenderer_Splats(
                self.raw.as_ptr(),
            ))
        }
    }

    pub fn replaceables(&mut self) -> Option<ReplaceablesView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ReplaceablesView::from_raw(ffi::whiteout_flakes_FlakesRenderer_Replaceables(
                self.raw.as_ptr(),
            ))
        }
    }

    pub fn assets(&mut self) -> Option<AssetsView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            AssetsView::from_raw(ffi::whiteout_flakes_FlakesRenderer_Assets(
                self.raw.as_ptr(),
            ))
        }
    }

    /// Play / pause / stop / restart the scene clock, for models and effects together.
    pub fn playback(&mut self) -> Option<PlaybackView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            PlaybackView::from_raw(ffi::whiteout_flakes_FlakesRenderer_Playback(
                self.raw.as_ptr(),
            ))
        }
    }

    /// Per-actor view. Returns an invalid view (`IsValid() == false`) for unknown handles.
    pub fn actor(&mut self, h: u32) -> Option<ActorView> {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ActorView::from_raw(ffi::whiteout_flakes_FlakesRenderer_Actor(
                self.raw.as_ptr(),
                h,
            ))
        }
    }

    /// Per-frame entry point.
    ///
    /// Drives animation, attachment-graph propagation, particle / PE1 / ribbon / corn-effects simulation, splat decay, DNC advancement. Hosts that drive their own actor evaluation (e.g. the Max plugin, which receives time scrubs from the host timeline) can skip `Tick` and call @ref ActorView::EvaluateAndApply manually. @param dt Elapsed time since the last tick, in seconds.
    pub fn tick(&mut self, dt: f32) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesRenderer_Tick(self.raw.as_ptr(), dt);
        }
    }
}

impl Default for Renderer {
    fn default() -> Self {
        Self::new()
    }
}

/// Browses a CASC install, an MPQ archive, or a directory as one folder tree of model and effect files.
///
/// The Reforged TVFS encodes the mod chain with `:` and the content tree with `\` — `war3.w3mod:_hd.w3mod:units\nightelf\druid\druid.mdx`. This walks the archive once on open, keeps only the folders that lead to a `.mdx`, `.mdl`, `.pkb` or `.pkfx`, and presents them as directories you can descend into. Display paths drop the leading `war3.w3mod:` and use `\` throughout; each file leaf remembers its original archive path, which is what the renderer reads.
///
/// The browser is standalone — it opens its own handle on the archive and needs no `Renderer`. To render what you pick, point a scene at the same install and spawn the path this hands you:
///
/// @code StorageBrowser br; if (!br.OpenAuto(R"(C:\Program Files\Warcraft III)")) return br.LastError(); br.Descend("units"); const auto path = br.ChildPath(br.Files()[0]);
///
/// r.Scene().SetCascInstallPath(br.Root()); r.Loader().SpawnUnit(path); @endcode
pub struct StorageBrowser {
    pub(crate) raw: core::ptr::NonNull<ffi::whiteout_FlakesStorageBrowser>,
}

impl Drop for StorageBrowser {
    fn drop(&mut self) {
        // SAFETY: `raw` came from a native constructor and Drop runs once.
        unsafe { ffi::whiteout_flakes_FlakesStorageBrowser_delete(self.raw.as_ptr()) }
    }
}

impl StorageBrowser {
    /// # Safety
    /// `raw` must be a live handle this value takes ownership of.
    #[allow(dead_code)] // used by whichever methods return this type
    pub(crate) unsafe fn from_raw(raw: *mut ffi::whiteout_FlakesStorageBrowser) -> Option<Self> {
        core::ptr::NonNull::new(raw).map(|raw| StorageBrowser { raw })
    }
}

// SAFETY: handles are plain heap pointers with no thread affinity. `Sync`
// is deliberately NOT implemented — the C++ types make no documented
// guarantee about concurrent use, and claiming one we haven't verified
// would be unsound. See `@bind thread_safe` in the plan.
unsafe impl Send for StorageBrowser {}

impl core::fmt::Debug for StorageBrowser {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("StorageBrowser").finish_non_exhaustive()
    }
}

impl StorageBrowser {
    /// # Panics
    /// Panics if the native allocation fails.
    pub fn new() -> Self {
        // SAFETY: the native constructor returns a live handle; a null here
        // means the library is unusable.
        unsafe {
            let raw = ffi::whiteout_flakes_FlakesStorageBrowser_new();
            Self::from_raw(raw).expect("native StorageBrowser allocation failed")
        }
    }

    /// Open @p root as @p kind and build the folder tree.
    ///
    /// For @ref StorageKind::Casc, @p root is the directory holding `.build.info` (or its `Data` subdirectory); for @ref StorageKind::Mpq, the archive file; for @ref StorageKind::Folder, the directory to walk.
    ///
    /// Enumerating is the expensive part and happens here, once. Returns `false` on failure; @ref GetLastError says why.
    pub fn open(&mut self, root: &str, kind: StorageKind) -> bool {
        let root_cstr = std::ffi::CString::new(root).unwrap_or_default();
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesStorageBrowser_Open(
                self.raw.as_ptr(),
                root_cstr.as_ptr(),
                kind as i32,
            ) != 0
        }
    }

    /// Work out what @p path is and open it.
    ///
    /// A directory holding `.build.info` is a CASC install, any other directory is walked as a folder, and a file is treated as an MPQ. What a dialog wants when the user has just typed or dropped a path.
    pub fn open_auto(&mut self, path: &str) -> bool {
        let path_cstr = std::ffi::CString::new(path).unwrap_or_default();
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesStorageBrowser_OpenAuto(
                self.raw.as_ptr(),
                path_cstr.as_ptr(),
            ) != 0
        }
    }

    /// What the open storage turned out to be.
    pub fn kind(&self) -> StorageKind {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            StorageKind::try_from(ffi::whiteout_flakes_FlakesStorageBrowser_Kind(
                self.raw.as_ptr(),
            ))
            .expect("unknown enum discriminant from the native library (ABI version skew)")
        }
    }

    /// Why the last @ref Open failed. Empty after a successful one.
    pub fn is_open(&self) -> bool {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesStorageBrowser_IsOpen(self.raw.as_ptr()) != 0 }
    }

    /// The root this was opened with.
    pub fn root(&self) -> String {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            crate::support::take_string(ffi::whiteout_flakes_FlakesStorageBrowser_Root(
                self.raw.as_ptr(),
            ))
        }
    }

    /// Message from the last failed @ref Open, or empty.
    pub fn last_error(&self) -> String {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            crate::support::take_string(ffi::whiteout_flakes_FlakesStorageBrowser_LastError(
                self.raw.as_ptr(),
            ))
        }
    }

    /// @name Navigation @{ @brief Current directory in display form (`\`-separated, empty at the root).
    pub fn current_path(&self) -> String {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            crate::support::take_string(ffi::whiteout_flakes_FlakesStorageBrowser_CurrentPath(
                self.raw.as_ptr(),
            ))
        }
    }

    /// The current path split into segments, for a breadcrumb bar.
    pub fn breadcrumb(&self) -> Vec<String> {
        // SAFETY: one call materialises the list; the
        // elements are borrowed out of it and it is freed
        // before returning. Reading is O(1) per element.
        unsafe {
            let list = ffi::whiteout_flakes_FlakesStorageBrowser_Breadcrumb(self.raw.as_ptr());
            if list.is_null() {
                return Vec::new();
            }
            let n = ffi::whiteout_flakes_StringList_size(list);
            let out = (0..n)
                .map(|i| crate::support::take_string(ffi::whiteout_flakes_StringList_at(list, i)))
                .collect();
            ffi::whiteout_flakes_StringList_delete(list);
            out
        }
    }

    /// Immediate subfolder names of the current directory.
    pub fn folders(&self) -> Vec<String> {
        // SAFETY: one call materialises the list; the
        // elements are borrowed out of it and it is freed
        // before returning. Reading is O(1) per element.
        unsafe {
            let list = ffi::whiteout_flakes_FlakesStorageBrowser_Folders(self.raw.as_ptr());
            if list.is_null() {
                return Vec::new();
            }
            let n = ffi::whiteout_flakes_StringList_size(list);
            let out = (0..n)
                .map(|i| crate::support::take_string(ffi::whiteout_flakes_StringList_at(list, i)))
                .collect();
            ffi::whiteout_flakes_StringList_delete(list);
            out
        }
    }

    /// File names directly inside the current directory, subject to @ref GetFilter. Names only — pair with @ref ChildPath to load one.
    pub fn files(&self) -> Vec<String> {
        // SAFETY: one call materialises the list; the
        // elements are borrowed out of it and it is freed
        // before returning. Reading is O(1) per element.
        unsafe {
            let list = ffi::whiteout_flakes_FlakesStorageBrowser_Files(self.raw.as_ptr());
            if list.is_null() {
                return Vec::new();
            }
            let n = ffi::whiteout_flakes_StringList_size(list);
            let out = (0..n)
                .map(|i| crate::support::take_string(ffi::whiteout_flakes_StringList_at(list, i)))
                .collect();
            ffi::whiteout_flakes_StringList_delete(list);
            out
        }
    }

    /// Restrict @ref Files to models, to effects, or to neither.
    ///
    /// Filters the file list only; @ref Folders is unchanged, so a folder whose contents are all filtered out still appears and simply reads as empty. That keeps the tree stable while the filter is toggled, which is what a panel wants — hiding folders would make the shape of the archive jump around.
    ///
    /// Costs nothing to change: the archive is walked once at @ref Open and the filter is applied per call.
    pub fn set_filter(&mut self, filter: StorageFileFilter) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesStorageBrowser_SetFilter(self.raw.as_ptr(), filter as i32);
        }
    }

    pub fn filter(&self) -> StorageFileFilter {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            StorageFileFilter::try_from(ffi::whiteout_flakes_FlakesStorageBrowser_Filter(
                self.raw.as_ptr(),
            ))
            .expect("unknown enum discriminant from the native library (ABI version skew)")
        }
    }

    /// How many files the current directory holds in total, ignoring the filter — so a panel can say "3 of 12" rather than making an empty folder look like a dead end.
    pub fn unfiltered_file_count(&self) -> i32 {
        // SAFETY: handle is live for the duration of the call.
        unsafe { ffi::whiteout_flakes_FlakesStorageBrowser_UnfilteredFileCount(self.raw.as_ptr()) }
    }

    /// Descend into a subfolder of the current directory. No-op for a name that is not in @ref Folders.
    pub fn descend(&mut self, folder_name: &str) {
        let folder_name_cstr = std::ffi::CString::new(folder_name).unwrap_or_default();
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesStorageBrowser_Descend(
                self.raw.as_ptr(),
                folder_name_cstr.as_ptr(),
            );
        }
    }

    /// Go up one level. No-op at the root.
    pub fn ascend(&mut self) {
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesStorageBrowser_Ascend(self.raw.as_ptr());
        }
    }

    /// Jump to a display path, as returned by @ref GetCurrentPath.
    pub fn navigate_to(&mut self, display_path: &str) {
        let display_path_cstr = std::ffi::CString::new(display_path).unwrap_or_default();
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesStorageBrowser_NavigateTo(
                self.raw.as_ptr(),
                display_path_cstr.as_ptr(),
            );
        }
    }

    /// The original archive path of a file in the current directory — what `LoaderView::SpawnUnit` / `SpawnEffect` reads. Empty for a name that is not in @ref Files.
    pub fn child_path(&self, file_name: &str) -> String {
        let file_name_cstr = std::ffi::CString::new(file_name).unwrap_or_default();
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            crate::support::take_string(ffi::whiteout_flakes_FlakesStorageBrowser_ChildPath(
                self.raw.as_ptr(),
                file_name_cstr.as_ptr(),
            ))
        }
    }

    /// `true` if @p fileName is an effect (`.pkb` / `.pkfx`) rather than a model, so a host knows which spawn call to make.
    pub fn is_effect(&self, file_name: &str) -> bool {
        let file_name_cstr = std::ffi::CString::new(file_name).unwrap_or_default();
        // SAFETY: handle is live for the duration of the call.
        unsafe {
            ffi::whiteout_flakes_FlakesStorageBrowser_IsEffect(
                self.raw.as_ptr(),
                file_name_cstr.as_ptr(),
            ) != 0
        }
    }
}

impl Default for StorageBrowser {
    fn default() -> Self {
        Self::new()
    }
}

// Not yet bound (shape unsupported by the emitter):
//   - ActorView::SetTransform (parameter shape)
//   - ActorView::Transform (return whiteout::Matrix44f)
//   - AssetsView::ApplyAsset (parameter shape)
//   - AssetsView::DrainNeeds (parameter shape)
//   - AssetsView::IsTextureCached (parameter shape)
//   - CameraView::GetTarget (return whiteout::Vector3f)
//   - CameraView::SetDirectPose (parameter shape)
//   - LoaderView::SpawnUnitFromSource (parameter shape)
//   - LoaderView::UpdateMaterials (parameter shape)
//   - Renderer::SwapSoundEmitter (parameter shape)
//   - SceneView::SetContentProvider (parameter shape)
//   - SceneView::SetEngineAssetRoot (parameter shape)
//   - SceneView::SetPE1BasePath (parameter shape)
//   - ShadowView::Params (return whiteout::flakes::ShadowParams)

#[doc(hidden)]
pub mod ffi {
    #![allow(missing_debug_implementations)]

    #[allow(unused_imports)]
    use crate::support::{RawBytes, RawCString};

    #[repr(C)]
    pub struct whiteout_SequenceInfoList {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_CameraPresetList {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesRect {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesShadowParams {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesDisplayFlags {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesFrameStats {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesCameraPreset {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesSequenceInfo {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesPipelineView {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesSceneView {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesCameraView {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesSettingsView {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesLoaderView {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesAssetsView {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesAssetsViewStats {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesDncView {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesShadowView {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesSplatView {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesReplaceablesView {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesPlaybackView {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesActorView {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesRenderer {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_FlakesStorageBrowser {
        _private: [u8; 0],
    }
    #[repr(C)]
    pub struct whiteout_StringList {
        _private: [u8; 0],
    }

    extern "C" {
        pub fn whiteout_flakes_StringList_size(self_: *mut whiteout_StringList) -> usize;
        pub fn whiteout_flakes_StringList_at(
            self_: *mut whiteout_StringList,
            index: usize,
        ) -> RawCString;
        pub fn whiteout_flakes_StringList_delete(self_: *mut whiteout_StringList);
        pub fn whiteout_flakes_SequenceInfoList_size(
            self_: *mut whiteout_SequenceInfoList,
        ) -> usize;
        pub fn whiteout_flakes_SequenceInfoList_at(
            self_: *mut whiteout_SequenceInfoList,
            index: usize,
        ) -> *mut whiteout_FlakesSequenceInfo;
        pub fn whiteout_flakes_SequenceInfoList_delete(self_: *mut whiteout_SequenceInfoList);
        pub fn whiteout_flakes_CameraPresetList_size(
            self_: *mut whiteout_CameraPresetList,
        ) -> usize;
        pub fn whiteout_flakes_CameraPresetList_at(
            self_: *mut whiteout_CameraPresetList,
            index: usize,
        ) -> *mut whiteout_FlakesCameraPreset;
        pub fn whiteout_flakes_CameraPresetList_delete(self_: *mut whiteout_CameraPresetList);
        // Rect
        pub fn whiteout_flakes_FlakesRect_new() -> *mut whiteout_FlakesRect;
        pub fn whiteout_flakes_FlakesRect_delete(self_: *mut whiteout_FlakesRect);
        pub fn whiteout_flakes_FlakesRect_get_left(self_: *mut whiteout_FlakesRect) -> i32;
        pub fn whiteout_flakes_FlakesRect_set_left(self_: *mut whiteout_FlakesRect, value: i32);
        pub fn whiteout_flakes_FlakesRect_get_top(self_: *mut whiteout_FlakesRect) -> i32;
        pub fn whiteout_flakes_FlakesRect_set_top(self_: *mut whiteout_FlakesRect, value: i32);
        pub fn whiteout_flakes_FlakesRect_get_right(self_: *mut whiteout_FlakesRect) -> i32;
        pub fn whiteout_flakes_FlakesRect_set_right(self_: *mut whiteout_FlakesRect, value: i32);
        pub fn whiteout_flakes_FlakesRect_get_bottom(self_: *mut whiteout_FlakesRect) -> i32;
        pub fn whiteout_flakes_FlakesRect_set_bottom(self_: *mut whiteout_FlakesRect, value: i32);
        // ShadowParams
        pub fn whiteout_flakes_FlakesShadowParams_new() -> *mut whiteout_FlakesShadowParams;
        pub fn whiteout_flakes_FlakesShadowParams_delete(self_: *mut whiteout_FlakesShadowParams);
        pub fn whiteout_flakes_FlakesShadowParams_get_cascadeCount(
            self_: *mut whiteout_FlakesShadowParams,
        ) -> i32;
        pub fn whiteout_flakes_FlakesShadowParams_set_cascadeCount(
            self_: *mut whiteout_FlakesShadowParams,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesShadowParams_get_cascadeResolution(
            self_: *mut whiteout_FlakesShadowParams,
        ) -> i32;
        pub fn whiteout_flakes_FlakesShadowParams_set_cascadeResolution(
            self_: *mut whiteout_FlakesShadowParams,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesShadowParams_get_casterHeight(
            self_: *mut whiteout_FlakesShadowParams,
        ) -> f32;
        pub fn whiteout_flakes_FlakesShadowParams_set_casterHeight(
            self_: *mut whiteout_FlakesShadowParams,
            value: f32,
        );
        pub fn whiteout_flakes_FlakesShadowParams_get_lambdaSplit(
            self_: *mut whiteout_FlakesShadowParams,
        ) -> f32;
        pub fn whiteout_flakes_FlakesShadowParams_set_lambdaSplit(
            self_: *mut whiteout_FlakesShadowParams,
            value: f32,
        );
        pub fn whiteout_flakes_FlakesShadowParams_get_depthBias(
            self_: *mut whiteout_FlakesShadowParams,
        ) -> i32;
        pub fn whiteout_flakes_FlakesShadowParams_set_depthBias(
            self_: *mut whiteout_FlakesShadowParams,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesShadowParams_get_slopeScaledBias(
            self_: *mut whiteout_FlakesShadowParams,
        ) -> f32;
        pub fn whiteout_flakes_FlakesShadowParams_set_slopeScaledBias(
            self_: *mut whiteout_FlakesShadowParams,
            value: f32,
        );
        pub fn whiteout_flakes_FlakesShadowParams_get_depthBiasClamp(
            self_: *mut whiteout_FlakesShadowParams,
        ) -> f32;
        pub fn whiteout_flakes_FlakesShadowParams_set_depthBiasClamp(
            self_: *mut whiteout_FlakesShadowParams,
            value: f32,
        );
        pub fn whiteout_flakes_FlakesShadowParams_get_texelSnap(
            self_: *mut whiteout_FlakesShadowParams,
        ) -> i32;
        pub fn whiteout_flakes_FlakesShadowParams_set_texelSnap(
            self_: *mut whiteout_FlakesShadowParams,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesShadowParams_get_enabled(
            self_: *mut whiteout_FlakesShadowParams,
        ) -> i32;
        pub fn whiteout_flakes_FlakesShadowParams_set_enabled(
            self_: *mut whiteout_FlakesShadowParams,
            value: i32,
        );
        // DisplayFlags
        pub fn whiteout_flakes_FlakesDisplayFlags_new() -> *mut whiteout_FlakesDisplayFlags;
        pub fn whiteout_flakes_FlakesDisplayFlags_delete(self_: *mut whiteout_FlakesDisplayFlags);
        pub fn whiteout_flakes_FlakesDisplayFlags_get_showGrid(
            self_: *mut whiteout_FlakesDisplayFlags,
        ) -> i32;
        pub fn whiteout_flakes_FlakesDisplayFlags_set_showGrid(
            self_: *mut whiteout_FlakesDisplayFlags,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesDisplayFlags_get_showParticles(
            self_: *mut whiteout_FlakesDisplayFlags,
        ) -> i32;
        pub fn whiteout_flakes_FlakesDisplayFlags_set_showParticles(
            self_: *mut whiteout_FlakesDisplayFlags,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesDisplayFlags_get_showRibbons(
            self_: *mut whiteout_FlakesDisplayFlags,
        ) -> i32;
        pub fn whiteout_flakes_FlakesDisplayFlags_set_showRibbons(
            self_: *mut whiteout_FlakesDisplayFlags,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesDisplayFlags_get_showCollisions(
            self_: *mut whiteout_FlakesDisplayFlags,
        ) -> i32;
        pub fn whiteout_flakes_FlakesDisplayFlags_set_showCollisions(
            self_: *mut whiteout_FlakesDisplayFlags,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesDisplayFlags_get_showLights(
            self_: *mut whiteout_FlakesDisplayFlags,
        ) -> i32;
        pub fn whiteout_flakes_FlakesDisplayFlags_set_showLights(
            self_: *mut whiteout_FlakesDisplayFlags,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesDisplayFlags_get_showEvents(
            self_: *mut whiteout_FlakesDisplayFlags,
        ) -> i32;
        pub fn whiteout_flakes_FlakesDisplayFlags_set_showEvents(
            self_: *mut whiteout_FlakesDisplayFlags,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesDisplayFlags_get_renderMode(
            self_: *mut whiteout_FlakesDisplayFlags,
        ) -> i32;
        pub fn whiteout_flakes_FlakesDisplayFlags_set_renderMode(
            self_: *mut whiteout_FlakesDisplayFlags,
            value: i32,
        );
        // FrameStats
        pub fn whiteout_flakes_FlakesFrameStats_new() -> *mut whiteout_FlakesFrameStats;
        pub fn whiteout_flakes_FlakesFrameStats_delete(self_: *mut whiteout_FlakesFrameStats);
        pub fn whiteout_flakes_FlakesFrameStats_get_geosets(
            self_: *mut whiteout_FlakesFrameStats,
        ) -> i32;
        pub fn whiteout_flakes_FlakesFrameStats_set_geosets(
            self_: *mut whiteout_FlakesFrameStats,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesFrameStats_get_textures(
            self_: *mut whiteout_FlakesFrameStats,
        ) -> i32;
        pub fn whiteout_flakes_FlakesFrameStats_set_textures(
            self_: *mut whiteout_FlakesFrameStats,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesFrameStats_get_nodes(
            self_: *mut whiteout_FlakesFrameStats,
        ) -> i32;
        pub fn whiteout_flakes_FlakesFrameStats_set_nodes(
            self_: *mut whiteout_FlakesFrameStats,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesFrameStats_get_particles(
            self_: *mut whiteout_FlakesFrameStats,
        ) -> i32;
        pub fn whiteout_flakes_FlakesFrameStats_set_particles(
            self_: *mut whiteout_FlakesFrameStats,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesFrameStats_get_segments(
            self_: *mut whiteout_FlakesFrameStats,
        ) -> i32;
        pub fn whiteout_flakes_FlakesFrameStats_set_segments(
            self_: *mut whiteout_FlakesFrameStats,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesFrameStats_get_cornParticles(
            self_: *mut whiteout_FlakesFrameStats,
        ) -> i32;
        pub fn whiteout_flakes_FlakesFrameStats_set_cornParticles(
            self_: *mut whiteout_FlakesFrameStats,
            value: i32,
        );
        // CameraPreset
        pub fn whiteout_flakes_FlakesCameraPreset_new() -> *mut whiteout_FlakesCameraPreset;
        pub fn whiteout_flakes_FlakesCameraPreset_delete(self_: *mut whiteout_FlakesCameraPreset);
        pub fn whiteout_flakes_FlakesCameraPreset_get_name(
            self_: *mut whiteout_FlakesCameraPreset,
        ) -> RawCString;
        pub fn whiteout_flakes_FlakesCameraPreset_set_name(
            self_: *mut whiteout_FlakesCameraPreset,
            value: *const core::ffi::c_char,
        );
        pub fn whiteout_flakes_FlakesCameraPreset_get_isLive(
            self_: *mut whiteout_FlakesCameraPreset,
        ) -> i32;
        pub fn whiteout_flakes_FlakesCameraPreset_set_isLive(
            self_: *mut whiteout_FlakesCameraPreset,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesCameraPreset_get_fovDiagonal(
            self_: *mut whiteout_FlakesCameraPreset,
        ) -> f32;
        pub fn whiteout_flakes_FlakesCameraPreset_set_fovDiagonal(
            self_: *mut whiteout_FlakesCameraPreset,
            value: f32,
        );
        pub fn whiteout_flakes_FlakesCameraPreset_get_zNear(
            self_: *mut whiteout_FlakesCameraPreset,
        ) -> f32;
        pub fn whiteout_flakes_FlakesCameraPreset_set_zNear(
            self_: *mut whiteout_FlakesCameraPreset,
            value: f32,
        );
        pub fn whiteout_flakes_FlakesCameraPreset_get_zFar(
            self_: *mut whiteout_FlakesCameraPreset,
        ) -> f32;
        pub fn whiteout_flakes_FlakesCameraPreset_set_zFar(
            self_: *mut whiteout_FlakesCameraPreset,
            value: f32,
        );
        pub fn whiteout_flakes_FlakesCameraPreset_get_staticRoll(
            self_: *mut whiteout_FlakesCameraPreset,
        ) -> f32;
        pub fn whiteout_flakes_FlakesCameraPreset_set_staticRoll(
            self_: *mut whiteout_FlakesCameraPreset,
            value: f32,
        );
        pub fn whiteout_flakes_FlakesCameraPreset_get_pitch(
            self_: *mut whiteout_FlakesCameraPreset,
        ) -> f32;
        pub fn whiteout_flakes_FlakesCameraPreset_set_pitch(
            self_: *mut whiteout_FlakesCameraPreset,
            value: f32,
        );
        pub fn whiteout_flakes_FlakesCameraPreset_get_yaw(
            self_: *mut whiteout_FlakesCameraPreset,
        ) -> f32;
        pub fn whiteout_flakes_FlakesCameraPreset_set_yaw(
            self_: *mut whiteout_FlakesCameraPreset,
            value: f32,
        );
        pub fn whiteout_flakes_FlakesCameraPreset_get_distance(
            self_: *mut whiteout_FlakesCameraPreset,
        ) -> f32;
        pub fn whiteout_flakes_FlakesCameraPreset_set_distance(
            self_: *mut whiteout_FlakesCameraPreset,
            value: f32,
        );
        // SequenceInfo
        pub fn whiteout_flakes_FlakesSequenceInfo_new() -> *mut whiteout_FlakesSequenceInfo;
        pub fn whiteout_flakes_FlakesSequenceInfo_delete(self_: *mut whiteout_FlakesSequenceInfo);
        pub fn whiteout_flakes_FlakesSequenceInfo_get_name(
            self_: *mut whiteout_FlakesSequenceInfo,
        ) -> RawCString;
        pub fn whiteout_flakes_FlakesSequenceInfo_set_name(
            self_: *mut whiteout_FlakesSequenceInfo,
            value: *const core::ffi::c_char,
        );
        pub fn whiteout_flakes_FlakesSequenceInfo_get_startMs(
            self_: *mut whiteout_FlakesSequenceInfo,
        ) -> i32;
        pub fn whiteout_flakes_FlakesSequenceInfo_set_startMs(
            self_: *mut whiteout_FlakesSequenceInfo,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesSequenceInfo_get_endMs(
            self_: *mut whiteout_FlakesSequenceInfo,
        ) -> i32;
        pub fn whiteout_flakes_FlakesSequenceInfo_set_endMs(
            self_: *mut whiteout_FlakesSequenceInfo,
            value: i32,
        );
        pub fn whiteout_flakes_FlakesSequenceInfo_get_moveSpeed(
            self_: *mut whiteout_FlakesSequenceInfo,
        ) -> f32;
        pub fn whiteout_flakes_FlakesSequenceInfo_set_moveSpeed(
            self_: *mut whiteout_FlakesSequenceInfo,
            value: f32,
        );
        pub fn whiteout_flakes_FlakesSequenceInfo_get_rarity(
            self_: *mut whiteout_FlakesSequenceInfo,
        ) -> f32;
        pub fn whiteout_flakes_FlakesSequenceInfo_set_rarity(
            self_: *mut whiteout_FlakesSequenceInfo,
            value: f32,
        );
        pub fn whiteout_flakes_FlakesSequenceInfo_get_nonLooping(
            self_: *mut whiteout_FlakesSequenceInfo,
        ) -> i32;
        pub fn whiteout_flakes_FlakesSequenceInfo_set_nonLooping(
            self_: *mut whiteout_FlakesSequenceInfo,
            value: i32,
        );
        // PipelineView
        pub fn whiteout_flakes_FlakesPipelineView_delete(self_: *mut whiteout_FlakesPipelineView);
        pub fn whiteout_flakes_FlakesPipelineView_InitDevice(
            self_: *mut whiteout_FlakesPipelineView,
            arg: i32,
        );
        pub fn whiteout_flakes_FlakesPipelineView_IsDeviceReady(
            self_: *mut whiteout_FlakesPipelineView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesPipelineView_CreateOffscreenTarget(
            self_: *mut whiteout_FlakesPipelineView,
            width: i32,
            height: i32,
        ) -> u32;
        pub fn whiteout_flakes_FlakesPipelineView_DestroyTarget(
            self_: *mut whiteout_FlakesPipelineView,
            arg: u32,
        );
        pub fn whiteout_flakes_FlakesPipelineView_SetPrimaryTarget(
            self_: *mut whiteout_FlakesPipelineView,
            arg: u32,
        );
        pub fn whiteout_flakes_FlakesPipelineView_ResizePrimaryTarget(
            self_: *mut whiteout_FlakesPipelineView,
            width: i32,
            height: i32,
        );
        pub fn whiteout_flakes_FlakesPipelineView_RenderFrame(
            self_: *mut whiteout_FlakesPipelineView,
            t: u32,
        );
        pub fn whiteout_flakes_FlakesPipelineView_RenderViewport(
            self_: *mut whiteout_FlakesPipelineView,
            t: u32,
            camera: u32,
        );
        pub fn whiteout_flakes_FlakesPipelineView_Present(
            self_: *mut whiteout_FlakesPipelineView,
            t: u32,
        );
        pub fn whiteout_flakes_FlakesPipelineView_Shutdown(self_: *mut whiteout_FlakesPipelineView);
        pub fn whiteout_flakes_FlakesPipelineView_FrameStats(
            self_: *mut whiteout_FlakesPipelineView,
        ) -> *mut whiteout_FlakesFrameStats;
        pub fn whiteout_flakes_FlakesPipelineView_LiveGpuBytes(
            self_: *mut whiteout_FlakesPipelineView,
        ) -> u64;
        // SceneView
        pub fn whiteout_flakes_FlakesSceneView_delete(self_: *mut whiteout_FlakesSceneView);
        pub fn whiteout_flakes_FlakesSceneView_AnimationTimeMs(
            self_: *mut whiteout_FlakesSceneView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesSceneView_SetAnimationTimeMs(
            self_: *mut whiteout_FlakesSceneView,
            arg: i32,
        );
        pub fn whiteout_flakes_FlakesSceneView_Update(
            self_: *mut whiteout_FlakesSceneView,
            dt: f32,
        );
        pub fn whiteout_flakes_FlakesSceneView_SetCascInstallPath(
            self_: *mut whiteout_FlakesSceneView,
            root: *const core::ffi::c_char,
        );
        pub fn whiteout_flakes_FlakesSceneView_SetHdMode(
            self_: *mut whiteout_FlakesSceneView,
            enabled: i32,
        );
        // CameraView
        pub fn whiteout_flakes_FlakesCameraView_delete(self_: *mut whiteout_FlakesCameraView);
        pub fn whiteout_flakes_FlakesCameraView_Reset(self_: *mut whiteout_FlakesCameraView);
        pub fn whiteout_flakes_FlakesCameraView_SetPitch(
            self_: *mut whiteout_FlakesCameraView,
            arg: f32,
        );
        pub fn whiteout_flakes_FlakesCameraView_SetYaw(
            self_: *mut whiteout_FlakesCameraView,
            arg: f32,
        );
        pub fn whiteout_flakes_FlakesCameraView_SetDistance(
            self_: *mut whiteout_FlakesCameraView,
            arg: f32,
        );
        pub fn whiteout_flakes_FlakesCameraView_SetTarget(
            self_: *mut whiteout_FlakesCameraView,
            x: f32,
            y: f32,
            z: f32,
        );
        pub fn whiteout_flakes_FlakesCameraView_Rotate(
            self_: *mut whiteout_FlakesCameraView,
            dx: i32,
            dy: i32,
        );
        pub fn whiteout_flakes_FlakesCameraView_Pan(
            self_: *mut whiteout_FlakesCameraView,
            dx: i32,
            dy: i32,
        );
        pub fn whiteout_flakes_FlakesCameraView_Zoom(
            self_: *mut whiteout_FlakesCameraView,
            wheel_delta: i32,
        );
        pub fn whiteout_flakes_FlakesCameraView_ZoomSmooth(
            self_: *mut whiteout_FlakesCameraView,
            factor: f32,
        );
        pub fn whiteout_flakes_FlakesCameraView_SetOrbitalMode(
            self_: *mut whiteout_FlakesCameraView,
        );
        pub fn whiteout_flakes_FlakesCameraView_SetFovDiagonal(
            self_: *mut whiteout_FlakesCameraView,
            arg: f32,
        );
        pub fn whiteout_flakes_FlakesCameraView_SetClip(
            self_: *mut whiteout_FlakesCameraView,
            nz: f32,
            fz: f32,
        );
        pub fn whiteout_flakes_FlakesCameraView_Mode(self_: *mut whiteout_FlakesCameraView) -> i32;
        pub fn whiteout_flakes_FlakesCameraView_Distance(
            self_: *mut whiteout_FlakesCameraView,
        ) -> f32;
        // SettingsView
        pub fn whiteout_flakes_FlakesSettingsView_delete(self_: *mut whiteout_FlakesSettingsView);
        pub fn whiteout_flakes_FlakesSettingsView_DisplayFlags(
            self_: *mut whiteout_FlakesSettingsView,
        ) -> *mut whiteout_FlakesDisplayFlags;
        pub fn whiteout_flakes_FlakesSettingsView_SetDisplayFlags(
            self_: *mut whiteout_FlakesSettingsView,
            arg: *mut whiteout_FlakesDisplayFlags,
        );
        pub fn whiteout_flakes_FlakesSettingsView_ConsumeRenderModeDirty(
            self_: *mut whiteout_FlakesSettingsView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesSettingsView_LightingMode(
            self_: *mut whiteout_FlakesSettingsView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesSettingsView_SetLightingMode(
            self_: *mut whiteout_FlakesSettingsView,
            arg: i32,
        );
        pub fn whiteout_flakes_FlakesSettingsView_BackgroundColorRaw(
            self_: *mut whiteout_FlakesSettingsView,
        ) -> u32;
        pub fn whiteout_flakes_FlakesSettingsView_SetBackgroundColor(
            self_: *mut whiteout_FlakesSettingsView,
            r: u8,
            g: u8,
            b: u8,
        );
        pub fn whiteout_flakes_FlakesSettingsView_TonemapExposure(
            self_: *mut whiteout_FlakesSettingsView,
        ) -> f32;
        pub fn whiteout_flakes_FlakesSettingsView_SetTonemapExposure(
            self_: *mut whiteout_FlakesSettingsView,
            arg: f32,
        );
        pub fn whiteout_flakes_FlakesSettingsView_BloomEnabled(
            self_: *mut whiteout_FlakesSettingsView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesSettingsView_SetBloomEnabled(
            self_: *mut whiteout_FlakesSettingsView,
            arg: i32,
        );
        pub fn whiteout_flakes_FlakesSettingsView_IblMode(
            self_: *mut whiteout_FlakesSettingsView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesSettingsView_SetIblMode(
            self_: *mut whiteout_FlakesSettingsView,
            arg: i32,
        );
        pub fn whiteout_flakes_FlakesSettingsView_HdDebugMode(
            self_: *mut whiteout_FlakesSettingsView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesSettingsView_SetHdDebugMode(
            self_: *mut whiteout_FlakesSettingsView,
            arg: i32,
        );
        pub fn whiteout_flakes_FlakesSettingsView_AoQuality(
            self_: *mut whiteout_FlakesSettingsView,
        ) -> u32;
        pub fn whiteout_flakes_FlakesSettingsView_SetAoQuality(
            self_: *mut whiteout_FlakesSettingsView,
            arg: u32,
        );
        pub fn whiteout_flakes_FlakesSettingsView_LodOverride(
            self_: *mut whiteout_FlakesSettingsView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesSettingsView_SetLodOverride(
            self_: *mut whiteout_FlakesSettingsView,
            arg: i32,
        );
        pub fn whiteout_flakes_FlakesSettingsView_SetRenderMode(
            self_: *mut whiteout_FlakesSettingsView,
            arg: i32,
        );
        // LoaderView
        pub fn whiteout_flakes_FlakesLoaderView_delete(self_: *mut whiteout_FlakesLoaderView);
        pub fn whiteout_flakes_FlakesLoaderView_SpawnUnit(
            self_: *mut whiteout_FlakesLoaderView,
            path: *const core::ffi::c_char,
        ) -> u32;
        pub fn whiteout_flakes_FlakesLoaderView_SpawnEffect(
            self_: *mut whiteout_FlakesLoaderView,
            path: *const core::ffi::c_char,
        ) -> u32;
        pub fn whiteout_flakes_FlakesLoaderView_RequestClearAll(
            self_: *mut whiteout_FlakesLoaderView,
        );
        pub fn whiteout_flakes_FlakesLoaderView_Destroy(
            self_: *mut whiteout_FlakesLoaderView,
            handle: u32,
        );
        // AssetsView
        pub fn whiteout_flakes_FlakesAssetsView_delete(self_: *mut whiteout_FlakesAssetsView);
        pub fn whiteout_flakes_FlakesAssetsView_Stats(
            self_: *mut whiteout_FlakesAssetsView,
        ) -> *mut whiteout_FlakesAssetsViewStats;
        pub fn whiteout_flakes_FlakesAssetsView_PrefetchEventAssets(
            self_: *mut whiteout_FlakesAssetsView,
        );
        pub fn whiteout_flakes_FlakesAssetsView_PrefetchEventAssetsForActor(
            self_: *mut whiteout_FlakesAssetsView,
            actor: u32,
        );
        // AssetsViewStats
        pub fn whiteout_flakes_FlakesAssetsViewStats_new() -> *mut whiteout_FlakesAssetsViewStats;
        pub fn whiteout_flakes_FlakesAssetsViewStats_delete(
            self_: *mut whiteout_FlakesAssetsViewStats,
        );
        pub fn whiteout_flakes_FlakesAssetsViewStats_get_liveSlots(
            self_: *mut whiteout_FlakesAssetsViewStats,
        ) -> u64;
        pub fn whiteout_flakes_FlakesAssetsViewStats_set_liveSlots(
            self_: *mut whiteout_FlakesAssetsViewStats,
            value: u64,
        );
        pub fn whiteout_flakes_FlakesAssetsViewStats_get_loadedSlots(
            self_: *mut whiteout_FlakesAssetsViewStats,
        ) -> u64;
        pub fn whiteout_flakes_FlakesAssetsViewStats_set_loadedSlots(
            self_: *mut whiteout_FlakesAssetsViewStats,
            value: u64,
        );
        pub fn whiteout_flakes_FlakesAssetsViewStats_get_pendingNeeds(
            self_: *mut whiteout_FlakesAssetsViewStats,
        ) -> u64;
        pub fn whiteout_flakes_FlakesAssetsViewStats_set_pendingNeeds(
            self_: *mut whiteout_FlakesAssetsViewStats,
            value: u64,
        );
        pub fn whiteout_flakes_FlakesAssetsViewStats_get_totalAcquires(
            self_: *mut whiteout_FlakesAssetsViewStats,
        ) -> u64;
        pub fn whiteout_flakes_FlakesAssetsViewStats_set_totalAcquires(
            self_: *mut whiteout_FlakesAssetsViewStats,
            value: u64,
        );
        pub fn whiteout_flakes_FlakesAssetsViewStats_get_totalReleases(
            self_: *mut whiteout_FlakesAssetsViewStats,
        ) -> u64;
        pub fn whiteout_flakes_FlakesAssetsViewStats_set_totalReleases(
            self_: *mut whiteout_FlakesAssetsViewStats,
            value: u64,
        );
        pub fn whiteout_flakes_FlakesAssetsViewStats_get_totalApplies(
            self_: *mut whiteout_FlakesAssetsViewStats,
        ) -> u64;
        pub fn whiteout_flakes_FlakesAssetsViewStats_set_totalApplies(
            self_: *mut whiteout_FlakesAssetsViewStats,
            value: u64,
        );
        pub fn whiteout_flakes_FlakesAssetsViewStats_get_totalApplyMisses(
            self_: *mut whiteout_FlakesAssetsViewStats,
        ) -> u64;
        pub fn whiteout_flakes_FlakesAssetsViewStats_set_totalApplyMisses(
            self_: *mut whiteout_FlakesAssetsViewStats,
            value: u64,
        );
        // DncView
        pub fn whiteout_flakes_FlakesDncView_delete(self_: *mut whiteout_FlakesDncView);
        pub fn whiteout_flakes_FlakesDncView_IsValid(self_: *mut whiteout_FlakesDncView) -> i32;
        pub fn whiteout_flakes_FlakesDncView_TimeOfDay(self_: *mut whiteout_FlakesDncView) -> f32;
        pub fn whiteout_flakes_FlakesDncView_SetTimeOfDay(
            self_: *mut whiteout_FlakesDncView,
            arg: f32,
        );
        pub fn whiteout_flakes_FlakesDncView_TodScale(self_: *mut whiteout_FlakesDncView) -> f32;
        pub fn whiteout_flakes_FlakesDncView_SetTodScale(
            self_: *mut whiteout_FlakesDncView,
            arg: f32,
        );
        pub fn whiteout_flakes_FlakesDncView_HoursPerDay(self_: *mut whiteout_FlakesDncView)
            -> f32;
        pub fn whiteout_flakes_FlakesDncView_SetUnitMdl(
            self_: *mut whiteout_FlakesDncView,
            arg: *const core::ffi::c_char,
        );
        pub fn whiteout_flakes_FlakesDncView_Advance(self_: *mut whiteout_FlakesDncView, dt: f32);
        // ShadowView
        pub fn whiteout_flakes_FlakesShadowView_delete(self_: *mut whiteout_FlakesShadowView);
        pub fn whiteout_flakes_FlakesShadowView_IsValid(
            self_: *mut whiteout_FlakesShadowView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesShadowView_IsEnabled(
            self_: *mut whiteout_FlakesShadowView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesShadowView_SetEnabled(
            self_: *mut whiteout_FlakesShadowView,
            on: i32,
        );
        pub fn whiteout_flakes_FlakesShadowView_SetParams(
            self_: *mut whiteout_FlakesShadowView,
            arg: *mut whiteout_FlakesShadowParams,
        );
        // SplatView
        pub fn whiteout_flakes_FlakesSplatView_delete(self_: *mut whiteout_FlakesSplatView);
        pub fn whiteout_flakes_FlakesSplatView_Clear(self_: *mut whiteout_FlakesSplatView);
        // ReplaceablesView
        pub fn whiteout_flakes_FlakesReplaceablesView_delete(
            self_: *mut whiteout_FlakesReplaceablesView,
        );
        pub fn whiteout_flakes_FlakesReplaceablesView_ConsumeDirty(
            self_: *mut whiteout_FlakesReplaceablesView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesReplaceablesView_SetTileset(
            self_: *mut whiteout_FlakesReplaceablesView,
            arg: i32,
        );
        // PlaybackView
        pub fn whiteout_flakes_FlakesPlaybackView_delete(self_: *mut whiteout_FlakesPlaybackView);
        pub fn whiteout_flakes_FlakesPlaybackView_State(
            self_: *mut whiteout_FlakesPlaybackView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesPlaybackView_SetState(
            self_: *mut whiteout_FlakesPlaybackView,
            arg: i32,
        );
        pub fn whiteout_flakes_FlakesPlaybackView_Play(self_: *mut whiteout_FlakesPlaybackView);
        pub fn whiteout_flakes_FlakesPlaybackView_Pause(self_: *mut whiteout_FlakesPlaybackView);
        pub fn whiteout_flakes_FlakesPlaybackView_Stop(self_: *mut whiteout_FlakesPlaybackView);
        pub fn whiteout_flakes_FlakesPlaybackView_Restart(self_: *mut whiteout_FlakesPlaybackView);
        pub fn whiteout_flakes_FlakesPlaybackView_ResyncEffects(
            self_: *mut whiteout_FlakesPlaybackView,
        );
        pub fn whiteout_flakes_FlakesPlaybackView_IsPaused(
            self_: *mut whiteout_FlakesPlaybackView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesPlaybackView_TimeScale(
            self_: *mut whiteout_FlakesPlaybackView,
        ) -> f32;
        pub fn whiteout_flakes_FlakesPlaybackView_SetTimeScale(
            self_: *mut whiteout_FlakesPlaybackView,
            arg: f32,
        );
        // ActorView
        pub fn whiteout_flakes_FlakesActorView_delete(self_: *mut whiteout_FlakesActorView);
        pub fn whiteout_flakes_FlakesActorView_IsValid(self_: *mut whiteout_FlakesActorView)
            -> i32;
        pub fn whiteout_flakes_FlakesActorView_Handle(self_: *mut whiteout_FlakesActorView) -> u32;
        pub fn whiteout_flakes_FlakesActorView_Role(self_: *mut whiteout_FlakesActorView) -> i32;
        pub fn whiteout_flakes_FlakesActorView_PlaybackSpeed(
            self_: *mut whiteout_FlakesActorView,
        ) -> f32;
        pub fn whiteout_flakes_FlakesActorView_SetPlaybackSpeed(
            self_: *mut whiteout_FlakesActorView,
            arg: f32,
        );
        pub fn whiteout_flakes_FlakesActorView_IgnoreNonLooping(
            self_: *mut whiteout_FlakesActorView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesActorView_SetIgnoreNonLooping(
            self_: *mut whiteout_FlakesActorView,
            arg: i32,
        );
        pub fn whiteout_flakes_FlakesActorView_TeamColor(
            self_: *mut whiteout_FlakesActorView,
        ) -> u32;
        pub fn whiteout_flakes_FlakesActorView_SetTeamColor(
            self_: *mut whiteout_FlakesActorView,
            r: u8,
            g: u8,
            b: u8,
        );
        pub fn whiteout_flakes_FlakesActorView_SetRoleExternal(
            self_: *mut whiteout_FlakesActorView,
        );
        pub fn whiteout_flakes_FlakesActorView_Sequences(
            self_: *mut whiteout_FlakesActorView,
        ) -> *mut whiteout_SequenceInfoList;
        pub fn whiteout_flakes_FlakesActorView_ActiveSequenceIndex(
            self_: *mut whiteout_FlakesActorView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesActorView_SetActiveSequence(
            self_: *mut whiteout_FlakesActorView,
            arg: i32,
        );
        pub fn whiteout_flakes_FlakesActorView_AnimationTimeMs(
            self_: *mut whiteout_FlakesActorView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesActorView_SetAnimationTimeMs(
            self_: *mut whiteout_FlakesActorView,
            arg: i32,
        );
        pub fn whiteout_flakes_FlakesActorView_HasAnimationSource(
            self_: *mut whiteout_FlakesActorView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesActorView_EvaluateAndApply(
            self_: *mut whiteout_FlakesActorView,
        );
        pub fn whiteout_flakes_FlakesActorView_EvaluateAt(
            self_: *mut whiteout_FlakesActorView,
            time_ms: i32,
        );
        pub fn whiteout_flakes_FlakesActorView_GeosetCount(
            self_: *mut whiteout_FlakesActorView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesActorView_MaterialCount(
            self_: *mut whiteout_FlakesActorView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesActorView_CollisionShapeCount(
            self_: *mut whiteout_FlakesActorView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesActorView_CameraPresets(
            self_: *mut whiteout_FlakesActorView,
        ) -> *mut whiteout_CameraPresetList;
        pub fn whiteout_flakes_FlakesActorView_PreferredRenderMode(
            self_: *mut whiteout_FlakesActorView,
        ) -> i32;
        pub fn whiteout_flakes_FlakesActorView_ChildModelPaths(
            self_: *mut whiteout_FlakesActorView,
        ) -> *mut whiteout_StringList;
        // Renderer
        pub fn whiteout_flakes_FlakesRenderer_new() -> *mut whiteout_FlakesRenderer;
        pub fn whiteout_flakes_FlakesRenderer_delete(self_: *mut whiteout_FlakesRenderer);
        pub fn whiteout_flakes_FlakesRenderer_Pipeline(
            self_: *mut whiteout_FlakesRenderer,
        ) -> *mut whiteout_FlakesPipelineView;
        pub fn whiteout_flakes_FlakesRenderer_Scene(
            self_: *mut whiteout_FlakesRenderer,
        ) -> *mut whiteout_FlakesSceneView;
        pub fn whiteout_flakes_FlakesRenderer_Camera(
            self_: *mut whiteout_FlakesRenderer,
        ) -> *mut whiteout_FlakesCameraView;
        pub fn whiteout_flakes_FlakesRenderer_CreateCamera(
            self_: *mut whiteout_FlakesRenderer,
        ) -> u32;
        pub fn whiteout_flakes_FlakesRenderer_CameraAt(
            self_: *mut whiteout_FlakesRenderer,
            arg: u32,
        ) -> *mut whiteout_FlakesCameraView;
        pub fn whiteout_flakes_FlakesRenderer_Settings(
            self_: *mut whiteout_FlakesRenderer,
        ) -> *mut whiteout_FlakesSettingsView;
        pub fn whiteout_flakes_FlakesRenderer_Loader(
            self_: *mut whiteout_FlakesRenderer,
        ) -> *mut whiteout_FlakesLoaderView;
        pub fn whiteout_flakes_FlakesRenderer_Dnc(
            self_: *mut whiteout_FlakesRenderer,
        ) -> *mut whiteout_FlakesDncView;
        pub fn whiteout_flakes_FlakesRenderer_Shadow(
            self_: *mut whiteout_FlakesRenderer,
        ) -> *mut whiteout_FlakesShadowView;
        pub fn whiteout_flakes_FlakesRenderer_Splats(
            self_: *mut whiteout_FlakesRenderer,
        ) -> *mut whiteout_FlakesSplatView;
        pub fn whiteout_flakes_FlakesRenderer_Replaceables(
            self_: *mut whiteout_FlakesRenderer,
        ) -> *mut whiteout_FlakesReplaceablesView;
        pub fn whiteout_flakes_FlakesRenderer_Assets(
            self_: *mut whiteout_FlakesRenderer,
        ) -> *mut whiteout_FlakesAssetsView;
        pub fn whiteout_flakes_FlakesRenderer_Playback(
            self_: *mut whiteout_FlakesRenderer,
        ) -> *mut whiteout_FlakesPlaybackView;
        pub fn whiteout_flakes_FlakesRenderer_Actor(
            self_: *mut whiteout_FlakesRenderer,
            h: u32,
        ) -> *mut whiteout_FlakesActorView;
        pub fn whiteout_flakes_FlakesRenderer_Tick(self_: *mut whiteout_FlakesRenderer, dt: f32);
        // StorageBrowser
        pub fn whiteout_flakes_FlakesStorageBrowser_new() -> *mut whiteout_FlakesStorageBrowser;
        pub fn whiteout_flakes_FlakesStorageBrowser_delete(
            self_: *mut whiteout_FlakesStorageBrowser,
        );
        pub fn whiteout_flakes_FlakesStorageBrowser_Open(
            self_: *mut whiteout_FlakesStorageBrowser,
            root: *const core::ffi::c_char,
            kind: i32,
        ) -> i32;
        pub fn whiteout_flakes_FlakesStorageBrowser_OpenAuto(
            self_: *mut whiteout_FlakesStorageBrowser,
            path: *const core::ffi::c_char,
        ) -> i32;
        pub fn whiteout_flakes_FlakesStorageBrowser_Kind(
            self_: *mut whiteout_FlakesStorageBrowser,
        ) -> i32;
        pub fn whiteout_flakes_FlakesStorageBrowser_IsOpen(
            self_: *mut whiteout_FlakesStorageBrowser,
        ) -> i32;
        pub fn whiteout_flakes_FlakesStorageBrowser_Root(
            self_: *mut whiteout_FlakesStorageBrowser,
        ) -> RawCString;
        pub fn whiteout_flakes_FlakesStorageBrowser_LastError(
            self_: *mut whiteout_FlakesStorageBrowser,
        ) -> RawCString;
        pub fn whiteout_flakes_FlakesStorageBrowser_CurrentPath(
            self_: *mut whiteout_FlakesStorageBrowser,
        ) -> RawCString;
        pub fn whiteout_flakes_FlakesStorageBrowser_Breadcrumb(
            self_: *mut whiteout_FlakesStorageBrowser,
        ) -> *mut whiteout_StringList;
        pub fn whiteout_flakes_FlakesStorageBrowser_Folders(
            self_: *mut whiteout_FlakesStorageBrowser,
        ) -> *mut whiteout_StringList;
        pub fn whiteout_flakes_FlakesStorageBrowser_Files(
            self_: *mut whiteout_FlakesStorageBrowser,
        ) -> *mut whiteout_StringList;
        pub fn whiteout_flakes_FlakesStorageBrowser_SetFilter(
            self_: *mut whiteout_FlakesStorageBrowser,
            filter: i32,
        );
        pub fn whiteout_flakes_FlakesStorageBrowser_Filter(
            self_: *mut whiteout_FlakesStorageBrowser,
        ) -> i32;
        pub fn whiteout_flakes_FlakesStorageBrowser_UnfilteredFileCount(
            self_: *mut whiteout_FlakesStorageBrowser,
        ) -> i32;
        pub fn whiteout_flakes_FlakesStorageBrowser_Descend(
            self_: *mut whiteout_FlakesStorageBrowser,
            folder_name: *const core::ffi::c_char,
        );
        pub fn whiteout_flakes_FlakesStorageBrowser_Ascend(
            self_: *mut whiteout_FlakesStorageBrowser,
        );
        pub fn whiteout_flakes_FlakesStorageBrowser_NavigateTo(
            self_: *mut whiteout_FlakesStorageBrowser,
            display_path: *const core::ffi::c_char,
        );
        pub fn whiteout_flakes_FlakesStorageBrowser_ChildPath(
            self_: *mut whiteout_FlakesStorageBrowser,
            file_name: *const core::ffi::c_char,
        ) -> RawCString;
        pub fn whiteout_flakes_FlakesStorageBrowser_IsEffect(
            self_: *mut whiteout_FlakesStorageBrowser,
            file_name: *const core::ffi::c_char,
        ) -> i32;
    }
}
