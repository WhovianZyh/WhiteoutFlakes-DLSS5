# SPDX-License-Identifier: BSD-3-Clause
# Copyright (c) 2026 Fernando Sahmkow
#
# C ABI shared library for whiteout_lib. Every translation unit is
# autogenerated from the C++ headers via `tools/codegen/emit_c.py`.
#
# Compiled `-fno-exceptions` (or `/EHs-c-` on MSVC): the library
# routes its error states through optional/null returns rather than
# throwing, so a C++ exception crossing the C boundary is a programmer
# error worth aborting on.

if(NOT WHITEOUT_BUILD_C_BINDINGS)
    return()
endif()

set(_whiteout_c_sources
    whiteout_c_common.cpp
    whiteout_v.cpp
    whiteout_textures.cpp
    whiteout_mdx.cpp
    whiteout_m2.cpp
    whiteout_m3.cpp
    whiteout_utils.cpp
    whiteout_host.cpp
    whiteout_host_shims.cpp
)
if(WHITEOUT_ENABLE_MPQ)
    list(APPEND _whiteout_c_sources whiteout_mpq.cpp)
endif()
if(WHITEOUT_ENABLE_CASC)
    list(APPEND _whiteout_c_sources whiteout_casc.cpp whiteout_casc_shims.cpp)
endif()

# JNI bridge sources are absorbed into whiteout_c when the JNI bindings
# are enabled — Java consumers load a single DLL via System.loadLibrary
# and both the flat-C surface (Panama) and the Java_* upcall symbols
# (JNI) live side-by-side. Non-Java consumers see the extra symbols but
# never trigger them (no JVM → JNI exports are never invoked).
set(_whiteout_jni_sources)
if(WHITEOUT_BUILD_JNI_BINDINGS)
    find_package(JNI REQUIRED)
    set(_jni_dir ${CMAKE_SOURCE_DIR}/bindings/java/jni)
    set(_whiteout_jni_sources
        ${_jni_dir}/jni_common.cpp
        ${_jni_dir}/smoke_invokers.cpp
        ${_jni_dir}/timelinesemaphore_jni.cpp
        ${_jni_dir}/httphandler_bridge.cpp
        ${_jni_dir}/virtualpathfilesystem_bridge.cpp
        ${_jni_dir}/workerpool_bridge.cpp
        ${_jni_dir}/cascfilesystem_bridge.cpp
        ${_jni_dir}/httpresponseconsumer_callback.cpp
        ${_jni_dir}/callbackrunnable_callback.cpp
        ${_jni_dir}/native_pool_submit.cpp
        ${_jni_dir}/native_http_async.cpp
    )
endif()

add_library(whiteout_c SHARED ${_whiteout_c_sources} ${_whiteout_jni_sources})

target_link_libraries(whiteout_c PRIVATE whiteout_lib)
if(WHITEOUT_ENABLE_MPQ)
    # whiteout_mpq exports WHITEOUT_HAS_MPQ=1 PUBLIC so the storage
    # headers' `#error` gate is satisfied for this TU as well.
    target_link_libraries(whiteout_c PRIVATE whiteout_mpq)
endif()
if(WHITEOUT_ENABLE_CASC)
    target_link_libraries(whiteout_c PRIVATE whiteout_casc)
endif()
target_include_directories(whiteout_c PRIVATE
    ${CMAKE_SOURCE_DIR}/include
    ${CMAKE_CURRENT_SOURCE_DIR}
)
if(WHITEOUT_BUILD_JNI_BINDINGS)
    target_include_directories(whiteout_c PRIVATE
        ${JNI_INCLUDE_DIRS}
        ${CMAKE_SOURCE_DIR}/bindings/java/jni
    )
endif()
target_compile_features(whiteout_c PRIVATE cxx_std_20)

# Disable exceptions for the C-ABI TUs only — see header comment.
# When the JNI bridge is bundled in (WHITEOUT_BUILD_JNI_BINDINGS=ON),
# the bridge sources need exceptions, so we keep them enabled at the
# target level and selectively disable exceptions per-source on just
# the C-ABI TUs. Otherwise (Java-free build), disable for the whole
# target. Mixing `_HAS_EXCEPTIONS=0` with the STL on TUs that use
# exceptions causes ODR-style breakage, so we use the compile FLAGS
# (`/EHs-c-`, `-fno-exceptions`) without touching `_HAS_EXCEPTIONS`.
if(WHITEOUT_BUILD_JNI_BINDINGS)
    if(MSVC)
        set_source_files_properties(${_whiteout_c_sources} PROPERTIES
            COMPILE_OPTIONS "/EHs-c-")
    else()
        set_source_files_properties(${_whiteout_c_sources} PROPERTIES
            COMPILE_OPTIONS "-fno-exceptions")
    endif()
else()
    if(MSVC)
        target_compile_options(whiteout_c PRIVATE /EHs-c- /D_HAS_EXCEPTIONS=0)
    else()
        target_compile_options(whiteout_c PRIVATE -fno-exceptions)
    endif()
endif()

# On Windows, DLL symbols aren't exported by default. Auto-export
# everything with C linkage so the FFM/cffi side can resolve them
# without us peppering the codegen output with __declspec(dllexport).
set_target_properties(whiteout_c PROPERTIES
    LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/c-dist"
    RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/c-dist"
    ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/c-dist"
    WINDOWS_EXPORT_ALL_SYMBOLS ON
)

# The output filename is `whiteout_native` regardless of which bindings
# are bundled in. The CMake target stays `whiteout_c` for internal
# linking (`target_link_libraries(... whiteout_c)` from other CMake
# targets keeps working) — only the produced DLL/.so/.dylib gets the
# `whiteout_native` name. All language bindings (Java FFM, C# P/Invoke,
# Python `ctypes`, Rust, etc.) reference the same artefact.
set_target_properties(whiteout_c PROPERTIES OUTPUT_NAME whiteout_native)

# Static sibling for consumers that would rather not ship a loose DLL —
# Rust in particular, where `cargo build` producing a self-contained
# binary is the expectation rather than the exception. Same sources, same
# flags; only the linkage differs. The JNI bridge is deliberately excluded
# (a static JVM upcall library makes no sense).
if(WHITEOUT_BUILD_C_STATIC)
    add_library(whiteout_c_static STATIC ${_whiteout_c_sources})
    target_link_libraries(whiteout_c_static PUBLIC whiteout_lib)
    if(WHITEOUT_ENABLE_MPQ)
        target_link_libraries(whiteout_c_static PUBLIC whiteout_mpq)
    endif()
    if(WHITEOUT_ENABLE_CASC)
        target_link_libraries(whiteout_c_static PUBLIC whiteout_casc)
    endif()
    target_include_directories(whiteout_c_static PUBLIC
        ${CMAKE_SOURCE_DIR}/include
        ${CMAKE_CURRENT_SOURCE_DIR}
    )
    target_compile_features(whiteout_c_static PRIVATE cxx_std_20)
    if(MSVC)
        target_compile_options(whiteout_c_static PRIVATE /EHs-c- /D_HAS_EXCEPTIONS=0)
    else()
        target_compile_options(whiteout_c_static PRIVATE -fno-exceptions)
    endif()
    set_target_properties(whiteout_c_static PROPERTIES
        ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/c-dist"
        OUTPUT_NAME whiteout_native_static
        POSITION_INDEPENDENT_CODE ON
    )
endif()
