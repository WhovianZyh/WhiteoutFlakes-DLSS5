// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * JOIN — connects two bodies with the joint named by @ref jointType.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * PhysicsJoint allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (PhysicsJoint obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class PhysicsJoint implements AutoCloseable {
    private static final long BYTES = 16L;

    final MemorySegment handle;
    final boolean owned;

    PhysicsJoint(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public PhysicsJoint() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsJoint_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("PhysicsJoint allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsJoint_delete, handle);
        }
    }

    /** @return the bodyAIndex field of this M2PhysicsJoint. */
    public int getBodyAIndex() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setBodyAIndex(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /** @return the bodyBIndex field of this M2PhysicsJoint. */
    public int getBodyBIndex() {
        return handle.get(ValueLayout.JAVA_INT, 4L);
    }
    public void setBodyBIndex(int value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value);
    }
    /**
     * Zero in every corpus joint.
     * @return the padding08 field of this M2PhysicsJoint.
     */
    public int getPadding08() {
        return handle.get(ValueLayout.JAVA_INT, 8L);
    }
    public void setPadding08(int value) {
        handle.set(ValueLayout.JAVA_INT, 8L, value);
    }
    /** @return the jointType field of this M2PhysicsJoint. */
    public PhysicsJointType getJointType() {
        return PhysicsJointType.fromInt(handle.get(ValueLayout.JAVA_INT, 12L));
    }
    public void setJointType(PhysicsJointType value) {
        handle.set(ValueLayout.JAVA_INT, 12L, value.value);
    }
    /**
     * Entry index within the joint array @ref jointType selects.
     * @return the jointId field of this M2PhysicsJoint.
     */
    public short getJointId() {
        return handle.get(ValueLayout.JAVA_SHORT, 14L);
    }
    public void setJointId(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 14L, value);
    }
    @Override public String toString() {
        return "PhysicsJoint(" + "bodyAIndex=" + getBodyAIndex() + ", " + "bodyBIndex=" + getBodyBIndex() + ", " + "padding08=" + getPadding08() + ", " + "jointType=" + getJointType() + ", " + "jointId=" + getJointId() + ")";
    }

}
