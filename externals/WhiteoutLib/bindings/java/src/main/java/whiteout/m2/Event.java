// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * Java binding for the native Event type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Event allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (Event obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class Event implements AutoCloseable {
    private static final long BYTES = 80L;

    final MemorySegment handle;
    final boolean owned;

    Event(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public Event() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2Event_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("Event allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2Event_delete, handle);
        }
    }

    /** @return the identifier field of this M2Event. */
    public int getIdentifier() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setIdentifier(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /** @return the data field of this M2Event. */
    public int getData() {
        return handle.get(ValueLayout.JAVA_INT, 4L);
    }
    public void setData(int value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value);
    }
    /** @return the boneId field of this M2Event. */
    public int getBoneId() {
        return handle.get(ValueLayout.JAVA_INT, 8L);
    }
    public void setBoneId(int value) {
        handle.set(ValueLayout.JAVA_INT, 8L, value);
    }
    /** @return the position field of this M2Event. */
    public Vector3f getPosition() {
        return Handles.wrapVector3f(handle.asSlice(12L, 12L), false);
    }
    public void setPosition(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2Event_set_position, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 12L, 12L);
    }
    /** @return the enabled field of this M2Event. */
    public AnimationTrackBase getEnabled() {
        return new AnimationTrackBase(handle.asSlice(24L, 56L), false);
    }
    public void setEnabled(AnimationTrackBase value) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2Event_set_enabled, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    @Override public String toString() {
        return "Event(" + "identifier=" + getIdentifier() + ", " + "data=" + getData() + ", " + "boneId=" + getBoneId() + ")";
    }

}
