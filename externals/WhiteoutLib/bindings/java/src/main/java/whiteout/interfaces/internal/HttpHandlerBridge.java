// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026 Fernando Sahmkow
//
// AUTOGENERATED by tools/codegen/emit_jni.py — do not edit by hand.

package whiteout.interfaces.internal;

import java.lang.ref.Cleaner;

import whiteout.interfaces.HttpHandler;

/** Internal: native bridge factory for {@link HttpHandler}.
 *  Not exported by module-info — only library entry points should
 *  invoke this. */
public final class HttpHandlerBridge {

    private static final Cleaner CLEANER = Cleaner.create();

    static { JniLoader.ensureLoaded(); }

    private HttpHandlerBridge() {}

    /** Wrap {@code impl} as a native handle. The handle is freed when
     *  {@code owner} is collected — keep a strong reference to {@code owner}
     *  (typically the storage / wrapper object that consumes the handle)
     *  for at least as long as the C++ side needs the bridge. */
    public static long createPinned(HttpHandler impl, Object owner) {
        if (impl == null) throw new NullPointerException("impl");
        if (owner == null) throw new NullPointerException("owner");
        long ptr = _createBridge(impl);
        CLEANER.register(owner, new Releaser(ptr));
        return ptr;
    }

    // Cleanup is a static-nested class (not a lambda) so the captured
    // pointer doesn't accidentally hold a strong reference back to the
    // owner — that would defeat the Cleaner.
    private static final class Releaser implements Runnable {
        private final long ptr;
        Releaser(long ptr) { this.ptr = ptr; }
        @Override public void run() { _destroyBridge(ptr); }
    }

    private static native long _createBridge(Object impl);
    private static native void _destroyBridge(long handle);
}
