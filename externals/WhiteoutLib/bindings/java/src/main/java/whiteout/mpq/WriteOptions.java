// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mpq;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mpq.internal.Native;

/**
 * Options for writing a file into the archive.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * WriteOptions allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (WriteOptions obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class WriteOptions implements AutoCloseable {
    private static final long BYTES = 6L;

    final MemorySegment handle;
    final boolean owned;

    WriteOptions(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public WriteOptions() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mpq_MpqWriteOptions_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("WriteOptions allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mpq_MpqWriteOptions_delete, handle);
        }
    }

    /**
     * Compression algorithm to apply.
     * @return the compression field of this MpqWriteOptions.
     */
    public Compression getCompression() {
        return Compression.fromInt(handle.get(ValueLayout.JAVA_INT, 0L));
    }
    public void setCompression(Compression value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value.value);
    }
    /**
     * Locale ID for the hash table slot.
     * @return the locale field of this MpqWriteOptions.
     */
    public short getLocale() {
        return handle.get(ValueLayout.JAVA_SHORT, 2L);
    }
    public void setLocale(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 2L, value);
    }
    /**
     * Encrypt file data with a derived key.
     * @return the encrypt field of this MpqWriteOptions.
     */
    public boolean getEncrypt() {
        return handle.get(ValueLayout.JAVA_BYTE, 4L) != 0;
    }
    public void setEncrypt(boolean value) {
        handle.set(ValueLayout.JAVA_BYTE, 4L, (byte) (value ? 1 : 0));
    }
    /**
     * Store the file as a single unpartitioned unit.
     * @return the singleUnit field of this MpqWriteOptions.
     */
    public boolean getSingleUnit() {
        return handle.get(ValueLayout.JAVA_BYTE, 5L) != 0;
    }
    public void setSingleUnit(boolean value) {
        handle.set(ValueLayout.JAVA_BYTE, 5L, (byte) (value ? 1 : 0));
    }
    @Override public String toString() {
        return "WriteOptions(" + "compression=" + getCompression() + ", " + "locale=" + getLocale() + ", " + "encrypt=" + getEncrypt() + ", " + "singleUnit=" + getSingleUnit() + ")";
    }

}
