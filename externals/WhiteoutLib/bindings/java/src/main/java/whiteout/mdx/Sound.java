// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mdx;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mdx.internal.Native;

/**
 * Sound definition (deprecated, not used in shipped models)
 * 
 * Sounds define audio file paths and playback properties. While readers for this chunk exist in the internal game code, no known shipped model contains SNDS data.
 * 
 * Binary layout: 56 bytes per entry
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Sound allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (Sound obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class Sound implements AutoCloseable {
    private static final long BYTES = 48L;

    final MemorySegment handle;
    final boolean owned;

    Sound(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public Sound() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxSound_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("Sound allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxSound_delete, handle);
        }
    }

    /**
     * Path to sound file (44 bytes, null-terminated)
     * @return the soundFile field of this MdxSound.
     */
    public String getSoundFile() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxSound_get_soundFile, arena, handle);
            MemorySegment __chars = __s.get(ValueLayout.ADDRESS, 0);
            long __len = __s.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__chars == null || __chars.equals(MemorySegment.NULL)) return "";
            byte[] __bytes = __chars.reinterpret(__len).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_CString_free, __s);
            return new String(__bytes, StandardCharsets.UTF_8);
        }
    }
    public void setSoundFile(String value) {
        try (Arena arena = Arena.ofConfined()) {
            byte[] __utf = value == null ? new byte[0] : value.getBytes(StandardCharsets.UTF_8);
            MemorySegment __seg = arena.allocate(__utf.length + 1);
            MemorySegment.copy(__utf, 0, __seg, ValueLayout.JAVA_BYTE, 0, __utf.length);
            __seg.set(ValueLayout.JAVA_BYTE, __utf.length, (byte) 0);
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxSound_set_soundFile, handle, __seg);
        }
    }
    /**
     * Maximum audible distance
     * @return the maximumDistance field of this MdxSound.
     */
    public float getMaximumDistance() {
        return handle.get(ValueLayout.JAVA_FLOAT, 32L);
    }
    public void setMaximumDistance(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 32L, value);
    }
    /**
     * Minimum distance (full volume)
     * @return the minimumDistance field of this MdxSound.
     */
    public float getMinimumDistance() {
        return handle.get(ValueLayout.JAVA_FLOAT, 36L);
    }
    public void setMinimumDistance(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 36L, value);
    }
    /**
     * Sound channel identifier
     * @return the soundChannel field of this MdxSound.
     */
    public int getSoundChannel() {
        return handle.get(ValueLayout.JAVA_INT, 40L);
    }
    public void setSoundChannel(int value) {
        handle.set(ValueLayout.JAVA_INT, 40L, value);
    }
    @Override public String toString() {
        return "Sound(" + "soundFile=" + getSoundFile() + ", " + "maximumDistance=" + getMaximumDistance() + ", " + "minimumDistance=" + getMinimumDistance() + ", " + "soundChannel=" + getSoundChannel() + ")";
    }

}
