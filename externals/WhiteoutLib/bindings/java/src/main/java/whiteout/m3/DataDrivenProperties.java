// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * The decoded contents of DataDrivenMaterial::propertyBlob
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * DataDrivenProperties allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (DataDrivenProperties obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class DataDrivenProperties implements AutoCloseable {
    private static final long BYTES = 32L;

    final MemorySegment handle;
    final boolean owned;

    DataDrivenProperties(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public DataDrivenProperties() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3DataDrivenProperties_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("DataDrivenProperties allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3DataDrivenProperties_delete, handle);
        }
    }

    /**
     * Fragment groups, in stored order
     * @return the groups field of this M3DataDrivenProperties.
     */
    public int getGroupsCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m3_M3DataDrivenProperties_get_groups_count, handle);
    }
    public DataDrivenGroup getGroupsAt(int index) {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3DataDrivenProperties_get_groups_at, handle, (long) index);
        return new DataDrivenGroup(__h, false);
    }
    public void resizeGroups(int count) {
        NativeCommon.invokeNative(Native.whiteout_m3_M3DataDrivenProperties_resize_groups, handle, (long) count);
    }
    public java.util.List<DataDrivenGroup> groupsView() {
        return new java.util.AbstractList<DataDrivenGroup>() {
            @Override public int size() { return getGroupsCount(); }
            @Override public DataDrivenGroup get(int index) { return getGroupsAt(index); }
        };
    }
    @Override public String toString() {
        return "DataDrivenProperties@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
