// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.host;

import java.util.Objects;

import whiteout.interfaces.CascFileSystem;
import whiteout.interfaces.internal.CascFileSystemBridge;

/**
 * Dispatch helper for consumer bindings that accept any {@link CascFileSystem}.
 *
 * <p>{@link #resolveNative(CascFileSystem, Object)} normalises native-backed
 * (Panama-wrapped, implementing {@link NativeHandled}) and pure-Java
 * implementations to a raw C++ {@code CascFileSystem*} address suitable for
 * a native consumer. Native-backed inputs come through unchanged (zero-cost);
 * pure-Java inputs are wrapped via {@link CascFileSystemBridge#createPinned}.
 */
public final class CascFileSystems {

    private CascFileSystems() {}

    /**
     * Resolve {@code impl} to a raw {@code CascFileSystem*} address.
     *
     * <p>For pure-Java implementations the resulting handle is pinned by a
     * {@code Cleaner} keyed on {@code owner}; keep a strong reference to
     * {@code owner} (typically the consumer wrapper) until C++ is done
     * with the CascFileSystem.
     */
    public static long resolveNative(CascFileSystem impl, Object owner) {
        Objects.requireNonNull(impl, "impl");
        if (impl instanceof NativeHandled nh) {
            return nh.nativeHandle().address();
        }
        return CascFileSystemBridge.createPinned(impl, owner);
    }
}
