// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.textures;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.textures.internal.Native;

/**
 * Per-frame metadata for an animated PNG (APNG).
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * ApngFrameInfo allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (PngApngFrameInfo obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class PngApngFrameInfo implements AutoCloseable {
    private static final long BYTES = 28L;

    final MemorySegment handle;
    final boolean owned;

    PngApngFrameInfo(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public PngApngFrameInfo() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_PngApngFrameInfo_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("PngApngFrameInfo allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_textures_PngApngFrameInfo_delete, handle);
        }
    }

    /**
     * Frame sub-rectangle width.
     * @return the width field of this PngApngFrameInfo.
     */
    public int getWidth() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setWidth(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /**
     * Frame sub-rectangle height.
     * @return the height field of this PngApngFrameInfo.
     */
    public int getHeight() {
        return handle.get(ValueLayout.JAVA_INT, 4L);
    }
    public void setHeight(int value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value);
    }
    /**
     * Frame sub-rectangle X offset on the canvas.
     * @return the xOffset field of this PngApngFrameInfo.
     */
    public int getXOffset() {
        return handle.get(ValueLayout.JAVA_INT, 8L);
    }
    public void setXOffset(int value) {
        handle.set(ValueLayout.JAVA_INT, 8L, value);
    }
    /**
     * Frame sub-rectangle Y offset on the canvas.
     * @return the yOffset field of this PngApngFrameInfo.
     */
    public int getYOffset() {
        return handle.get(ValueLayout.JAVA_INT, 12L);
    }
    public void setYOffset(int value) {
        handle.set(ValueLayout.JAVA_INT, 12L, value);
    }
    /**
     * Frame display duration in milliseconds.
     * @return the delayMs field of this PngApngFrameInfo.
     */
    public int getDelayMs() {
        return handle.get(ValueLayout.JAVA_INT, 16L);
    }
    public void setDelayMs(int value) {
        handle.set(ValueLayout.JAVA_INT, 16L, value);
    }
    /**
     * 0 = NONE, 1 = BACKGROUND, 2 = PREVIOUS.
     * @return the disposeOp field of this PngApngFrameInfo.
     */
    public int getDisposeOp() {
        return handle.get(ValueLayout.JAVA_INT, 20L);
    }
    public void setDisposeOp(int value) {
        handle.set(ValueLayout.JAVA_INT, 20L, value);
    }
    /**
     * 0 = SOURCE, 1 = OVER.
     * @return the blendOp field of this PngApngFrameInfo.
     */
    public int getBlendOp() {
        return handle.get(ValueLayout.JAVA_INT, 24L);
    }
    public void setBlendOp(int value) {
        handle.set(ValueLayout.JAVA_INT, 24L, value);
    }
    @Override public String toString() {
        return "PngApngFrameInfo(" + "width=" + getWidth() + ", " + "height=" + getHeight() + ", " + "xOffset=" + getXOffset() + ", " + "yOffset=" + getYOffset() + ", " + "delayMs=" + getDelayMs() + ", " + "disposeOp=" + getDisposeOp() + ", " + "blendOp=" + getBlendOp() + ")";
    }

}
