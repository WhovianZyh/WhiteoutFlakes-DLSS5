// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.utils;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.utils.internal.Native;

/**
 * Java binding for the native VertexBuffer::Attribute type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * VertexBuffer::Attribute allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (VertexBufferAttribute obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class VertexBufferAttribute implements AutoCloseable {
    private static final long BYTES = 24L;

    final MemorySegment handle;
    final boolean owned;

    VertexBufferAttribute(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public VertexBufferAttribute() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_utils_UtilsVertexBufferAttribute_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("VertexBufferAttribute allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_utils_UtilsVertexBufferAttribute_delete, handle);
        }
    }

    /** @return the attr_class field of this UtilsVertexBufferAttribute. */
    public AttributeClass getAttr_class() {
        return AttributeClass.fromInt(handle.get(ValueLayout.JAVA_INT, 0L));
    }
    public void setAttr_class(AttributeClass value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value.value);
    }
    /** @return the encoding field of this UtilsVertexBufferAttribute. */
    public AttributeEncoding getEncoding() {
        return AttributeEncoding.fromInt(handle.get(ValueLayout.JAVA_INT, 4L));
    }
    public void setEncoding(AttributeEncoding value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value.value);
    }
    /** @return the component_count field of this UtilsVertexBufferAttribute. */
    public long getComponent_count() {
        return handle.get(ValueLayout.JAVA_LONG, 8L);
    }
    public void setComponent_count(long value) {
        handle.set(ValueLayout.JAVA_LONG, 8L, value);
    }
    /** @return the offset field of this UtilsVertexBufferAttribute. */
    public long getOffset() {
        return handle.get(ValueLayout.JAVA_LONG, 16L);
    }
    public void setOffset(long value) {
        handle.set(ValueLayout.JAVA_LONG, 16L, value);
    }
    @Override public String toString() {
        return "VertexBufferAttribute(" + "attr_class=" + getAttr_class() + ", " + "encoding=" + getEncoding() + ", " + "component_count=" + getComponent_count() + ", " + "offset=" + getOffset() + ")";
    }

}
