// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.casc;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.casc.internal.Native;

/**
 * Options for creating a new empty CASC storage.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * CreateOptions allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (CreateOptions obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class CreateOptions implements AutoCloseable {
    private static final long BYTES = 80L;

    final MemorySegment handle;
    final boolean owned;

    CreateOptions(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public CreateOptions() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_casc_CascCreateOptions_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("CreateOptions allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_casc_CascCreateOptions_delete, handle);
        }
    }

    /** @return the product field of this CascCreateOptions. */
    public String getProduct() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_casc_CascCreateOptions_get_product, arena, handle);
            MemorySegment __chars = __s.get(ValueLayout.ADDRESS, 0);
            long __len = __s.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__chars == null || __chars.equals(MemorySegment.NULL)) return "";
            byte[] __bytes = __chars.reinterpret(__len).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_CString_free, __s);
            return new String(__bytes, StandardCharsets.UTF_8);
        }
    }
    public void setProduct(String value) {
        try (Arena arena = Arena.ofConfined()) {
            byte[] __utf = value == null ? new byte[0] : value.getBytes(StandardCharsets.UTF_8);
            MemorySegment __seg = arena.allocate(__utf.length + 1);
            MemorySegment.copy(__utf, 0, __seg, ValueLayout.JAVA_BYTE, 0, __utf.length);
            __seg.set(ValueLayout.JAVA_BYTE, __utf.length, (byte) 0);
            NativeCommon.invokeNative(Native.whiteout_casc_CascCreateOptions_set_product, handle, __seg);
        }
    }
    /** @return the version field of this CascCreateOptions. */
    public String getVersion() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_casc_CascCreateOptions_get_version, arena, handle);
            MemorySegment __chars = __s.get(ValueLayout.ADDRESS, 0);
            long __len = __s.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__chars == null || __chars.equals(MemorySegment.NULL)) return "";
            byte[] __bytes = __chars.reinterpret(__len).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_CString_free, __s);
            return new String(__bytes, StandardCharsets.UTF_8);
        }
    }
    public void setVersion(String value) {
        try (Arena arena = Arena.ofConfined()) {
            byte[] __utf = value == null ? new byte[0] : value.getBytes(StandardCharsets.UTF_8);
            MemorySegment __seg = arena.allocate(__utf.length + 1);
            MemorySegment.copy(__utf, 0, __seg, ValueLayout.JAVA_BYTE, 0, __utf.length);
            __seg.set(ValueLayout.JAVA_BYTE, __utf.length, (byte) 0);
            NativeCommon.invokeNative(Native.whiteout_casc_CascCreateOptions_set_version, handle, __seg);
        }
    }
    /**
     * 1 GB.
     * @return the archiveMaxSize field of this CascCreateOptions.
     */
    public int getArchiveMaxSize() {
        return handle.get(ValueLayout.JAVA_INT, 64L);
    }
    public void setArchiveMaxSize(int value) {
        handle.set(ValueLayout.JAVA_INT, 64L, value);
    }
    /**
     * 64 KB.
     * @return the blteFrameSize field of this CascCreateOptions.
     */
    public int getBlteFrameSize() {
        return handle.get(ValueLayout.JAVA_INT, 68L);
    }
    public void setBlteFrameSize(int value) {
        handle.set(ValueLayout.JAVA_INT, 68L, value);
    }
    /** @return the rootFormat field of this CascCreateOptions. */
    public RootFormat getRootFormat() {
        return RootFormat.fromInt(handle.get(ValueLayout.JAVA_INT, 72L));
    }
    public void setRootFormat(RootFormat value) {
        handle.set(ValueLayout.JAVA_INT, 72L, value.value);
    }
    @Override public String toString() {
        return "CreateOptions(" + "product=" + getProduct() + ", " + "version=" + getVersion() + ", " + "archiveMaxSize=" + getArchiveMaxSize() + ", " + "blteFrameSize=" + getBlteFrameSize() + ", " + "rootFormat=" + getRootFormat() + ")";
    }

}
