// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mdx;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mdx.internal.Native;

/**
 * Java binding for the native Extent type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Extent allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (Extent obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class Extent implements AutoCloseable {
    private static final long BYTES = 28L;

    final MemorySegment handle;
    final boolean owned;

    Extent(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public Extent() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxExtent_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("Extent allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxExtent_delete, handle);
        }
    }

    /** @return the boundsRadius field of this MdxExtent. */
    public float getBoundsRadius() {
        return handle.get(ValueLayout.JAVA_FLOAT, 0L);
    }
    public void setBoundsRadius(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 0L, value);
    }
    /** @return the minimum field of this MdxExtent. */
    public Vector3f getMinimum() {
        return Handles.wrapVector3f(handle.asSlice(4L, 12L), false);
    }
    public void setMinimum(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxExtent_set_minimum, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 4L, 12L);
    }
    /** @return the maximum field of this MdxExtent. */
    public Vector3f getMaximum() {
        return Handles.wrapVector3f(handle.asSlice(16L, 12L), false);
    }
    public void setMaximum(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxExtent_set_maximum, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 16L, 12L);
    }
    @Override public String toString() {
        return "Extent(" + "boundsRadius=" + getBoundsRadius() + ")";
    }

}
