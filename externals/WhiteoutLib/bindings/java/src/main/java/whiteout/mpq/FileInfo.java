// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mpq;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mpq.internal.Native;

/**
 * Information about a single file in the archive.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * FileInfo allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (FileInfo obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class FileInfo implements AutoCloseable {
    private static final long BYTES = 48L;

    final MemorySegment handle;
    final boolean owned;

    FileInfo(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public FileInfo() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mpq_MpqFileInfo_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("FileInfo allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mpq_MpqFileInfo_delete, handle);
        }
    }

    /**
     * Filename (from listfile or hash table lookup).
     * @return the name field of this MpqFileInfo.
     */
    public String getName() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mpq_MpqFileInfo_get_name, arena, handle);
            MemorySegment __chars = __s.get(ValueLayout.ADDRESS, 0);
            long __len = __s.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__chars == null || __chars.equals(MemorySegment.NULL)) return "";
            byte[] __bytes = __chars.reinterpret(__len).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_CString_free, __s);
            return new String(__bytes, StandardCharsets.UTF_8);
        }
    }
    public void setName(String value) {
        try (Arena arena = Arena.ofConfined()) {
            byte[] __utf = value == null ? new byte[0] : value.getBytes(StandardCharsets.UTF_8);
            MemorySegment __seg = arena.allocate(__utf.length + 1);
            MemorySegment.copy(__utf, 0, __seg, ValueLayout.JAVA_BYTE, 0, __utf.length);
            __seg.set(ValueLayout.JAVA_BYTE, __utf.length, (byte) 0);
            NativeCommon.invokeNative(Native.whiteout_mpq_MpqFileInfo_set_name, handle, __seg);
        }
    }
    /**
     * Compressed storage size in bytes.
     * @return the compressedSize field of this MpqFileInfo.
     */
    public int getCompressedSize() {
        return handle.get(ValueLayout.JAVA_INT, 32L);
    }
    public void setCompressedSize(int value) {
        handle.set(ValueLayout.JAVA_INT, 32L, value);
    }
    /**
     * Uncompressed (original) file size in bytes.
     * @return the uncompressedSize field of this MpqFileInfo.
     */
    public int getUncompressedSize() {
        return handle.get(ValueLayout.JAVA_INT, 36L);
    }
    public void setUncompressedSize(int value) {
        handle.set(ValueLayout.JAVA_INT, 36L, value);
    }
    /**
     * Block entry flags (see FileFlags enum).
     * @return the flags field of this MpqFileInfo.
     */
    public FileFlags getFlags() {
        return FileFlags.fromInt(handle.get(ValueLayout.JAVA_INT, 40L));
    }
    public void setFlags(FileFlags value) {
        handle.set(ValueLayout.JAVA_INT, 40L, value.value);
    }
    /**
     * Locale ID (typically Locale::Neutral).
     * @return the locale field of this MpqFileInfo.
     */
    public short getLocale() {
        return handle.get(ValueLayout.JAVA_SHORT, 44L);
    }
    public void setLocale(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 44L, value);
    }
    @Override public String toString() {
        return "FileInfo(" + "name=" + getName() + ", " + "compressedSize=" + getCompressedSize() + ", " + "uncompressedSize=" + getUncompressedSize() + ", " + "flags=" + getFlags() + ", " + "locale=" + getLocale() + ")";
    }

}
