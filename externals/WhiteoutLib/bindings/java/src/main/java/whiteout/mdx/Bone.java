// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mdx;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mdx.internal.Native;

/**
 * Skeleton bone for skinned animation
 * 
 * Bones  form the skeleton that deforms mesh geometry. Each bone is a node in the hierarchy and can affect one or more geosets.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Bone allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (Bone obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class Bone implements AutoCloseable {
    private static final long BYTES = 280L;

    final MemorySegment handle;
    final boolean owned;

    Bone(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public Bone() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxBone_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("Bone allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxBone_delete, handle);
        }
    }

    /**
     * Base node data with transform
     * @return the node field of this MdxBone.
     */
    public Node getNode() {
        return new Node(handle.asSlice(0L, 272L), false);
    }
    public void setNode(Node value) {
        NativeCommon.invokeNative(Native.whiteout_mdx_MdxBone_set_node, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    /**
     * Geoset this bone affects
     * @return the geosetId field of this MdxBone.
     */
    public int getGeosetId() {
        return handle.get(ValueLayout.JAVA_INT, 272L);
    }
    public void setGeosetId(int value) {
        handle.set(ValueLayout.JAVA_INT, 272L, value);
    }
    /**
     * Geoset animation index
     * @return the geosetAnimationId field of this MdxBone.
     */
    public int getGeosetAnimationId() {
        return handle.get(ValueLayout.JAVA_INT, 276L);
    }
    public void setGeosetAnimationId(int value) {
        handle.set(ValueLayout.JAVA_INT, 276L, value);
    }
    @Override public String toString() {
        return "Bone(" + "geosetId=" + getGeosetId() + ", " + "geosetAnimationId=" + getGeosetAnimationId() + ")";
    }

}
