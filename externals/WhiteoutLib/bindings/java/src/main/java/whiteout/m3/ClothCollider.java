// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * PHCC — Cloth collider (v0, 76 bytes)
 * 
 * Capsule-shaped collider used by cloth simulation.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * ClothCollider allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (ClothCollider obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class ClothCollider implements AutoCloseable {
    private static final long BYTES = 80L;

    final MemorySegment handle;
    final boolean owned;

    ClothCollider(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public ClothCollider() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3ClothCollider_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("ClothCollider allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3ClothCollider_delete, handle);
        }
    }

    /**
     * Capsule radius
     * @return the radius field of this M3ClothCollider.
     */
    public float getRadius() {
        return handle.get(ValueLayout.JAVA_FLOAT, 64L);
    }
    public void setRadius(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 64L, value);
    }
    /**
     * Capsule height
     * @return the height field of this M3ClothCollider.
     */
    public float getHeight() {
        return handle.get(ValueLayout.JAVA_FLOAT, 68L);
    }
    public void setHeight(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 68L, value);
    }
    /**
     * Alignment padding
     * @return the padding field of this M3ClothCollider.
     */
    public int getPadding() {
        return handle.get(ValueLayout.JAVA_INT, 72L);
    }
    public void setPadding(int value) {
        handle.set(ValueLayout.JAVA_INT, 72L, value);
    }
    @Override public String toString() {
        return "ClothCollider(" + "radius=" + getRadius() + ", " + "height=" + getHeight() + ", " + "padding=" + getPadding() + ")";
    }

}
