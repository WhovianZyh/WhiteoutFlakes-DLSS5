// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * Outcome of rebuilding a StandardMaterial from a DataDrivenMaterial
 * 
 * Not every data-driven material has a standard equivalent: some were authored directly against the shader-graph vocabulary, and others are the converted form of a DisplacementMaterial or ReflectionMaterial. `blocker` says which.
 * 
 * Conversion is lossy even when it succeeds, because the forward direction is: variant fragments collapse onto one layer slot, several fragments are derived from the model rather than the material, and per-field animation links are not carried in the blob. `lossy` lists what was dropped for this material.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * StandardMaterialConversion allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (StandardMaterialConversion obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class StandardMaterialConversion implements AutoCloseable {
    private static final long BYTES = 9328L;

    final MemorySegment handle;
    final boolean owned;

    StandardMaterialConversion(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public StandardMaterialConversion() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3StandardMaterialConversion_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("StandardMaterialConversion allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3StandardMaterialConversion_delete, handle);
        }
    }

    /**
     * Whether `material` was produced
     * @return the converted field of this M3StandardMaterialConversion.
     */
    public boolean getConverted() {
        return handle.get(ValueLayout.JAVA_BYTE, 0L) != 0;
    }
    public void setConverted(boolean value) {
        handle.set(ValueLayout.JAVA_BYTE, 0L, (byte) (value ? 1 : 0));
    }
    /**
     * Why not, when `converted` is false
     * @return the blocker field of this M3StandardMaterialConversion.
     */
    public String getBlocker() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3StandardMaterialConversion_get_blocker, arena, handle);
            MemorySegment __chars = __s.get(ValueLayout.ADDRESS, 0);
            long __len = __s.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__chars == null || __chars.equals(MemorySegment.NULL)) return "";
            byte[] __bytes = __chars.reinterpret(__len).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_CString_free, __s);
            return new String(__bytes, StandardCharsets.UTF_8);
        }
    }
    public void setBlocker(String value) {
        try (Arena arena = Arena.ofConfined()) {
            byte[] __utf = value == null ? new byte[0] : value.getBytes(StandardCharsets.UTF_8);
            MemorySegment __seg = arena.allocate(__utf.length + 1);
            MemorySegment.copy(__utf, 0, __seg, ValueLayout.JAVA_BYTE, 0, __utf.length);
            __seg.set(ValueLayout.JAVA_BYTE, __utf.length, (byte) 0);
            NativeCommon.invokeNative(Native.whiteout_m3_M3StandardMaterialConversion_set_blocker, handle, __seg);
        }
    }
    /**
     * Only meaningful when `converted`
     * @return the material field of this M3StandardMaterialConversion.
     */
    public StandardMaterial getMaterial() {
        return new StandardMaterial(handle.asSlice(64L, 9256L), false);
    }
    public void setMaterial(StandardMaterial value) {
        NativeCommon.invokeNative(Native.whiteout_m3_M3StandardMaterialConversion_set_material, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    @Override public String toString() {
        return "StandardMaterialConversion(" + "converted=" + getConverted() + ", " + "blocker=" + getBlocker() + ")";
    }

}
