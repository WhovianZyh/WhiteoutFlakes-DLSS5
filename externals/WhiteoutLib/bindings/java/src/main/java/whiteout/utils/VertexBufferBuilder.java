// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.utils;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.utils.internal.Native;

/**
 * Java binding for the native VertexBufferBuilder type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * VertexBufferBuilder allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (VertexBufferBuilder obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class VertexBufferBuilder implements AutoCloseable {
    private static final long BYTES = 8L;

    final MemorySegment handle;
    final boolean owned;

    VertexBufferBuilder(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public VertexBufferBuilder() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_utils_UtilsVertexBufferBuilder_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("VertexBufferBuilder allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_utils_UtilsVertexBufferBuilder_delete, handle);
        }
    }

    /**
     * Non-template float-attribute entry point — accepts an already- flattened `f32` array as a `std::span` so the binding layer can aim straight at numpy buffers / `Float32Array` views with no intermediate copy (vertex_count = data.size() / components).
     *
     * @param data float[] input.
     * @param components long input.
     * @param attr_class AttributeClass input.
     * @param encoding AttributeEncoding input.
     * @param align long input.
     * @return a fresh VertexBufferBuilder owning a native allocation.
     */
    public VertexBufferBuilder declareFloatAttribute(float[] data, long components, AttributeClass attr_class, AttributeEncoding encoding, long align) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment data_seg = arena.allocate(data.length * 4L);
            MemorySegment.copy(data, 0, data_seg, ValueLayout.JAVA_FLOAT, 0, data.length);
            MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_utils_UtilsVertexBufferBuilder_declareFloatAttribute, handle, data_seg, (long) data.length, components, attr_class.value, encoding.value, align);
            return new VertexBufferBuilder(__h, false);
        }
    }

    /**
     * Non-template integer-attribute entry point — accepts an already- flattened `u32` array. Smaller integer types (u8/u16/i8/i16/i32) are upcast at the call site. `std::span` keeps the boundary zero-copy.
     *
     * @param data int[] input.
     * @param components long input.
     * @param attr_class AttributeClass input.
     * @param encoding AttributeEncoding input.
     * @param align long input.
     * @return a fresh VertexBufferBuilder owning a native allocation.
     */
    public VertexBufferBuilder declareIntAttribute(int[] data, long components, AttributeClass attr_class, AttributeEncoding encoding, long align) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment data_seg = arena.allocate(data.length * 4L);
            MemorySegment.copy(data, 0, data_seg, ValueLayout.JAVA_INT, 0, data.length);
            MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_utils_UtilsVertexBufferBuilder_declareIntAttribute, handle, data_seg, (long) data.length, components, attr_class.value, encoding.value, align);
            return new VertexBufferBuilder(__h, false);
        }
    }

    /**
     * Build the interleaved vertex buffer from all declared attributes. @param pool  Optional WorkerPool for parallel encoding. When non-null and the vertex count is large enough, the vertex encoding loop is split into cache-friendly chunks across workers.
     *
     * @param pool WorkerPool input.
     * @return a fresh VertexBuffer owning a native allocation.
     */
    public VertexBuffer build(whiteout.interfaces.WorkerPool pool) {
        long __pool_h = pool == null ? 0L
        : whiteout.host.WorkerPools.resolveNative(pool, pool);
        MemorySegment __pool_seg = __pool_h == 0L
        ? MemorySegment.NULL : MemorySegment.ofAddress(__pool_h);
        try {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_utils_UtilsVertexBufferBuilder_build, handle, __pool_seg);
        return new VertexBuffer(__h, true);
        } finally {
        java.lang.ref.Reference.reachabilityFence(pool);
        }
    }

    @Override public String toString() {
        return "VertexBufferBuilder@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
