// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * BBSC — Billboard behavior (v0, 48 bytes)
 * 
 * Turns one bone to face the camera. The only source of billboarding there is: `BoneFlag::Billboard1/2` are set on no bone in the whole corpus.
 * 
 * StarCraft II applies these at draw time, per view, from `sub_10290A890` — `CBBSolver::Solve` is a stub — and writes the bone's *local* rotation, so the subtree follows.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * BillboardBehavior allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (BillboardBehavior obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class BillboardBehavior implements AutoCloseable {
    private static final long BYTES = 64L;

    final MemorySegment handle;
    final boolean owned;

    BillboardBehavior(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public BillboardBehavior() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3BillboardBehavior_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("BillboardBehavior allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3BillboardBehavior_delete, handle);
        }
    }

    /**
     * Dependent bone indices (U16_). Empty in every shipped record; the engine never reads them
     * @return the dependents field of this M3BillboardBehavior.
     */
    public int getDependentsCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m3_M3BillboardBehavior_get_dependents_count, handle);
    }
    public short[] getDependents() {
        long __count = (long) NativeCommon.invokeNative(Native.whiteout_m3_M3BillboardBehavior_get_dependents_count, handle);
        MemorySegment __ptr = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3BillboardBehavior_get_dependents_data, handle);
        if (__count == 0 || __ptr == null || __ptr.equals(MemorySegment.NULL)) return new short[0];
        long __scalars = __count * 1L;
        return __ptr.reinterpret(__scalars * 2L).toArray(ValueLayout.JAVA_SHORT);
    }
    public void setDependents(short[] values) {
        try (Arena arena = Arena.ofConfined()) {
            long __count = (long) values.length / 1;
            MemorySegment __seg = arena.allocate((long) values.length * 2L);
            if (values.length > 0) MemorySegment.copy(values, 0, __seg, ValueLayout.JAVA_SHORT, 0, values.length);
            NativeCommon.invokeNative(Native.whiteout_m3_M3BillboardBehavior_assign_dependents, handle, __seg, __count);
        }
    }
    public void resizeDependents(int count) {
        NativeCommon.invokeNative(Native.whiteout_m3_M3BillboardBehavior_resize_dependents, handle, (long) count);
    }
    /**
     * Index into BONE array
     * @return the boneIndex field of this M3BillboardBehavior.
     */
    public short getBoneIndex() {
        return handle.get(ValueLayout.JAVA_SHORT, 24L);
    }
    public void setBoneIndex(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 24L, value);
    }
    /**
     * Which axes may turn — see BillboardType
     * @return the billboardType field of this M3BillboardBehavior.
     */
    public byte getBillboardType() {
        return handle.get(ValueLayout.JAVA_BYTE, 26L);
    }
    public void setBillboardType(byte value) {
        handle.set(ValueLayout.JAVA_BYTE, 26L, value);
    }
    /**
     * Non-zero: aim from this bone at the eye. Zero: aim along the camera's view direction instead, so every such bone shares one orientation
     * @return the cameraLookAt field of this M3BillboardBehavior.
     */
    public byte getCameraLookAt() {
        return handle.get(ValueLayout.JAVA_BYTE, 27L);
    }
    public void setCameraLookAt(byte value) {
        handle.set(ValueLayout.JAVA_BYTE, 27L, value);
    }
    /**
     * MISNAMED: not a direction. A rotation applied *before* the billboard basis, and only by the axis-locked types 0/1/2
     * @return the up field of this M3BillboardBehavior.
     */
    public Quaternion getUp() {
        return Handles.wrapQuaternion(handle.asSlice(28L, 16L), false);
    }
    public void setUp(Quaternion value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3BillboardBehavior_set_up, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 28L, 16L);
    }
    /**
     * MISNAMED likewise: the same kind of pre-rotation, taken only by type 6, and only on a bone whose parent is another bone. Types 3/4/5 take neither
     * @return the forward field of this M3BillboardBehavior.
     */
    public Quaternion getForward() {
        return Handles.wrapQuaternion(handle.asSlice(44L, 16L), false);
    }
    public void setForward(Quaternion value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3BillboardBehavior_set_forward, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 44L, 16L);
    }
    @Override public String toString() {
        return "BillboardBehavior(" + "boneIndex=" + getBoneIndex() + ", " + "billboardType=" + getBillboardType() + ", " + "cameraLookAt=" + getCameraLookAt() + ")";
    }

}
