// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.textures;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.textures.internal.Native;

/**
 * Per-write options for GIF encoding.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * SaveOptions allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (GifSaveOptions obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class GifSaveOptions implements AutoCloseable {
    private static final long BYTES = 16L;

    final MemorySegment handle;
    final boolean owned;

    GifSaveOptions(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public GifSaveOptions() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_GifSaveOptions_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("GifSaveOptions allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_textures_GifSaveOptions_delete, handle);
        }
    }

    /**
     * Delay between frames in centiseconds (1/100 s).  0 = unspecified.
     * @return the delayCs field of this GifSaveOptions.
     */
    public short getDelayCs() {
        return handle.get(ValueLayout.JAVA_SHORT, 0L);
    }
    public void setDelayCs(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 0L, value);
    }
    /**
     * Number of times the animation should loop.  0 = loop forever.
     * @return the loopCount field of this GifSaveOptions.
     */
    public short getLoopCount() {
        return handle.get(ValueLayout.JAVA_SHORT, 2L);
    }
    public void setLoopCount(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 2L, value);
    }
    /**
     * Enable blue-noise ordered dithering when mapping pixels to the palette.
     * @return the dither field of this GifSaveOptions.
     */
    public boolean getDither() {
        return handle.get(ValueLayout.JAVA_BYTE, 4L) != 0;
    }
    public void setDither(boolean value) {
        handle.set(ValueLayout.JAVA_BYTE, 4L, (byte) (value ? 1 : 0));
    }
    /**
     * Dither strength in [0, 1].  0 = no visible dithering, 1 = full.
     * @return the ditherStrength field of this GifSaveOptions.
     */
    public float getDitherStrength() {
        return handle.get(ValueLayout.JAVA_FLOAT, 8L);
    }
    public void setDitherStrength(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 8L, value);
    }
    /**
     * Emit a transparent background. Pixels whose source alpha is below 50% become the GIF's transparent palette index; the rest are quantised normally. GIF transparency is 1-bit, so partially-covered (anti- aliased) edge pixels are forced fully opaque or fully transparent.
     * @return the transparent field of this GifSaveOptions.
     */
    public boolean getTransparent() {
        return handle.get(ValueLayout.JAVA_BYTE, 12L) != 0;
    }
    public void setTransparent(boolean value) {
        handle.set(ValueLayout.JAVA_BYTE, 12L, (byte) (value ? 1 : 0));
    }
    @Override public String toString() {
        return "GifSaveOptions(" + "delayCs=" + getDelayCs() + ", " + "loopCount=" + getLoopCount() + ", " + "dither=" + getDither() + ", " + "ditherStrength=" + getDitherStrength() + ", " + "transparent=" + getTransparent() + ")";
    }

}
