// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.host;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.host.internal.Native;

/**
 * java_package=whiteout.utils — abstract file system that resolves files by numeric data ID (e.g. CASC).
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * CascFileSystem allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (CascFileSystem obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class CascFileSystem implements AutoCloseable, whiteout.interfaces.CascFileSystem, whiteout.host.NativeHandled {
    private static final long BYTES = 8L;

    final MemorySegment handle;
    final boolean owned;

    CascFileSystem(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_host_CascFileSystem_delete, handle);
        }
    }

    /**
     * Read the entire contents of a file by its numeric data ID.
     *
     * @param fileId int input.
     * @return a fresh byte[] copied out of native memory.
     */
    public byte[] readFile(int fileId) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __struct = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_host_CascFileSystem_readFile, arena, handle, fileId);
            MemorySegment __data = __struct.get(ValueLayout.ADDRESS, 0);
            long __size = __struct.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__data == null || __data.equals(MemorySegment.NULL)) return new byte[0];
            byte[] __out = __data.reinterpret(__size).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_Bytes_free, __struct);
            return __out;
        }
    }

    /**
     * Resolve a path to a numeric file ID (nullable).
     *
     * @param path String input.
     * @return a fresh Integer owning a native allocation.
     */
    public Integer reserveFileId(String path) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment path_seg = path == null
                ? MemorySegment.NULL
                : arena.allocateFrom(path, StandardCharsets.UTF_8);
            MemorySegment __out_value = arena.allocate(ValueLayout.JAVA_INT);
            if (((int) NativeCommon.invokeNative(Native.whiteout_host_CascFileSystem_reserveFileId, handle, path_seg, __out_value)) == 0) return null;
            return __out_value.get(ValueLayout.JAVA_INT, 0L);
        }
    }

    /**
     * Write a file by its numeric data ID. Returns true on success.
     *
     * @param fileId int input.
     * @param data byte[] input.
     * @return boolean result.
     */
    public boolean writeFile(int fileId, byte[] data) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment data_seg = data == null || data.length == 0
                ? MemorySegment.NULL
                : arena.allocate(data.length);
            if (data != null && data.length != 0) {
                MemorySegment.copy(data, 0, data_seg, ValueLayout.JAVA_BYTE, 0, data.length);
            }
            return ((int) NativeCommon.invokeNative(Native.whiteout_host_CascFileSystem_writeFile, handle, fileId, data_seg, (long) (data == null ? 0 : data.length))) != 0;
        }
    }

    /**
     * Check if a file with the given data ID exists.
     *
     * @param fileId int input.
     * @return boolean result.
     */
    public boolean fileExists(int fileId) {
        return ((int) NativeCommon.invokeNative(Native.whiteout_host_CascFileSystem_fileExists, handle, fileId)) != 0;
    }

    /** {@inheritDoc} */
    @Override
    public MemorySegment nativeHandle() { return handle; }

    @Override public String toString() {
        return "CascFileSystem@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
