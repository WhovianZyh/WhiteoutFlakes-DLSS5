// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.host;

import java.util.Objects;

import whiteout.interfaces.WorkerPool;
import whiteout.interfaces.internal.WorkerPoolBridge;

/**
 * Dispatch helper for consumer bindings that accept any {@link WorkerPool}.
 *
 * <p>{@link #resolveNative(WorkerPool, Object)} normalises native-backed
 * (Panama-wrapped, implementing {@link NativeHandled}) and pure-Java
 * implementations to a raw C++ {@code WorkerPool*} address suitable for
 * a native consumer. Native-backed inputs come through unchanged (zero-cost);
 * pure-Java inputs are wrapped via {@link WorkerPoolBridge#createPinned}.
 */
public final class WorkerPools {

    private WorkerPools() {}

    /**
     * Resolve {@code impl} to a raw {@code WorkerPool*} address.
     *
     * <p>For pure-Java implementations the resulting handle is pinned by a
     * {@code Cleaner} keyed on {@code owner}; keep a strong reference to
     * {@code owner} (typically the consumer wrapper) until C++ is done
     * with the WorkerPool.
     */
    public static long resolveNative(WorkerPool impl, Object owner) {
        Objects.requireNonNull(impl, "impl");
        if (impl instanceof NativeHandled nh) {
            return nh.nativeHandle().address();
        }
        return WorkerPoolBridge.createPinned(impl, owner);
    }
}
