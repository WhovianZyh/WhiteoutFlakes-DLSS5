// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * PHYV — six floats that overwrite the head of a tuning block the client otherwise fills with constants. Version 1+.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * PhysicsTuning allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (PhysicsTuning obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class PhysicsTuning implements AutoCloseable {
    private static final long BYTES = 24L;

    final MemorySegment handle;
    final boolean owned;

    PhysicsTuning(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public PhysicsTuning() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsTuning_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("PhysicsTuning allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsTuning_delete, handle);
        }
    }

    /** @return the values field of this M2PhysicsTuning. */
    public int getValuesCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsTuning_values_size);
    }
    public float getValuesAt(int index) {
        return (float) NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsTuning_get_values_at, handle, (long) index);
    }
    public void setValuesAt(int index, float value) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsTuning_set_values_at, handle, (long) index, value);
    }
    @Override public String toString() {
        return "PhysicsTuning@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
