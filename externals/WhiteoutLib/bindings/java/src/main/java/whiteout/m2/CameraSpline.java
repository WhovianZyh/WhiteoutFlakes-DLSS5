// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * Java binding for the native CameraSpline type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * CameraSpline allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (CameraSpline obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class CameraSpline implements AutoCloseable {
    private static final long BYTES = 36L;

    final MemorySegment handle;
    final boolean owned;

    CameraSpline(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public CameraSpline() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2CameraSpline_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("CameraSpline allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2CameraSpline_delete, handle);
        }
    }

    /** @return the value field of this M2CameraSpline. */
    public Vector3f getValue() {
        return Handles.wrapVector3f(handle.asSlice(0L, 12L), false);
    }
    public void setValue(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2CameraSpline_set_value, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 0L, 12L);
    }
    /** @return the inTangent field of this M2CameraSpline. */
    public Vector3f getInTangent() {
        return Handles.wrapVector3f(handle.asSlice(12L, 12L), false);
    }
    public void setInTangent(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2CameraSpline_set_inTangent, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 12L, 12L);
    }
    /** @return the outTangent field of this M2CameraSpline. */
    public Vector3f getOutTangent() {
        return Handles.wrapVector3f(handle.asSlice(24L, 12L), false);
    }
    public void setOutTangent(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2CameraSpline_set_outTangent, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 24L, 12L);
    }
    @Override public String toString() {
        return "CameraSpline@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
