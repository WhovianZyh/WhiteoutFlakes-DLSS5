// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mdx;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mdx.internal.Native;

/**
 * Animation sequence definition
 * 
 * Sequences define named animation clips with specific frame ranges. Examples: "Stand", "Walk", "Attack", "Death"
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Sequence allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (Sequence obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class Sequence implements AutoCloseable {
    private static final long BYTES = 88L;

    final MemorySegment handle;
    final boolean owned;

    Sequence(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public Sequence() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxSequence_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("Sequence allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxSequence_delete, handle);
        }
    }

    /**
     * Sequence name (e.g., "Stand", "Walk")
     * @return the name field of this MdxSequence.
     */
    public String getName() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxSequence_get_name, arena, handle);
            MemorySegment __chars = __s.get(ValueLayout.ADDRESS, 0);
            long __len = __s.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__chars == null || __chars.equals(MemorySegment.NULL)) return "";
            byte[] __bytes = __chars.reinterpret(__len).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_CString_free, __s);
            return new String(__bytes, StandardCharsets.UTF_8);
        }
    }
    public void setName(String value) {
        try (Arena arena = Arena.ofConfined()) {
            byte[] __utf = value == null ? new byte[0] : value.getBytes(StandardCharsets.UTF_8);
            MemorySegment __seg = arena.allocate(__utf.length + 1);
            MemorySegment.copy(__utf, 0, __seg, ValueLayout.JAVA_BYTE, 0, __utf.length);
            __seg.set(ValueLayout.JAVA_BYTE, __utf.length, (byte) 0);
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxSequence_set_name, handle, __seg);
        }
    }
    /**
     * Starting frame number
     * @return the intervalStart field of this MdxSequence.
     */
    public int getIntervalStart() {
        return handle.get(ValueLayout.JAVA_INT, 32L);
    }
    public void setIntervalStart(int value) {
        handle.set(ValueLayout.JAVA_INT, 32L, value);
    }
    /**
     * Ending frame number (exclusive)
     * @return the intervalEnd field of this MdxSequence.
     */
    public int getIntervalEnd() {
        return handle.get(ValueLayout.JAVA_INT, 36L);
    }
    public void setIntervalEnd(int value) {
        handle.set(ValueLayout.JAVA_INT, 36L, value);
    }
    /**
     * Movement speed during this animation
     * @return the moveSpeed field of this MdxSequence.
     */
    public float getMoveSpeed() {
        return handle.get(ValueLayout.JAVA_FLOAT, 40L);
    }
    public void setMoveSpeed(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 40L, value);
    }
    /**
     * Playback flags
     * @return the flags field of this MdxSequence.
     */
    public SequenceFlag getFlags() {
        return SequenceFlag.fromInt(handle.get(ValueLayout.JAVA_INT, 44L));
    }
    public void setFlags(SequenceFlag value) {
        handle.set(ValueLayout.JAVA_INT, 44L, value.value);
    }
    /**
     * Rarity factor for variation sequences
     * @return the rarity field of this MdxSequence.
     */
    public float getRarity() {
        return handle.get(ValueLayout.JAVA_FLOAT, 48L);
    }
    public void setRarity(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 48L, value);
    }
    /**
     * Sync point for blending
     * @return the syncPoint field of this MdxSequence.
     */
    public int getSyncPoint() {
        return handle.get(ValueLayout.JAVA_INT, 52L);
    }
    public void setSyncPoint(int value) {
        handle.set(ValueLayout.JAVA_INT, 52L, value);
    }
    /**
     * Bounding volume for this sequence
     * @return the extent field of this MdxSequence.
     */
    public Extent getExtent() {
        return new Extent(handle.asSlice(56L, 28L), false);
    }
    public void setExtent(Extent value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxSequence_set_extent, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(value.handle, 0L, handle, 56L, 28L);
    }
    @Override public String toString() {
        return "Sequence(" + "name=" + getName() + ", " + "intervalStart=" + getIntervalStart() + ", " + "intervalEnd=" + getIntervalEnd() + ", " + "moveSpeed=" + getMoveSpeed() + ", " + "flags=" + getFlags() + ", " + "rarity=" + getRarity() + ", " + "syncPoint=" + getSyncPoint() + ")";
    }

}
