// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * A `.phys` chunk this library does not know, kept verbatim so a parse/write cycle does not drop it.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * PhysicsUnknownChunk allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (PhysicsUnknownChunk obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class PhysicsUnknownChunk implements AutoCloseable {
    private static final long BYTES = 32L;

    final MemorySegment handle;
    final boolean owned;

    PhysicsUnknownChunk(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public PhysicsUnknownChunk() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsUnknownChunk_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("PhysicsUnknownChunk allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsUnknownChunk_delete, handle);
        }
    }

    /**
     * FourCC as written on disk — `.phys` stores chunk ids reversed, so a PHYS chunk reads as "SYHP".
     * @return the tag field of this M2PhysicsUnknownChunk.
     */
    public int getTagCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsUnknownChunk_tag_size);
    }
    public byte getTagAt(int index) {
        return (byte) NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsUnknownChunk_get_tag_at, handle, (long) index);
    }
    public void setTagAt(int index, byte value) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsUnknownChunk_set_tag_at, handle, (long) index, value);
    }
    /** @return the data field of this M2PhysicsUnknownChunk. */
    public int getDataCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsUnknownChunk_get_data_count, handle);
    }
    public byte[] getData() {
        long __count = (long) NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsUnknownChunk_get_data_count, handle);
        MemorySegment __ptr = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsUnknownChunk_get_data_data, handle);
        if (__count == 0 || __ptr == null || __ptr.equals(MemorySegment.NULL)) return new byte[0];
        long __scalars = __count * 1L;
        return __ptr.reinterpret(__scalars * 1L).toArray(ValueLayout.JAVA_BYTE);
    }
    public void setData(byte[] values) {
        try (Arena arena = Arena.ofConfined()) {
            long __count = (long) values.length / 1;
            MemorySegment __seg = arena.allocate((long) values.length * 1L);
            if (values.length > 0) MemorySegment.copy(values, 0, __seg, ValueLayout.JAVA_BYTE, 0, values.length);
            NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsUnknownChunk_assign_data, handle, __seg, __count);
        }
    }
    public void resizeData(int count) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsUnknownChunk_resize_data, handle, (long) count);
    }
    @Override public String toString() {
        return "PhysicsUnknownChunk@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
