// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * One DPIV (pivot displacement) record, 32 bytes.
 * 
 * The client keeps the payload pointer and a record count of `chunkSize / 32`, so a chunk holds N of these — the corpus has both 1- and 2-record chunks.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * PivotDisplacementData allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (PivotDisplacementData obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class PivotDisplacementData implements AutoCloseable {
    private static final long BYTES = 32L;

    final MemorySegment handle;
    final boolean owned;

    PivotDisplacementData(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public PivotDisplacementData() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PivotDisplacementData_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("PivotDisplacementData allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PivotDisplacementData_delete, handle);
        }
    }

    /**
     * small displacement; Z varies most
     * @return the offset field of this M2PivotDisplacementData.
     */
    public Vector3f getOffset() {
        return Handles.wrapVector3f(handle.asSlice(0L, 12L), false);
    }
    public void setOffset(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PivotDisplacementData_set_offset, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 0L, 12L);
    }
    /**
     * 0 or 1
     * @return the flags field of this M2PivotDisplacementData.
     */
    public int getFlags() {
        return handle.get(ValueLayout.JAVA_INT, 12L);
    }
    public void setFlags(int value) {
        handle.set(ValueLayout.JAVA_INT, 12L, value);
    }
    /**
     * always 0
     * @return the reserved field of this M2PivotDisplacementData.
     */
    public int getReservedCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m2_M2PivotDisplacementData_reserved_size);
    }
    public int getReservedAt(int index) {
        return (int) NativeCommon.invokeNative(Native.whiteout_m2_M2PivotDisplacementData_get_reserved_at, handle, (long) index);
    }
    public void setReservedAt(int index, int value) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2PivotDisplacementData_set_reserved_at, handle, (long) index, value);
    }
    @Override public String toString() {
        return "PivotDisplacementData(" + "flags=" + getFlags() + ")";
    }

}
