// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.textures;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.textures.internal.Native;

/**
 * Describes a single mip level within a Texture's data buffer.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * MipLevel allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (MipLevel obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class MipLevel implements AutoCloseable {
    private static final long BYTES = 32L;

    final MemorySegment handle;
    final boolean owned;

    MipLevel(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public MipLevel() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_MipLevel_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("MipLevel allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_textures_MipLevel_delete, handle);
        }
    }

    /**
     * Width of this mip in pixels.
     * @return the width field of this MipLevel.
     */
    public int getWidth() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setWidth(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /**
     * Height of this mip in pixels.
     * @return the height field of this MipLevel.
     */
    public int getHeight() {
        return handle.get(ValueLayout.JAVA_INT, 4L);
    }
    public void setHeight(int value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value);
    }
    /**
     * Depth of this mip (always 1 for 2D / cube textures).
     * @return the depth field of this MipLevel.
     */
    public int getDepth() {
        return handle.get(ValueLayout.JAVA_INT, 8L);
    }
    public void setDepth(int value) {
        handle.set(ValueLayout.JAVA_INT, 8L, value);
    }
    /**
     * Byte offset into the Texture data buffer.
     * @return the offset field of this MipLevel.
     */
    public long getOffset() {
        return handle.get(ValueLayout.JAVA_LONG, 16L);
    }
    public void setOffset(long value) {
        handle.set(ValueLayout.JAVA_LONG, 16L, value);
    }
    /**
     * Byte size of this mip's data.
     * @return the size field of this MipLevel.
     */
    public long getSize() {
        return handle.get(ValueLayout.JAVA_LONG, 24L);
    }
    public void setSize(long value) {
        handle.set(ValueLayout.JAVA_LONG, 24L, value);
    }
    @Override public String toString() {
        return "MipLevel(" + "width=" + getWidth() + ", " + "height=" + getHeight() + ", " + "depth=" + getDepth() + ", " + "offset=" + getOffset() + ", " + "size=" + getSize() + ")";
    }

}
