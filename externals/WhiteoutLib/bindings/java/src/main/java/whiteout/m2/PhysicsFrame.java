// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * The affine frame the Domino chunks store: three basis columns and an origin, twelve floats in all.
 * 
 * The client reassembles it as `dmMtx{axisX, axisY, axisZ}` -> `dmQuatFromMtx` plus `origin` as the translation, giving a `dmTransform`.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * PhysicsFrame allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (PhysicsFrame obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class PhysicsFrame implements AutoCloseable {
    private static final long BYTES = 48L;

    final MemorySegment handle;
    final boolean owned;

    PhysicsFrame(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public PhysicsFrame() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsFrame_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("PhysicsFrame allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsFrame_delete, handle);
        }
    }

    /** @return the axisX field of this M2PhysicsFrame. */
    public Vector3f getAxisX() {
        return Handles.wrapVector3f(handle.asSlice(0L, 12L), false);
    }
    public void setAxisX(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsFrame_set_axisX, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 0L, 12L);
    }
    /** @return the axisY field of this M2PhysicsFrame. */
    public Vector3f getAxisY() {
        return Handles.wrapVector3f(handle.asSlice(12L, 12L), false);
    }
    public void setAxisY(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsFrame_set_axisY, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 12L, 12L);
    }
    /** @return the axisZ field of this M2PhysicsFrame. */
    public Vector3f getAxisZ() {
        return Handles.wrapVector3f(handle.asSlice(24L, 12L), false);
    }
    public void setAxisZ(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsFrame_set_axisZ, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 24L, 12L);
    }
    /** @return the origin field of this M2PhysicsFrame. */
    public Vector3f getOrigin() {
        return Handles.wrapVector3f(handle.asSlice(36L, 12L), false);
    }
    public void setOrigin(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsFrame_set_origin, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 36L, 12L);
    }
    @Override public String toString() {
        return "PhysicsFrame@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
