// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * One collision shape reference — SHAP/SHP2. Points at an entry of the box/capsule/sphere/polytope array named by @ref shapeType.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * PhysicsShape allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (PhysicsShape obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class PhysicsShape implements AutoCloseable {
    private static final long BYTES = 32L;

    final MemorySegment handle;
    final boolean owned;

    PhysicsShape(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public PhysicsShape() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsShape_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("PhysicsShape allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PhysicsShape_delete, handle);
        }
    }

    /** @return the shapeType field of this M2PhysicsShape. */
    public PhysicsShapeType getShapeType() {
        return PhysicsShapeType.fromInt(handle.get(ValueLayout.JAVA_INT, 0L));
    }
    public void setShapeType(PhysicsShapeType value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value.value);
    }
    /** @return the shapeIndex field of this M2PhysicsShape. */
    public short getShapeIndex() {
        return handle.get(ValueLayout.JAVA_SHORT, 2L);
    }
    public void setShapeIndex(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 2L, value);
    }
    /**
     * Zero in every corpus shape.
     * @return the padding04 field of this M2PhysicsShape.
     */
    public int getPadding04() {
        return handle.get(ValueLayout.JAVA_INT, 4L);
    }
    public void setPadding04(int value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value);
    }
    /** @return the friction field of this M2PhysicsShape. */
    public float getFriction() {
        return handle.get(ValueLayout.JAVA_FLOAT, 8L);
    }
    public void setFriction(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 8L, value);
    }
    /** @return the restitution field of this M2PhysicsShape. */
    public float getRestitution() {
        return handle.get(ValueLayout.JAVA_FLOAT, 12L);
    }
    public void setRestitution(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 12L, value);
    }
    /** @return the density field of this M2PhysicsShape. */
    public float getDensity() {
        return handle.get(ValueLayout.JAVA_FLOAT, 16L);
    }
    public void setDensity(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 16L, value);
    }
    /**
     * SHP2+. Unidentified, but a float: only 0, 0.01, 0.8 and 1.0 occur. The one `dmFixtureDef` float the rest of this struct does not account for is `m_rollingResistance`.
     * @return the unknown14 field of this M2PhysicsShape.
     */
    public float getUnknown14() {
        return handle.get(ValueLayout.JAVA_FLOAT, 20L);
    }
    public void setUnknown14(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 20L, value);
    }
    /**
     * SHP2+. 1.0 in 3229 of 3230 shapes, matching the `m_scaleOrRadius` the client hands every fixture.
     * @return the scale field of this M2PhysicsShape.
     */
    public float getScale() {
        return handle.get(ValueLayout.JAVA_FLOAT, 24L);
    }
    public void setScale(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 24L, value);
    }
    /**
     * SHP2+. Zero in every corpus shape.
     * @return the unknown1c field of this M2PhysicsShape.
     */
    public short getUnknown1c() {
        return handle.get(ValueLayout.JAVA_SHORT, 28L);
    }
    public void setUnknown1c(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 28L, value);
    }
    /**
     * SHP2+. Uninitialised on disk; kept so writes match.
     * @return the padding1e field of this M2PhysicsShape.
     */
    public short getPadding1e() {
        return handle.get(ValueLayout.JAVA_SHORT, 30L);
    }
    public void setPadding1e(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 30L, value);
    }
    @Override public String toString() {
        return "PhysicsShape(" + "shapeType=" + getShapeType() + ", " + "shapeIndex=" + getShapeIndex() + ", " + "padding04=" + getPadding04() + ", " + "friction=" + getFriction() + ", " + "restitution=" + getRestitution() + ", " + "density=" + getDensity() + ", " + "unknown14=" + getUnknown14() + ", " + "scale=" + getScale() + ", " + "unknown1c=" + getUnknown1c() + ", " + "padding1e=" + getPadding1e() + ")";
    }

}
