// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * LFSB — Sub-flare element (v0–v2, 56 bytes)
 * 
 * A single flare element within a LensFlare material, with position, size, scale, fade, color, and offset parameters.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * SubFlare allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (SubFlare obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class SubFlare implements AutoCloseable {
    private static final long BYTES = 60L;

    final MemorySegment handle;
    final boolean owned;

    SubFlare(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public SubFlare() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3SubFlare_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("SubFlare allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3SubFlare_delete, handle);
        }
    }

    /**
     * Flare element index
     * @return the index field of this M3SubFlare.
     */
    public int getIndex() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setIndex(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /**
     * Position along the flare axis (0–1)
     * @return the position field of this M3SubFlare.
     */
    public float getPosition() {
        return handle.get(ValueLayout.JAVA_FLOAT, 4L);
    }
    public void setPosition(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 4L, value);
    }
    /**
     * Base size (width, height)
     * @return the sizeXY field of this M3SubFlare.
     */
    public Vector2f getSizeXY() {
        return Handles.wrapVector2f(handle.asSlice(8L, 8L), false);
    }
    public void setSizeXY(Vector2f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3SubFlare_set_sizeXY, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 8L, 8L);
    }
    /**
     * Scale multiplier (width, height)
     * @return the scaleXY field of this M3SubFlare.
     */
    public Vector2f getScaleXY() {
        return Handles.wrapVector2f(handle.asSlice(16L, 8L), false);
    }
    public void setScaleXY(Vector2f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3SubFlare_set_scaleXY, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 16L, 8L);
    }
    /**
     * Fade-in range (start, end)
     * @return the fadeIn field of this M3SubFlare.
     */
    public Vector2f getFadeIn() {
        return Handles.wrapVector2f(handle.asSlice(24L, 8L), false);
    }
    public void setFadeIn(Vector2f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3SubFlare_set_fadeIn, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 24L, 8L);
    }
    /**
     * Fade-out range (start, end)
     * @return the fadeOut field of this M3SubFlare.
     */
    public Vector2f getFadeOut() {
        return Handles.wrapVector2f(handle.asSlice(32L, 8L), false);
    }
    public void setFadeOut(Vector2f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3SubFlare_set_fadeOut, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 32L, 8L);
    }
    /**
     * Flare color and alpha
     * @return the colorAlpha field of this M3SubFlare.
     */
    public ColorBGRA getColorAlpha() {
        return new ColorBGRA(handle.asSlice(40L, 4L), false);
    }
    public void setColorAlpha(ColorBGRA value) {
        NativeCommon.invokeNative(Native.whiteout_m3_M3SubFlare_set_colorAlpha, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    /**
     * Whether to face the flare center
     * @return the faceCenter field of this M3SubFlare.
     */
    public int getFaceCenter() {
        return handle.get(ValueLayout.JAVA_INT, 44L);
    }
    public void setFaceCenter(int value) {
        handle.set(ValueLayout.JAVA_INT, 44L, value);
    }
    /**
     * Offset from flare center
     * @return the offset field of this M3SubFlare.
     */
    public Vector2f getOffset() {
        return Handles.wrapVector2f(handle.asSlice(48L, 8L), false);
    }
    public void setOffset(Vector2f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3SubFlare_set_offset, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 48L, 8L);
    }
    @Override public String toString() {
        return "SubFlare(" + "index=" + getIndex() + ", " + "position=" + getPosition() + ", " + "faceCenter=" + getFaceCenter() + ")";
    }

}
