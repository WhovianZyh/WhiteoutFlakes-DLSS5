// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py â€” do not edit.
package whiteout.m3;

/**
 * Color channel selection (LAYR.colorType)
 */
public enum ColorChannelSelect {
    /** Use RGB channels (alpha forced to 1) */
    RGB(0),
    /** Use all RGBA channels */
    RGBA(1),
    /** Use alpha channel only (splat to all) */
    Alpha(2),
    /** Use red channel only (splat to all) */
    Red(3),
    /** Use green channel only (splat to all) */
    Green(4),
    /** Use blue channel only (splat to all) */
    Blue(5);

    public final int value;
    ColorChannelSelect(int v) { this.value = v; }
    public static ColorChannelSelect fromInt(int v) {
        for (var e : values()) if (e.value == v) return e;
        throw new IllegalArgumentException("unknown ColorChannelSelect: " + v);
    }
}
