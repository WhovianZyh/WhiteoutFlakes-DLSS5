// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * Java binding for the native Sequence type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Sequence allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (Sequence obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class Sequence implements AutoCloseable {
    private static final long BYTES = 64L;

    final MemorySegment handle;
    final boolean owned;

    Sequence(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public Sequence() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2Sequence_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("Sequence allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2Sequence_delete, handle);
        }
    }

    /** @return the id field of this M2Sequence. */
    public short getId() {
        return handle.get(ValueLayout.JAVA_SHORT, 0L);
    }
    public void setId(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 0L, value);
    }
    /** @return the variationIndex field of this M2Sequence. */
    public short getVariationIndex() {
        return handle.get(ValueLayout.JAVA_SHORT, 2L);
    }
    public void setVariationIndex(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 2L, value);
    }
    /** @return the duration field of this M2Sequence. */
    public int getDuration() {
        return handle.get(ValueLayout.JAVA_INT, 4L);
    }
    public void setDuration(int value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value);
    }
    /** @return the movespeed field of this M2Sequence. */
    public float getMovespeed() {
        return handle.get(ValueLayout.JAVA_FLOAT, 8L);
    }
    public void setMovespeed(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 8L, value);
    }
    /** @return the flags field of this M2Sequence. */
    public SequenceFlag getFlags() {
        return SequenceFlag.fromInt(handle.get(ValueLayout.JAVA_INT, 12L));
    }
    public void setFlags(SequenceFlag value) {
        handle.set(ValueLayout.JAVA_INT, 12L, value.value);
    }
    /** @return the frequency field of this M2Sequence. */
    public short getFrequency() {
        return handle.get(ValueLayout.JAVA_SHORT, 16L);
    }
    public void setFrequency(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 16L, value);
    }
    /** @return the padding field of this M2Sequence. */
    public short getPadding() {
        return handle.get(ValueLayout.JAVA_SHORT, 18L);
    }
    public void setPadding(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 18L, value);
    }
    /** @return the replayMin field of this M2Sequence. */
    public int getReplayMin() {
        return handle.get(ValueLayout.JAVA_INT, 20L);
    }
    public void setReplayMin(int value) {
        handle.set(ValueLayout.JAVA_INT, 20L, value);
    }
    /** @return the replayMax field of this M2Sequence. */
    public int getReplayMax() {
        return handle.get(ValueLayout.JAVA_INT, 24L);
    }
    public void setReplayMax(int value) {
        handle.set(ValueLayout.JAVA_INT, 24L, value);
    }
    /** @return the blendTimeIn field of this M2Sequence. */
    public short getBlendTimeIn() {
        return handle.get(ValueLayout.JAVA_SHORT, 28L);
    }
    public void setBlendTimeIn(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 28L, value);
    }
    /** @return the blendTimeOut field of this M2Sequence. */
    public short getBlendTimeOut() {
        return handle.get(ValueLayout.JAVA_SHORT, 30L);
    }
    public void setBlendTimeOut(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 30L, value);
    }
    /** @return the bounding field of this M2Sequence. */
    public Extent getBounding() {
        return new Extent(handle.asSlice(32L, 28L), false);
    }
    public void setBounding(Extent value) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2Sequence_set_bounding, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    /** @return the variationNext field of this M2Sequence. */
    public short getVariationNext() {
        return handle.get(ValueLayout.JAVA_SHORT, 60L);
    }
    public void setVariationNext(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 60L, value);
    }
    /** @return the aliasNext field of this M2Sequence. */
    public short getAliasNext() {
        return handle.get(ValueLayout.JAVA_SHORT, 62L);
    }
    public void setAliasNext(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 62L, value);
    }
    @Override public String toString() {
        return "Sequence(" + "id=" + getId() + ", " + "variationIndex=" + getVariationIndex() + ", " + "duration=" + getDuration() + ", " + "movespeed=" + getMovespeed() + ", " + "flags=" + getFlags() + ", " + "frequency=" + getFrequency() + ", " + "padding=" + getPadding() + ", " + "replayMin=" + getReplayMin() + ", " + "replayMax=" + getReplayMax() + ", " + "blendTimeIn=" + getBlendTimeIn() + ", " + "blendTimeOut=" + getBlendTimeOut() + ", " + "variationNext=" + getVariationNext() + ", " + "aliasNext=" + getAliasNext() + ")";
    }

}
