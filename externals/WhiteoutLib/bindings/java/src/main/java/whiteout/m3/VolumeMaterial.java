// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * VOL_ — Volume material (v0, 84 bytes)
 * 
 * Volumetric rendering material with density falloff, color map, and two noise maps for procedural volumetric effects.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * VolumeMaterial allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (VolumeMaterial obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class VolumeMaterial implements AutoCloseable {
    private static final long BYTES = 1592L;

    final MemorySegment handle;
    final boolean owned;

    VolumeMaterial(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public VolumeMaterial() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3VolumeMaterial_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("VolumeMaterial allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3VolumeMaterial_delete, handle);
        }
    }

    /**
     * Material name (Ref<CHAR>)
     * @return the name field of this M3VolumeMaterial.
     */
    public String getName() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3VolumeMaterial_get_name, arena, handle);
            MemorySegment __chars = __s.get(ValueLayout.ADDRESS, 0);
            long __len = __s.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__chars == null || __chars.equals(MemorySegment.NULL)) return "";
            byte[] __bytes = __chars.reinterpret(__len).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_CString_free, __s);
            return new String(__bytes, StandardCharsets.UTF_8);
        }
    }
    public void setName(String value) {
        try (Arena arena = Arena.ofConfined()) {
            byte[] __utf = value == null ? new byte[0] : value.getBytes(StandardCharsets.UTF_8);
            MemorySegment __seg = arena.allocate(__utf.length + 1);
            MemorySegment.copy(__utf, 0, __seg, ValueLayout.JAVA_BYTE, 0, __utf.length);
            __seg.set(ValueLayout.JAVA_BYTE, __utf.length, (byte) 0);
            NativeCommon.invokeNative(Native.whiteout_m3_M3VolumeMaterial_set_name, handle, __seg);
        }
    }
    /**
     * Blend mode
     * @return the blendMode field of this M3VolumeMaterial.
     */
    public int getBlendMode() {
        return handle.get(ValueLayout.JAVA_INT, 32L);
    }
    public void setBlendMode(int value) {
        handle.set(ValueLayout.JAVA_INT, 32L, value);
    }
    /**
     * Density falloff type
     * @return the falloffType field of this M3VolumeMaterial.
     */
    public VolumeFalloffType getFalloffType() {
        return VolumeFalloffType.fromInt(handle.get(ValueLayout.JAVA_INT, 36L));
    }
    public void setFalloffType(VolumeFalloffType value) {
        handle.set(ValueLayout.JAVA_INT, 36L, value.value);
    }
    /**
     * Animated density
     * @return the density field of this M3VolumeMaterial.
     */
    public AnimRefF32 getDensity() {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3VolumeMaterial_get_density, handle);
        return new AnimRefF32(__h, false);
    }
    public void setDensity(AnimRefF32 value) {
        NativeCommon.invokeNative(Native.whiteout_m3_M3VolumeMaterial_set_density, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    /**
     * Alpha test threshold
     * @return the alphaThreshold field of this M3VolumeMaterial.
     */
    public int getAlphaThreshold() {
        return handle.get(ValueLayout.JAVA_INT, 1576L);
    }
    public void setAlphaThreshold(int value) {
        handle.set(ValueLayout.JAVA_INT, 1576L, value);
    }
    @Override public String toString() {
        return "VolumeMaterial(" + "name=" + getName() + ", " + "blendMode=" + getBlendMode() + ", " + "falloffType=" + getFalloffType() + ", " + "alphaThreshold=" + getAlphaThreshold() + ")";
    }

}
