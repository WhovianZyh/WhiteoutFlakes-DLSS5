// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mdx;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mdx.internal.Native;

/**
 * Sub-texture definition (Reforged multi-texture)
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Layer::SubTexture allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (LayerSubTexture obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class LayerSubTexture implements AutoCloseable {
    private static final long BYTES = 80L;

    final MemorySegment handle;
    final boolean owned;

    LayerSubTexture(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public LayerSubTexture() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxLayerSubTexture_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("LayerSubTexture allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxLayerSubTexture_delete, handle);
        }
    }

    /**
     * Index into texture array
     * @return the textureId field of this MdxLayerSubTexture.
     */
    public int getTextureId() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setTextureId(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /**
     * Texture slot number
     * @return the slot field of this MdxLayerSubTexture.
     */
    public LayerSlotType getSlot() {
        return LayerSlotType.fromInt(handle.get(ValueLayout.JAVA_INT, 4L));
    }
    public void setSlot(LayerSlotType value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value.value);
    }
    /**
     * Texture ID animation
     * @return the tracks field of this MdxLayerSubTexture.
     */
    public TrackU32 getTracks() {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxLayerSubTexture_get_tracks, handle);
        return new TrackU32(__h, false);
    }
    public void setTracks(TrackU32 value) {
        NativeCommon.invokeNative(Native.whiteout_mdx_MdxLayerSubTexture_set_tracks, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    @Override public String toString() {
        return "LayerSubTexture(" + "textureId=" + getTextureId() + ", " + "slot=" + getSlot() + ")";
    }

}
