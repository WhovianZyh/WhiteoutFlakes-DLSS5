// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.host;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.host.internal.Native;

/**
 * VirtualPathFileSystem implementation backed by the OS filesystem.
 * 
 * All paths passed to readFile() / fileExists() are resolved relative to the root directory supplied at construction time.
 * 
 * Example: utils::OsFileSystem fs("C:/Games/Warcraft III/Data"); auto data = fs.readFile("units/human/arthas/arthas.mdx");
 * 
 * java_package=whiteout.utils
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * OsFileSystem allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (OsFileSystem obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class OsFileSystem implements AutoCloseable, whiteout.interfaces.VirtualPathFileSystem, whiteout.host.NativeHandled {
    private static final long BYTES = 16L;

    final MemorySegment handle;
    final boolean owned;

    OsFileSystem(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public static OsFileSystem createRootPath(String rootPath) {
        try (Arena __arena = Arena.ofConfined()) {
            MemorySegment __seg_rootPath = __arena.allocateFrom(rootPath, StandardCharsets.UTF_8);
            MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_host_OsFileSystem_new_rootPath, __seg_rootPath);
            if (__raw == null || __raw.equals(MemorySegment.NULL))
                throw new RuntimeException("OsFileSystem allocation failed");
            return new OsFileSystem(__raw, true);
        }
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_host_OsFileSystem_delete, handle);
        }
    }

    /**
     * Read a file at `rootPath / path`. Returns an empty vector if not found.
     *
     * @param path String input.
     * @return a fresh byte[] copied out of native memory.
     */
    public byte[] readFile(String path) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment path_seg = path == null
                ? MemorySegment.NULL
                : arena.allocateFrom(path, StandardCharsets.UTF_8);
            MemorySegment __struct = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_host_OsFileSystem_readFile, arena, handle, path_seg);
            MemorySegment __data = __struct.get(ValueLayout.ADDRESS, 0);
            long __size = __struct.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__data == null || __data.equals(MemorySegment.NULL)) return new byte[0];
            byte[] __out = __data.reinterpret(__size).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_Bytes_free, __struct);
            return __out;
        }
    }

    /**
     * Native method wrapping {@code OsFileSystem::writeFile}.
     * @param path String input.
     * @param data byte[] input.
     * @return boolean result.
     */
    public boolean writeFile(String path, byte[] data) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment path_seg = path == null
                ? MemorySegment.NULL
                : arena.allocateFrom(path, StandardCharsets.UTF_8);
            MemorySegment data_seg = data == null || data.length == 0
                ? MemorySegment.NULL
                : arena.allocate(data.length);
            if (data != null && data.length != 0) {
                MemorySegment.copy(data, 0, data_seg, ValueLayout.JAVA_BYTE, 0, data.length);
            }
            return ((int) NativeCommon.invokeNative(Native.whiteout_host_OsFileSystem_writeFile, handle, path_seg, data_seg, (long) (data == null ? 0 : data.length))) != 0;
        }
    }

    /**
     * Native method wrapping {@code OsFileSystem::fileExists}.
     * @param path String input.
     * @return boolean result.
     */
    public boolean fileExists(String path) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment path_seg = path == null
                ? MemorySegment.NULL
                : arena.allocateFrom(path, StandardCharsets.UTF_8);
            return ((int) NativeCommon.invokeNative(Native.whiteout_host_OsFileSystem_fileExists, handle, path_seg)) != 0;
        }
    }

    /** {@inheritDoc} */
    @Override
    public MemorySegment nativeHandle() { return handle; }

    @Override public String toString() {
        return "OsFileSystem@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
