// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * WELJ/WLJ2/WLJ3 — a soft rigid connection. Zero frequency means the axis is solved as a hard constraint.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * WeldJoint allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (WeldJoint obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class WeldJoint implements AutoCloseable {
    private static final long BYTES = 116L;

    final MemorySegment handle;
    final boolean owned;

    WeldJoint(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public WeldJoint() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2WeldJoint_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("WeldJoint allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2WeldJoint_delete, handle);
        }
    }

    /** @return the frameA field of this M2WeldJoint. */
    public PhysicsFrame getFrameA() {
        return new PhysicsFrame(handle.asSlice(0L, 48L), false);
    }
    public void setFrameA(PhysicsFrame value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2WeldJoint_set_frameA, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(value.handle, 0L, handle, 0L, 48L);
    }
    /** @return the frameB field of this M2WeldJoint. */
    public PhysicsFrame getFrameB() {
        return new PhysicsFrame(handle.asSlice(48L, 48L), false);
    }
    public void setFrameB(PhysicsFrame value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2WeldJoint_set_frameB, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(value.handle, 0L, handle, 48L, 48L);
    }
    /** @return the angularFrequencyHz field of this M2WeldJoint. */
    public float getAngularFrequencyHz() {
        return handle.get(ValueLayout.JAVA_FLOAT, 96L);
    }
    public void setAngularFrequencyHz(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 96L, value);
    }
    /** @return the angularDampingRatio field of this M2WeldJoint. */
    public float getAngularDampingRatio() {
        return handle.get(ValueLayout.JAVA_FLOAT, 100L);
    }
    public void setAngularDampingRatio(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 100L, value);
    }
    /**
     * WLJ2+
     * @return the linearFrequencyHz field of this M2WeldJoint.
     */
    public float getLinearFrequencyHz() {
        return handle.get(ValueLayout.JAVA_FLOAT, 104L);
    }
    public void setLinearFrequencyHz(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 104L, value);
    }
    /**
     * WLJ2+
     * @return the linearDampingRatio field of this M2WeldJoint.
     */
    public float getLinearDampingRatio() {
        return handle.get(ValueLayout.JAVA_FLOAT, 108L);
    }
    public void setLinearDampingRatio(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 108L, value);
    }
    /**
     * WLJ3+. Zero in 265 of 274 weld joints.
     * @return the unknown70 field of this M2WeldJoint.
     */
    public float getUnknown70() {
        return handle.get(ValueLayout.JAVA_FLOAT, 112L);
    }
    public void setUnknown70(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 112L, value);
    }
    @Override public String toString() {
        return "WeldJoint(" + "angularFrequencyHz=" + getAngularFrequencyHz() + ", " + "angularDampingRatio=" + getAngularDampingRatio() + ", " + "linearFrequencyHz=" + getLinearFrequencyHz() + ", " + "linearDampingRatio=" + getLinearDampingRatio() + ", " + "unknown70=" + getUnknown70() + ")";
    }

}
