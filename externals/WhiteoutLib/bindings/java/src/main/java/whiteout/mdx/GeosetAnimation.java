// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mdx;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mdx.internal.Native;

/**
 * Geoset visibility and color animation
 * 
 * Geoset animations control the visibility and color tinting of meshes.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * GeosetAnimation allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (GeosetAnimation obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class GeosetAnimation implements AutoCloseable {
    private static final long BYTES = 168L;

    final MemorySegment handle;
    final boolean owned;

    GeosetAnimation(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public GeosetAnimation() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxGeosetAnimation_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("GeosetAnimation allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxGeosetAnimation_delete, handle);
        }
    }

    /**
     * Base alpha value
     * @return the alpha field of this MdxGeosetAnimation.
     */
    public float getAlpha() {
        return handle.get(ValueLayout.JAVA_FLOAT, 0L);
    }
    public void setAlpha(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 0L, value);
    }
    /**
     * Animation flags
     * @return the flags field of this MdxGeosetAnimation.
     */
    public GeosetAnimationFlag getFlags() {
        return GeosetAnimationFlag.fromInt(handle.get(ValueLayout.JAVA_INT, 4L));
    }
    public void setFlags(GeosetAnimationFlag value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value.value);
    }
    /**
     * Base color tint
     * @return the color field of this MdxGeosetAnimation.
     */
    public Vector3f getColor() {
        return Handles.wrapVector3f(handle.asSlice(8L, 12L), false);
    }
    public void setColor(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxGeosetAnimation_set_color, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 8L, 12L);
    }
    /**
     * Target geoset index
     * @return the geosetId field of this MdxGeosetAnimation.
     */
    public int getGeosetId() {
        return handle.get(ValueLayout.JAVA_INT, 20L);
    }
    public void setGeosetId(int value) {
        handle.set(ValueLayout.JAVA_INT, 20L, value);
    }
    /**
     * Alpha animation
     * @return the alphaTracks field of this MdxGeosetAnimation.
     */
    public TrackF32 getAlphaTracks() {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxGeosetAnimation_get_alphaTracks, handle);
        return new TrackF32(__h, false);
    }
    public void setAlphaTracks(TrackF32 value) {
        NativeCommon.invokeNative(Native.whiteout_mdx_MdxGeosetAnimation_set_alphaTracks, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    /**
     * Color animation
     * @return the colorTracks field of this MdxGeosetAnimation.
     */
    public TrackVector3f getColorTracks() {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxGeosetAnimation_get_colorTracks, handle);
        return new TrackVector3f(__h, false);
    }
    public void setColorTracks(TrackVector3f value) {
        NativeCommon.invokeNative(Native.whiteout_mdx_MdxGeosetAnimation_set_colorTracks, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    @Override public String toString() {
        return "GeosetAnimation(" + "alpha=" + getAlpha() + ", " + "flags=" + getFlags() + ", " + "geosetId=" + getGeosetId() + ")";
    }

}
