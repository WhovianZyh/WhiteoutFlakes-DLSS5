// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mpq;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mpq.internal.Native;

/**
 * Summary information about the archive.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * ArchiveInfo allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (ArchiveInfo obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class ArchiveInfo implements AutoCloseable {
    private static final long BYTES = 24L;

    final MemorySegment handle;
    final boolean owned;

    ArchiveInfo(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public ArchiveInfo() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mpq_MpqArchiveInfo_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("ArchiveInfo allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mpq_MpqArchiveInfo_delete, handle);
        }
    }

    /**
     * 0 = V1, 1 = V2.
     * @return the formatVersion field of this MpqArchiveInfo.
     */
    public short getFormatVersion() {
        return handle.get(ValueLayout.JAVA_SHORT, 0L);
    }
    public void setFormatVersion(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 0L, value);
    }
    /**
     * Hash table capacity (always a power of 2).
     * @return the hashTableEntries field of this MpqArchiveInfo.
     */
    public int getHashTableEntries() {
        return handle.get(ValueLayout.JAVA_INT, 4L);
    }
    public void setHashTableEntries(int value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value);
    }
    /**
     * Number of occupied block table entries.
     * @return the blockTableEntries field of this MpqArchiveInfo.
     */
    public int getBlockTableEntries() {
        return handle.get(ValueLayout.JAVA_INT, 8L);
    }
    public void setBlockTableEntries(int value) {
        handle.set(ValueLayout.JAVA_INT, 8L, value);
    }
    /**
     * Sector size in bytes (512 << sectorSizeShift).
     * @return the sectorSize field of this MpqArchiveInfo.
     */
    public int getSectorSize() {
        return handle.get(ValueLayout.JAVA_INT, 12L);
    }
    public void setSectorSize(int value) {
        handle.set(ValueLayout.JAVA_INT, 12L, value);
    }
    /**
     * Total archive size in bytes.
     * @return the archiveSize field of this MpqArchiveInfo.
     */
    public long getArchiveSize() {
        return handle.get(ValueLayout.JAVA_LONG, 16L);
    }
    public void setArchiveSize(long value) {
        handle.set(ValueLayout.JAVA_LONG, 16L, value);
    }
    @Override public String toString() {
        return "ArchiveInfo(" + "formatVersion=" + getFormatVersion() + ", " + "hashTableEntries=" + getHashTableEntries() + ", " + "blockTableEntries=" + getBlockTableEntries() + ", " + "sectorSize=" + getSectorSize() + ", " + "archiveSize=" + getArchiveSize() + ")";
    }

}
