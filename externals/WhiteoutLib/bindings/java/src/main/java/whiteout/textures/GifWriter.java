// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.textures;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.textures.internal.Native;

/**
 * Encodes a sequence of Texture frames into GIF89a format.
 * 
 * Unlike the single-image writers (BMP, TGA, …), this writer accepts a vector of frames.  It does **not** inherit from `textures::Writer`.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Writer allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (GifWriter obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class GifWriter implements AutoCloseable {
    private static final long BYTES = 8L;

    final MemorySegment handle;
    final boolean owned;

    GifWriter(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public GifWriter() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_GifWriter_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("GifWriter allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    public static GifWriter createPool(whiteout.interfaces.WorkerPool pool) {
        long __pool_h = pool == null ? 0L
            : whiteout.host.WorkerPools.resolveNative(pool, pool);
        MemorySegment __pool_seg = __pool_h == 0L
            ? MemorySegment.NULL : MemorySegment.ofAddress(__pool_h);
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_GifWriter_new_pool, __pool_seg);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("GifWriter allocation failed");
        return new GifWriter(__raw, true);
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_textures_GifWriter_delete, handle);
        }
    }

    /**
     * Write frames to a GIF file on disk using default options.
     *
     * @param filePath String input.
     * @param frames Texture[] input.
     */
    public void write(String filePath, Texture[] frames) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment filePath_seg = filePath == null
                ? MemorySegment.NULL
                : arena.allocateFrom(filePath, StandardCharsets.UTF_8);
            MemorySegment frames_seg = frames == null || frames.length == 0
                ? MemorySegment.NULL
                : arena.allocate(ValueLayout.ADDRESS, frames.length);
            if (frames != null) {
                for (int __i = 0; __i < frames.length; ++__i) {
                    frames_seg.setAtIndex(ValueLayout.ADDRESS, __i,
                        frames[__i] == null ? MemorySegment.NULL : frames[__i].handle);
                }
            }
            NativeCommon.invokeNative(Native.whiteout_textures_GifWriter_write, handle, filePath_seg, frames_seg, (long) (frames == null ? 0 : frames.length));
        }
    }

    /**
     * Write frames to a GIF byte buffer using default options.
     *
     * @param frames Texture[] input.
     * @return a fresh byte[] copied out of native memory.
     */
    public byte[] write(Texture[] frames) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment frames_seg = frames == null || frames.length == 0
                ? MemorySegment.NULL
                : arena.allocate(ValueLayout.ADDRESS, frames.length);
            if (frames != null) {
                for (int __i = 0; __i < frames.length; ++__i) {
                    frames_seg.setAtIndex(ValueLayout.ADDRESS, __i,
                        frames[__i] == null ? MemorySegment.NULL : frames[__i].handle);
                }
            }
            MemorySegment __struct = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_GifWriter_write_frames, arena, handle, frames_seg, (long) (frames == null ? 0 : frames.length));
            MemorySegment __data = __struct.get(ValueLayout.ADDRESS, 0);
            long __size = __struct.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__data == null || __data.equals(MemorySegment.NULL)) return new byte[0];
            byte[] __out = __data.reinterpret(__size).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_Bytes_free, __struct);
            return __out;
        }
    }

    /**
     * Write frames to a GIF file on disk with explicit options.
     *
     * @param filePath String input.
     * @param frames Texture[] input.
     * @param opts GifSaveOptions input.
     */
    public void write(String filePath, Texture[] frames, GifSaveOptions opts) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment filePath_seg = filePath == null
                ? MemorySegment.NULL
                : arena.allocateFrom(filePath, StandardCharsets.UTF_8);
            MemorySegment frames_seg = frames == null || frames.length == 0
                ? MemorySegment.NULL
                : arena.allocate(ValueLayout.ADDRESS, frames.length);
            if (frames != null) {
                for (int __i = 0; __i < frames.length; ++__i) {
                    frames_seg.setAtIndex(ValueLayout.ADDRESS, __i,
                        frames[__i] == null ? MemorySegment.NULL : frames[__i].handle);
                }
            }
            NativeCommon.invokeNative(Native.whiteout_textures_GifWriter_write_filePath_frames_opts, handle, filePath_seg, frames_seg, (long) (frames == null ? 0 : frames.length), opts == null ? MemorySegment.NULL : opts.handle);
        }
    }

    /**
     * Write frames to a GIF byte buffer with explicit options.
     *
     * @param frames Texture[] input.
     * @param opts GifSaveOptions input.
     * @return a fresh byte[] copied out of native memory.
     */
    public byte[] write(Texture[] frames, GifSaveOptions opts) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment frames_seg = frames == null || frames.length == 0
                ? MemorySegment.NULL
                : arena.allocate(ValueLayout.ADDRESS, frames.length);
            if (frames != null) {
                for (int __i = 0; __i < frames.length; ++__i) {
                    frames_seg.setAtIndex(ValueLayout.ADDRESS, __i,
                        frames[__i] == null ? MemorySegment.NULL : frames[__i].handle);
                }
            }
            MemorySegment __struct = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_GifWriter_write_frames_opts, arena, handle, frames_seg, (long) (frames == null ? 0 : frames.length), opts == null ? MemorySegment.NULL : opts.handle);
            MemorySegment __data = __struct.get(ValueLayout.ADDRESS, 0);
            long __size = __struct.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__data == null || __data.equals(MemorySegment.NULL)) return new byte[0];
            byte[] __out = __data.reinterpret(__size).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_Bytes_free, __struct);
            return __out;
        }
    }

    /**
     * @return true if the last write produced any issues.
     */
    public boolean hasIssues() {
        return ((int) NativeCommon.invokeNative(Native.whiteout_textures_GifWriter_hasIssues, handle)) != 0;
    }

    /**
     * @return accumulated issues from the last write call.
     * @return the strings, materialised in one native call.
     */
    public java.util.List<String> getIssues() {
        try (Arena arena = Arena.ofConfined()) {
            long __n = (long) NativeCommon.invokeNative(Native.whiteout_textures_GifWriter_getIssues_count, handle);
            var __out = new java.util.ArrayList<String>((int) __n);
            for (long __i = 0; __i < __n; __i++) {
                MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_GifWriter_getIssues_at, arena, handle, __i);
                __out.add(NativeCommon.takeString(__s));
            }
            return __out;
        }
    }

    @Override public String toString() {
        return "GifWriter@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
