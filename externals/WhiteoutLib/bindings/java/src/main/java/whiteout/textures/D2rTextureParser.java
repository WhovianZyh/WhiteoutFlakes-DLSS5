// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.textures;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.textures.internal.Native;

/**
 * Reads a Diablo II: Resurrected `.texture` file and decodes it into a Texture.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Parser allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (D2rTextureParser obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class D2rTextureParser implements AutoCloseable {
    private static final long BYTES = 16L;

    final MemorySegment handle;
    final boolean owned;

    D2rTextureParser(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public D2rTextureParser() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_D2rTextureParser_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("D2rTextureParser allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_textures_D2rTextureParser_delete, handle);
        }
    }

    /**
     * Parse a `.texture` byte buffer.
     *
     * @param buffer byte[] input.
     * @return a fresh java.util.Optional<Texture> owning a native allocation.
     */
    public java.util.Optional<Texture> parse(byte[] buffer) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment buffer_seg = arena.allocate(buffer.length * 1L);
            MemorySegment.copy(buffer, 0, buffer_seg, ValueLayout.JAVA_BYTE, 0, buffer.length);
            MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_D2rTextureParser_parse, handle, buffer_seg, (long) buffer.length);
            if (__h == null || __h.equals(MemorySegment.NULL)) return java.util.Optional.empty();
            return java.util.Optional.of(new Texture(__h, true));
        }
    }

    /**
     * @return true if @p buffer has a valid `.texture` header and known format.
     *
     * @param buffer byte[] input.
     */
    public boolean detect(byte[] buffer) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment buffer_seg = arena.allocate(buffer.length * 1L);
            MemorySegment.copy(buffer, 0, buffer_seg, ValueLayout.JAVA_BYTE, 0, buffer.length);
            return ((int) NativeCommon.invokeNative(Native.whiteout_textures_D2rTextureParser_detect, handle, buffer_seg, (long) buffer.length)) != 0;
        }
    }

    /**
     * @return true if the last parse produced any issues.
     */
    public boolean hasIssues() {
        return ((int) NativeCommon.invokeNative(Native.whiteout_textures_D2rTextureParser_hasIssues, handle)) != 0;
    }

    /**
     * @return accumulated issues from the last parse call.
     * @return the strings, materialised in one native call.
     */
    public java.util.List<String> getIssues() {
        try (Arena arena = Arena.ofConfined()) {
            long __n = (long) NativeCommon.invokeNative(Native.whiteout_textures_D2rTextureParser_getIssues_count, handle);
            var __out = new java.util.ArrayList<String>((int) __n);
            for (long __i = 0; __i < __n; __i++) {
                MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_D2rTextureParser_getIssues_at, arena, handle, __i);
                __out.add(NativeCommon.takeString(__s));
            }
            return __out;
        }
    }

    @Override public String toString() {
        return "D2rTextureParser@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
