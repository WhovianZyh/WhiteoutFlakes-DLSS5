// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py â€” do not edit.
package whiteout.m3;

/**
 * Identifies which material array a MATM entry references
 */
public enum MaterialType {
    /** MAT_ — Standard material */
    Standard(1),
    /** DIS_ — Displacement material */
    Displacement(2),
    /** CMP_ — Composite material */
    Composite(3),
    /** TER_ — Terrain material */
    Terrain(4),
    /** VOL_ — Volume material */
    Volume(5),
    /** VON_ — Volume noise material */
    VolumeNoise(6),
    /** CREP — Creep material */
    Creep(7),
    /** HAI_ — Hair material (defunct) */
    Hair(8),
    /** STBM — Splat terrain bake material */
    SplatTerrainBake(9),
    /** REF_ — Reflection material */
    Reflection(10),
    /** LFLR — Lens flare material */
    LensFlare(11),
    /** MADD — Data-driven material; what every other type is converted into at load */
    DataDriven(12);

    public final int value;
    MaterialType(int v) { this.value = v; }
    public static MaterialType fromInt(int v) {
        for (var e : values()) if (e.value == v) return e;
        throw new IllegalArgumentException("unknown MaterialType: " + v);
    }
}
