// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mpq;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mpq.internal.Native;

/**
 * Options for creating a new archive.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * CreateOptions allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (CreateOptions obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class CreateOptions implements AutoCloseable {
    private static final long BYTES = 12L;

    final MemorySegment handle;
    final boolean owned;

    CreateOptions(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public CreateOptions() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mpq_MpqCreateOptions_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("CreateOptions allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mpq_MpqCreateOptions_delete, handle);
        }
    }

    /**
     * Archive format version (V1 or V2).
     * @return the version field of this MpqCreateOptions.
     */
    public FormatVersion getVersion() {
        return FormatVersion.fromInt(handle.get(ValueLayout.JAVA_INT, 0L));
    }
    public void setVersion(FormatVersion value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value.value);
    }
    /**
     * Initial hash table capacity; rounded up to the next power of 2.
     * @return the hashTableSize field of this MpqCreateOptions.
     */
    public int getHashTableSize() {
        return handle.get(ValueLayout.JAVA_INT, 4L);
    }
    public void setHashTableSize(int value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value);
    }
    /**
     * Sector size = 512 << shift (default 3 → 4096 bytes).
     * @return the sectorSizeShift field of this MpqCreateOptions.
     */
    public short getSectorSizeShift() {
        return handle.get(ValueLayout.JAVA_SHORT, 8L);
    }
    public void setSectorSizeShift(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 8L, value);
    }
    @Override public String toString() {
        return "CreateOptions(" + "version=" + getVersion() + ", " + "hashTableSize=" + getHashTableSize() + ", " + "sectorSizeShift=" + getSectorSizeShift() + ")";
    }

}
