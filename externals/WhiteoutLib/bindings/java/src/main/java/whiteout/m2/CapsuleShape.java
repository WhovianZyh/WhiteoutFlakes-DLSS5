// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * CAPS — a capsule between two local points.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * CapsuleShape allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (CapsuleShape obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class CapsuleShape implements AutoCloseable {
    private static final long BYTES = 28L;

    final MemorySegment handle;
    final boolean owned;

    CapsuleShape(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public CapsuleShape() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2CapsuleShape_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("CapsuleShape allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2CapsuleShape_delete, handle);
        }
    }

    /** @return the localPosition1 field of this M2CapsuleShape. */
    public Vector3f getLocalPosition1() {
        return Handles.wrapVector3f(handle.asSlice(0L, 12L), false);
    }
    public void setLocalPosition1(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2CapsuleShape_set_localPosition1, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 0L, 12L);
    }
    /** @return the localPosition2 field of this M2CapsuleShape. */
    public Vector3f getLocalPosition2() {
        return Handles.wrapVector3f(handle.asSlice(12L, 12L), false);
    }
    public void setLocalPosition2(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2CapsuleShape_set_localPosition2, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 12L, 12L);
    }
    /** @return the radius field of this M2CapsuleShape. */
    public float getRadius() {
        return handle.get(ValueLayout.JAVA_FLOAT, 24L);
    }
    public void setRadius(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 24L, value);
    }
    @Override public String toString() {
        return "CapsuleShape(" + "radius=" + getRadius() + ")";
    }

}
