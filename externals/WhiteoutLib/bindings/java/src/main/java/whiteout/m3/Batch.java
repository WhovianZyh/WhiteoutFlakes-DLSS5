// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * BAT_ — Batch / draw call (v0–v1, 14 bytes)
 * 
 * Associates a Region with a material for rendering. Multiple batches may reference the same region with different materials.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Batch allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (Batch obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class Batch implements AutoCloseable {
    private static final long BYTES = 20L;

    final MemorySegment handle;
    final boolean owned;

    Batch(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public Batch() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3Batch_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("Batch allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3Batch_delete, handle);
        }
    }

    /**
     * Unknown field
     * @return the unknown field of this M3Batch.
     */
    public int getUnknown() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setUnknown(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /**
     * Index into REGN array
     * @return the regionIndex field of this M3Batch.
     */
    public short getRegionIndex() {
        return handle.get(ValueLayout.JAVA_SHORT, 4L);
    }
    public void setRegionIndex(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 4L, value);
    }
    /**
     * Unknown field
     * @return the unknown2 field of this M3Batch.
     */
    public int getUnknown2() {
        return handle.get(ValueLayout.JAVA_INT, 8L);
    }
    public void setUnknown2(int value) {
        handle.set(ValueLayout.JAVA_INT, 8L, value);
    }
    /**
     * Index into MATM material map array
     * @return the materialIndex field of this M3Batch.
     */
    public short getMaterialIndex() {
        return handle.get(ValueLayout.JAVA_SHORT, 12L);
    }
    public void setMaterialIndex(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 12L, value);
    }
    /**
     * Bone whose animated visibility gates this batch's draw (0xFFFF = always drawn). Misnamed — it is a bone index, not a count: the engine's submit loop reads it and skips the batch when that bone is invisible.
     * @return the boneCount field of this M3Batch.
     */
    public short getBoneCount() {
        return handle.get(ValueLayout.JAVA_SHORT, 14L);
    }
    public void setBoneCount(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 14L, value);
    }
    @Override public String toString() {
        return "Batch(" + "unknown=" + getUnknown() + ", " + "regionIndex=" + getRegionIndex() + ", " + "unknown2=" + getUnknown2() + ", " + "materialIndex=" + getMaterialIndex() + ", " + "boneCount=" + getBoneCount() + ")";
    }

}
