// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.utils;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.host.internal.Native;

/**
 * Basic HTTP handler with two platform backends: * Windows  — WinHTTP * Other    — libcurl (linked at build time)
 * 
 * Requests are dispatched asynchronously onto an internal thread pool; each worker owns one backend handle and keeps its connection cache warm. Thread-safe: multiple threads may call getAsync / getRangeAsync concurrently.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * SimpleHttpHandler allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (SimpleHttpHandler obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class SimpleHttpHandler implements AutoCloseable, whiteout.interfaces.HttpHandler, whiteout.host.NativeHandled {
    private static final long BYTES = 16L;

    final MemorySegment handle;
    final boolean owned;

    SimpleHttpHandler(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public SimpleHttpHandler() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_host_SimpleHttpHandler_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("SimpleHttpHandler allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    public static SimpleHttpHandler createNThreads(long nThreads) {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_host_SimpleHttpHandler_new_nThreads, nThreads);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("SimpleHttpHandler allocation failed");
        return new SimpleHttpHandler(__raw, true);
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_host_SimpleHttpHandler_delete, handle);
        }
    }

    /**
     * Reported handler capability flags.
     * @return int result.
     */
    public int capabilities() {
        return (int) NativeCommon.invokeNative(Native.whiteout_host_SimpleHttpHandler_capabilities, handle);
    }

    /** {@inheritDoc} */
    @Override
    public MemorySegment nativeHandle() { return handle; }

    @Override
    public void getAsync(String url, java.util.function.Consumer<whiteout.interfaces.HttpResponse> callback) throws Exception {
        _getAsync(handle.address(), url, callback);
    }
    private static native void _getAsync(
        long handlerHandle, String url,
        java.util.function.Consumer<whiteout.interfaces.HttpResponse> callback);

    @Override
    public void getRangeAsync(String url, long start, long end, java.util.function.Consumer<whiteout.interfaces.HttpResponse> callback) throws Exception {
        _getRangeAsync(handle.address(), url, start, end, callback);
    }
    private static native void _getRangeAsync(
        long handlerHandle, String url, long start, long end,
        java.util.function.Consumer<whiteout.interfaces.HttpResponse> callback);

    @Override public String toString() {
        return "SimpleHttpHandler@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
