// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.host;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.host.internal.Native;

/**
 * Abstract HTTP handler.  The library calls these methods to fetch CDN data. Users must provide a concrete implementation.
 * 
 * Both methods are pure virtual — async support is mandatory. The handler invokes @p callback exactly once when the request completes (or fails). The callback may be invoked from any thread.
 * 
 * Thread safety: methods may be called concurrently from multiple WorkerPool threads.  The implementation must be thread-safe.
 * 
 * java_package=whiteout.utils — abstract opaque base. Concrete impl: utils::SimpleHttpHandler.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * HttpHandler allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (HttpHandler obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class HttpHandler implements AutoCloseable, whiteout.interfaces.HttpHandler, whiteout.host.NativeHandled {
    private static final long BYTES = 8L;

    final MemorySegment handle;
    final boolean owned;

    HttpHandler(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_host_HttpHandler_delete, handle);
        }
    }

    /**
     * Reported handler capability flags.
     * @return int result.
     */
    public int capabilities() {
        return (int) NativeCommon.invokeNative(Native.whiteout_host_HttpHandler_capabilities, handle);
    }

    /** {@inheritDoc} */
    @Override
    public MemorySegment nativeHandle() { return handle; }

    @Override
    public void getAsync(String url, java.util.function.Consumer<whiteout.interfaces.HttpResponse> callback) throws Exception {
        _getAsync(handle.address(), url, callback);
    }
    private static native void _getAsync(
        long handlerHandle, String url,
        java.util.function.Consumer<whiteout.interfaces.HttpResponse> callback);

    @Override
    public void getRangeAsync(String url, long start, long end, java.util.function.Consumer<whiteout.interfaces.HttpResponse> callback) throws Exception {
        _getRangeAsync(handle.address(), url, start, end, callback);
    }
    private static native void _getRangeAsync(
        long handlerHandle, String url, long start, long end,
        java.util.function.Consumer<whiteout.interfaces.HttpResponse> callback);

    @Override public String toString() {
        return "HttpHandler@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
