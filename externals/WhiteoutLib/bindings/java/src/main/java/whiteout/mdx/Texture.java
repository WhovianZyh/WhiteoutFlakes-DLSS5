// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mdx;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mdx.internal.Native;

/**
 * Texture definition
 * 
 * Defines a texture file path and its properties.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Texture allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (Texture obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class Texture implements AutoCloseable {
    private static final long BYTES = 48L;

    final MemorySegment handle;
    final boolean owned;

    Texture(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public Texture() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxTexture_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("Texture allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxTexture_delete, handle);
        }
    }

    /** @return the replaceableId field of this MdxTexture. */
    public int getReplaceableId() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setReplaceableId(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /**
     * Path to texture file (BLP, DDS or TGA)
     * @return the fileName field of this MdxTexture.
     */
    public String getFileName() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxTexture_get_fileName, arena, handle);
            MemorySegment __chars = __s.get(ValueLayout.ADDRESS, 0);
            long __len = __s.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__chars == null || __chars.equals(MemorySegment.NULL)) return "";
            byte[] __bytes = __chars.reinterpret(__len).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_CString_free, __s);
            return new String(__bytes, StandardCharsets.UTF_8);
        }
    }
    public void setFileName(String value) {
        try (Arena arena = Arena.ofConfined()) {
            byte[] __utf = value == null ? new byte[0] : value.getBytes(StandardCharsets.UTF_8);
            MemorySegment __seg = arena.allocate(__utf.length + 1);
            MemorySegment.copy(__utf, 0, __seg, ValueLayout.JAVA_BYTE, 0, __utf.length);
            __seg.set(ValueLayout.JAVA_BYTE, __utf.length, (byte) 0);
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxTexture_set_fileName, handle, __seg);
        }
    }
    /**
     * Texture wrap flags
     * @return the flags field of this MdxTexture.
     */
    public TextureFlag getFlags() {
        return TextureFlag.fromInt(handle.get(ValueLayout.JAVA_INT, 40L));
    }
    public void setFlags(TextureFlag value) {
        handle.set(ValueLayout.JAVA_INT, 40L, value.value);
    }
    @Override public String toString() {
        return "Texture(" + "replaceableId=" + getReplaceableId() + ", " + "fileName=" + getFileName() + ", " + "flags=" + getFlags() + ")";
    }

}
