// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * One half-edge of a polytope — Domino's `dmSubEdge`, four bytes.
 * 
 * Half-edges are stored in twin pairs at adjacent indices, and the ones bounding a face form a cycle through @ref nextEdge.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * PolytopeHalfEdge allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (PolytopeHalfEdge obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class PolytopeHalfEdge implements AutoCloseable {
    private static final long BYTES = 4L;

    final MemorySegment handle;
    final boolean owned;

    PolytopeHalfEdge(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public PolytopeHalfEdge() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeHalfEdge_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("PolytopeHalfEdge allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeHalfEdge_delete, handle);
        }
    }

    /**
     * Signed step to the paired half-edge: the twin of edge `i` is `i + twinOffset`. Only +1 and -1 occur, in equal numbers.
     * @return the twinOffset field of this M2PolytopeHalfEdge.
     */
    public byte getTwinOffset() {
        return handle.get(ValueLayout.JAVA_BYTE, 0L);
    }
    public void setTwinOffset(byte value) {
        handle.set(ValueLayout.JAVA_BYTE, 0L, value);
    }
    /**
     * Where this half-edge starts, indexing PolytopeShape::vertices.
     * @return the originVertex field of this M2PolytopeHalfEdge.
     */
    public byte getOriginVertex() {
        return handle.get(ValueLayout.JAVA_BYTE, 1L);
    }
    public void setOriginVertex(byte value) {
        handle.set(ValueLayout.JAVA_BYTE, 1L, value);
    }
    /**
     * The face this half-edge bounds, indexing PolytopeShape::facePlanes.
     * @return the faceIndex field of this M2PolytopeHalfEdge.
     */
    public byte getFaceIndex() {
        return handle.get(ValueLayout.JAVA_BYTE, 2L);
    }
    public void setFaceIndex(byte value) {
        handle.set(ValueLayout.JAVA_BYTE, 2L, value);
    }
    /**
     * Next half-edge around @ref faceIndex.
     * @return the nextEdge field of this M2PolytopeHalfEdge.
     */
    public byte getNextEdge() {
        return handle.get(ValueLayout.JAVA_BYTE, 3L);
    }
    public void setNextEdge(byte value) {
        handle.set(ValueLayout.JAVA_BYTE, 3L, value);
    }
    @Override public String toString() {
        return "PolytopeHalfEdge(" + "twinOffset=" + getTwinOffset() + ", " + "originVertex=" + getOriginVertex() + ", " + "faceIndex=" + getFaceIndex() + ", " + "nextEdge=" + getNextEdge() + ")";
    }

}
