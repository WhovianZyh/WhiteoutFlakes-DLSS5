// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * A whole `.bone` file: the skeleton edits one customization choice needs.
 * 
 * On disk this is two parallel chunks — `BIDA` holds the bone ids and `BOMT` the matrices — but their lengths match in every corpus file, so they are paired here and split again on write.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * BoneOverrideSet allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (BoneOverrideSet obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class BoneOverrideSet implements AutoCloseable {
    private static final long BYTES = 32L;

    final MemorySegment handle;
    final boolean owned;

    BoneOverrideSet(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public BoneOverrideSet() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2BoneOverrideSet_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("BoneOverrideSet allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2BoneOverrideSet_delete, handle);
        }
    }

    /**
     * 1 in every known file.
     * @return the version field of this M2BoneOverrideSet.
     */
    public int getVersion() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setVersion(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /**
     * Ascending by @ref BoneOverride::boneIndex, which is the order the files store and what lets the client binary-search a bone.
     * @return the overrides field of this M2BoneOverrideSet.
     */
    public int getOverridesCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m2_M2BoneOverrideSet_get_overrides_count, handle);
    }
    public BoneOverride getOverridesAt(int index) {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2BoneOverrideSet_get_overrides_at, handle, (long) index);
        return new BoneOverride(__h, false);
    }
    public void resizeOverrides(int count) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2BoneOverrideSet_resize_overrides, handle, (long) count);
    }
    public java.util.List<BoneOverride> overridesView() {
        return new java.util.AbstractList<BoneOverride>() {
            @Override public int size() { return getOverridesCount(); }
            @Override public BoneOverride get(int index) { return getOverridesAt(index); }
        };
    }
    @Override public String toString() {
        return "BoneOverrideSet(" + "version=" + getVersion() + ")";
    }

}
