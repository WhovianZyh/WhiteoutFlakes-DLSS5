// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * PARC — Particle emitter copy (v0, 40 bytes)
 * 
 * Lightweight copy of a particle emitter with overridden emission rate, squirt amount, and bone index. References the original PAR_ via Model.copyIndices.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * ParticleEmitterCopy allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (ParticleEmitterCopy obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class ParticleEmitterCopy implements AutoCloseable {
    private static final long BYTES = 44L;

    final MemorySegment handle;
    final boolean owned;

    ParticleEmitterCopy(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public ParticleEmitterCopy() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3ParticleEmitterCopy_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("ParticleEmitterCopy allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3ParticleEmitterCopy_delete, handle);
        }
    }

    /**
     * Overridden emission rate
     * @return the emissionRate field of this M3ParticleEmitterCopy.
     */
    public AnimRefF32 getEmissionRate() {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3ParticleEmitterCopy_get_emissionRate, handle);
        return new AnimRefF32(__h, false);
    }
    public void setEmissionRate(AnimRefF32 value) {
        NativeCommon.invokeNative(Native.whiteout_m3_M3ParticleEmitterCopy_set_emissionRate, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    /**
     * Overridden squirt burst count
     * @return the squirtAmount field of this M3ParticleEmitterCopy.
     */
    public AnimRefU16 getSquirtAmount() {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3ParticleEmitterCopy_get_squirtAmount, handle);
        return new AnimRefU16(__h, false);
    }
    public void setSquirtAmount(AnimRefU16 value) {
        NativeCommon.invokeNative(Native.whiteout_m3_M3ParticleEmitterCopy_set_squirtAmount, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    /**
     * Index into BONE array
     * @return the boneIndex field of this M3ParticleEmitterCopy.
     */
    public int getBoneIndex() {
        return handle.get(ValueLayout.JAVA_INT, 36L);
    }
    public void setBoneIndex(int value) {
        handle.set(ValueLayout.JAVA_INT, 36L, value);
    }
    @Override public String toString() {
        return "ParticleEmitterCopy(" + "boneIndex=" + getBoneIndex() + ")";
    }

}
