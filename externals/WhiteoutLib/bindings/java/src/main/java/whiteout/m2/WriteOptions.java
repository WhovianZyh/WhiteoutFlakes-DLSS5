// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * Java binding for the native WriteOptions type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * WriteOptions allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (WriteOptions obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class WriteOptions implements AutoCloseable {
    private static final long BYTES = 48L;

    final MemorySegment handle;
    final boolean owned;

    WriteOptions(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public WriteOptions() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2WriteOptions_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("WriteOptions allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2WriteOptions_delete, handle);
        }
    }

    /** @return the m2Version field of this M2WriteOptions. */
    public int getM2Version() {
        return handle.get(ValueLayout.JAVA_INT, 4L);
    }
    public void setM2Version(int value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value);
    }
    /** @return the emitSkeleton field of this M2WriteOptions. */
    public boolean getEmitSkeleton() {
        return handle.get(ValueLayout.JAVA_BYTE, 8L) != 0;
    }
    public void setEmitSkeleton(boolean value) {
        handle.set(ValueLayout.JAVA_BYTE, 8L, (byte) (value ? 1 : 0));
    }
    /** @return the baseStem field of this M2WriteOptions. */
    public String getBaseStem() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2WriteOptions_get_baseStem, arena, handle);
            MemorySegment __chars = __s.get(ValueLayout.ADDRESS, 0);
            long __len = __s.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__chars == null || __chars.equals(MemorySegment.NULL)) return "";
            byte[] __bytes = __chars.reinterpret(__len).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_CString_free, __s);
            return new String(__bytes, StandardCharsets.UTF_8);
        }
    }
    public void setBaseStem(String value) {
        try (Arena arena = Arena.ofConfined()) {
            byte[] __utf = value == null ? new byte[0] : value.getBytes(StandardCharsets.UTF_8);
            MemorySegment __seg = arena.allocate(__utf.length + 1);
            MemorySegment.copy(__utf, 0, __seg, ValueLayout.JAVA_BYTE, 0, __utf.length);
            __seg.set(ValueLayout.JAVA_BYTE, __utf.length, (byte) 0);
            NativeCommon.invokeNative(Native.whiteout_m2_M2WriteOptions_set_baseStem, handle, __seg);
        }
    }
    @Override public String toString() {
        return "WriteOptions(" + "m2Version=" + getM2Version() + ", " + "emitSkeleton=" + getEmitSkeleton() + ", " + "baseStem=" + getBaseStem() + ")";
    }

}
