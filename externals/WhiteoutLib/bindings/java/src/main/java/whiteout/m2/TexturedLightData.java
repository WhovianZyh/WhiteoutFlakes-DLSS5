// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * Java binding for the native TexturedLightData type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * TexturedLightData allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (TexturedLightData obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class TexturedLightData implements AutoCloseable {
    private static final long BYTES = 16L;

    final MemorySegment handle;
    final boolean owned;

    TexturedLightData(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public TexturedLightData() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2TexturedLightData_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("TexturedLightData allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2TexturedLightData_delete, handle);
        }
    }

    /** @return the unknown0 field of this M2TexturedLightData. */
    public float getUnknown0() {
        return handle.get(ValueLayout.JAVA_FLOAT, 0L);
    }
    public void setUnknown0(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 0L, value);
    }
    /** @return the unknown1 field of this M2TexturedLightData. */
    public float getUnknown1() {
        return handle.get(ValueLayout.JAVA_FLOAT, 4L);
    }
    public void setUnknown1(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 4L, value);
    }
    /** @return the textureLookup field of this M2TexturedLightData. */
    public int getTextureLookup() {
        return handle.get(ValueLayout.JAVA_INT, 8L);
    }
    public void setTextureLookup(int value) {
        handle.set(ValueLayout.JAVA_INT, 8L, value);
    }
    /** @return the unknown2 field of this M2TexturedLightData. */
    public int getUnknown2() {
        return handle.get(ValueLayout.JAVA_INT, 12L);
    }
    public void setUnknown2(int value) {
        handle.set(ValueLayout.JAVA_INT, 12L, value);
    }
    @Override public String toString() {
        return "TexturedLightData(" + "unknown0=" + getUnknown0() + ", " + "unknown1=" + getUnknown1() + ", " + "textureLookup=" + getTextureLookup() + ", " + "unknown2=" + getUnknown2() + ")";
    }

}
