// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * Java binding for the native Writer type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Writer allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (Writer obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class Writer implements AutoCloseable {
    private static final long BYTES = 8L;

    final MemorySegment handle;
    final boolean owned;

    Writer(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public Writer() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2Writer_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("Writer allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    public static Writer createOptions(WriteOptions options) {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2Writer_new_options, options == null ? MemorySegment.NULL : options.handle);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("Writer allocation failed");
        return new Writer(__raw, true);
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2Writer_delete, handle);
        }
    }

    /**
     * Native method wrapping {@code Writer::write}.
     * @param fs VirtualPathFileSystem input.
     * @param filePath String input.
     * @param model Model input.
     */
    public void write(whiteout.interfaces.VirtualPathFileSystem fs, String filePath, Model model) {
        try (Arena arena = Arena.ofConfined()) {
            long __fs_h = fs == null ? 0L
                : whiteout.host.VirtualPathFileSystems.resolveNative(fs, fs);
            MemorySegment __fs_seg = __fs_h == 0L
                ? MemorySegment.NULL : MemorySegment.ofAddress(__fs_h);
            MemorySegment filePath_seg = filePath == null
                ? MemorySegment.NULL
                : arena.allocateFrom(filePath, StandardCharsets.UTF_8);
            try {
                NativeCommon.invokeNative(Native.whiteout_m2_M2Writer_write, handle, __fs_seg, filePath_seg, model == null ? MemorySegment.NULL : model.handle);
            } finally {
                java.lang.ref.Reference.reachabilityFence(fs);
            }
        }
    }

    /**
     * Native method wrapping {@code Writer::write}.
     * @param cascFs CascFileSystem input.
     * @param model Model input.
     */
    public void write(whiteout.interfaces.CascFileSystem cascFs, Model model) {
        long __cascFs_h = cascFs == null ? 0L
        : whiteout.host.CascFileSystems.resolveNative(cascFs, cascFs);
        MemorySegment __cascFs_seg = __cascFs_h == 0L
        ? MemorySegment.NULL : MemorySegment.ofAddress(__cascFs_h);
        try {
        NativeCommon.invokeNative(Native.whiteout_m2_M2Writer_write_cascFs_model, handle, __cascFs_seg, model == null ? MemorySegment.NULL : model.handle);
        } finally {
        java.lang.ref.Reference.reachabilityFence(cascFs);
        }
    }

    /**
     * Native method wrapping {@code Writer::write}.
     * @param model Model input.
     * @return a fresh SerializeResult owning a native allocation.
     */
    public SerializeResult write(Model model) {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2Writer_write_model, handle, model == null ? MemorySegment.NULL : model.handle);
        return new SerializeResult(__h, true);
    }

    /**
     * Native method wrapping {@code Writer::hasIssues}.
     * @return boolean result.
     */
    public boolean hasIssues() {
        return ((int) NativeCommon.invokeNative(Native.whiteout_m2_M2Writer_hasIssues, handle)) != 0;
    }

    /**
     * String list.
     * @return the strings, materialised in one native call.
     */
    public java.util.List<String> getIssues() {
        try (Arena arena = Arena.ofConfined()) {
            long __n = (long) NativeCommon.invokeNative(Native.whiteout_m2_M2Writer_getIssues_count, handle);
            var __out = new java.util.ArrayList<String>((int) __n);
            for (long __i = 0; __i < __n; __i++) {
                MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2Writer_getIssues_at, arena, handle, __i);
                __out.add(NativeCommon.takeString(__s));
            }
            return __out;
        }
    }

    @Override public String toString() {
        return "Writer@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
