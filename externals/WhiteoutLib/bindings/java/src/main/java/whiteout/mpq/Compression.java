// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py â€” do not edit.
package whiteout.mpq;

/**
 * Compression algorithm for writing files.
 */
public enum Compression {
    /** No compression; data stored verbatim. */
    None(0),
    /** Huffman coding (used for audio in older Blizzard games). */
    Huffman(1),
    /** zlib / DEFLATE compression (most common MPQ codec). */
    Zlib(2),
    /** PKware DCL (implode) compression. */
    PKware(8),
    /** bzip2 compression. */
    BZip2(16),
    /** Sparse / RLE compression. */
    Sparse(32),
    /** IMA ADPCM mono (used for mono audio). */
    AdpcmMono(64),
    /** IMA ADPCM stereo (used for stereo audio). */
    AdpcmStereo(-128);

    public final int value;
    Compression(int v) { this.value = v; }
    public static Compression fromInt(int v) {
        for (var e : values()) if (e.value == v) return e;
        throw new IllegalArgumentException("unknown Compression: " + v);
    }
}
