// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * BOXS — an oriented box. The client turns it straight into a polytope via `CPhysicsBoxShapeDef::SetPolytopeData(frame, halfExtents)`.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * BoxShape allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (BoxShape obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class BoxShape implements AutoCloseable {
    private static final long BYTES = 60L;

    final MemorySegment handle;
    final boolean owned;

    BoxShape(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public BoxShape() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2BoxShape_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("BoxShape allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2BoxShape_delete, handle);
        }
    }

    /** @return the frame field of this M2BoxShape. */
    public PhysicsFrame getFrame() {
        return new PhysicsFrame(handle.asSlice(0L, 48L), false);
    }
    public void setFrame(PhysicsFrame value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2BoxShape_set_frame, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(value.handle, 0L, handle, 0L, 48L);
    }
    /** @return the halfExtents field of this M2BoxShape. */
    public Vector3f getHalfExtents() {
        return Handles.wrapVector3f(handle.asSlice(48L, 12L), false);
    }
    public void setHalfExtents(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2BoxShape_set_halfExtents, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 48L, 12L);
    }
    @Override public String toString() {
        return "BoxShape@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
