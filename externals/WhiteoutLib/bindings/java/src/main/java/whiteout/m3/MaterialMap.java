// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * MATM — Material map entry (v0, 8 bytes)
 * 
 * Maps a material type enum to an index into the corresponding material array. The MODL root references an array of these; the renderer uses materialType to dispatch to the correct material vector.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * MaterialMap allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (MaterialMap obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class MaterialMap implements AutoCloseable {
    private static final long BYTES = 12L;

    final MemorySegment handle;
    final boolean owned;

    MaterialMap(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public MaterialMap() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3MaterialMap_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("MaterialMap allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3MaterialMap_delete, handle);
        }
    }

    /**
     * Material type (1=standard, 2=displacement, etc.)
     * @return the materialType field of this M3MaterialMap.
     */
    public MaterialType getMaterialType() {
        return MaterialType.fromInt(handle.get(ValueLayout.JAVA_INT, 0L));
    }
    public void setMaterialType(MaterialType value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value.value);
    }
    /**
     * Index into the typed material array
     * @return the materialIndex field of this M3MaterialMap.
     */
    public int getMaterialIndex() {
        return handle.get(ValueLayout.JAVA_INT, 4L);
    }
    public void setMaterialIndex(int value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value);
    }
    @Override public String toString() {
        return "MaterialMap(" + "materialType=" + getMaterialType() + ", " + "materialIndex=" + getMaterialIndex() + ")";
    }

}
