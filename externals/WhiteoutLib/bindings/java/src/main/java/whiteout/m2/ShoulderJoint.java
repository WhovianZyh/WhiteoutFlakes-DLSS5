// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * SHOJ/SHJ2 — a twist-and-cone joint, the one that chains cloth.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * ShoulderJoint allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (ShoulderJoint obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class ShoulderJoint implements AutoCloseable {
    private static final long BYTES = 124L;

    final MemorySegment handle;
    final boolean owned;

    ShoulderJoint(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public ShoulderJoint() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2ShoulderJoint_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("ShoulderJoint allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2ShoulderJoint_delete, handle);
        }
    }

    /** @return the frameA field of this M2ShoulderJoint. */
    public PhysicsFrame getFrameA() {
        return new PhysicsFrame(handle.asSlice(0L, 48L), false);
    }
    public void setFrameA(PhysicsFrame value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2ShoulderJoint_set_frameA, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(value.handle, 0L, handle, 0L, 48L);
    }
    /** @return the frameB field of this M2ShoulderJoint. */
    public PhysicsFrame getFrameB() {
        return new PhysicsFrame(handle.asSlice(48L, 48L), false);
    }
    public void setFrameB(PhysicsFrame value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2ShoulderJoint_set_frameB, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(value.handle, 0L, handle, 48L, 48L);
    }
    /** @return the lowerTwistAngle field of this M2ShoulderJoint. */
    public float getLowerTwistAngle() {
        return handle.get(ValueLayout.JAVA_FLOAT, 96L);
    }
    public void setLowerTwistAngle(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 96L, value);
    }
    /** @return the upperTwistAngle field of this M2ShoulderJoint. */
    public float getUpperTwistAngle() {
        return handle.get(ValueLayout.JAVA_FLOAT, 100L);
    }
    public void setUpperTwistAngle(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 100L, value);
    }
    /**
     * Degrees: the corpus holds 20, 35, 45 and 60, while `dmShoulderJoint` clamps its own cone to [10°, 170°] expressed in radians — so the loader converts on the way in.
     * @return the coneAngle field of this M2ShoulderJoint.
     */
    public float getConeAngle() {
        return handle.get(ValueLayout.JAVA_FLOAT, 104L);
    }
    public void setConeAngle(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 104L, value);
    }
    /**
     * version 2+
     * @return the maxMotorTorque field of this M2ShoulderJoint.
     */
    public float getMaxMotorTorque() {
        return handle.get(ValueLayout.JAVA_FLOAT, 108L);
    }
    public void setMaxMotorTorque(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 108L, value);
    }
    /**
     * version 2+
     * @return the motorMode field of this M2ShoulderJoint.
     */
    public int getMotorMode() {
        return handle.get(ValueLayout.JAVA_INT, 112L);
    }
    public void setMotorMode(int value) {
        handle.set(ValueLayout.JAVA_INT, 112L, value);
    }
    /**
     * SHJ2
     * @return the motorFrequencyHz field of this M2ShoulderJoint.
     */
    public float getMotorFrequencyHz() {
        return handle.get(ValueLayout.JAVA_FLOAT, 116L);
    }
    public void setMotorFrequencyHz(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 116L, value);
    }
    /**
     * SHJ2
     * @return the motorDampingRatio field of this M2ShoulderJoint.
     */
    public float getMotorDampingRatio() {
        return handle.get(ValueLayout.JAVA_FLOAT, 120L);
    }
    public void setMotorDampingRatio(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 120L, value);
    }
    @Override public String toString() {
        return "ShoulderJoint(" + "lowerTwistAngle=" + getLowerTwistAngle() + ", " + "upperTwistAngle=" + getUpperTwistAngle() + ", " + "coneAngle=" + getConeAngle() + ", " + "maxMotorTorque=" + getMaxMotorTorque() + ", " + "motorMode=" + getMotorMode() + ", " + "motorFrequencyHz=" + getMotorFrequencyHz() + ", " + "motorDampingRatio=" + getMotorDampingRatio() + ")";
    }

}
