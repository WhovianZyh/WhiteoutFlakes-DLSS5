// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * Java binding for the native ParticleAnimationTrack<whiteout::Vector2f> type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * ParticleAnimationTrack<whiteout::Vector2f> allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (ParticleAnimationTrackVector2f obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class ParticleAnimationTrackVector2f implements AutoCloseable {
    final MemorySegment handle;
    final boolean owned;

    ParticleAnimationTrackVector2f(MemorySegment seg, boolean owned) {
        this.handle = seg;
        this.owned = owned;
    }

    public ParticleAnimationTrackVector2f() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2ParticleAnimationTrackVector2f_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("ParticleAnimationTrackVector2f allocation failed");
        this.handle = __raw;
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2ParticleAnimationTrackVector2f_delete, handle);
        }
    }

    /** @return the values field of this M2ParticleAnimationTrackVector2f. */
    public int getValuesCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m2_M2ParticleAnimationTrackVector2f_get_values_count, handle);
    }
    public float[] getValues() {
        long __count = (long) NativeCommon.invokeNative(Native.whiteout_m2_M2ParticleAnimationTrackVector2f_get_values_count, handle);
        MemorySegment __ptr = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2ParticleAnimationTrackVector2f_get_values_data, handle);
        if (__count == 0 || __ptr == null || __ptr.equals(MemorySegment.NULL)) return new float[0];
        long __scalars = __count * 2L;
        return __ptr.reinterpret(__scalars * 4L).toArray(ValueLayout.JAVA_FLOAT);
    }
    public void setValues(float[] values) {
        try (Arena arena = Arena.ofConfined()) {
            long __count = (long) values.length / 2;
            MemorySegment __seg = arena.allocate((long) values.length * 4L);
            if (values.length > 0) MemorySegment.copy(values, 0, __seg, ValueLayout.JAVA_FLOAT, 0, values.length);
            NativeCommon.invokeNative(Native.whiteout_m2_M2ParticleAnimationTrackVector2f_assign_values, handle, __seg, __count);
        }
    }
    public void resizeValues(int count) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2ParticleAnimationTrackVector2f_resize_values, handle, (long) count);
    }
    @Override public String toString() {
        return "ParticleAnimationTrackVector2f@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
