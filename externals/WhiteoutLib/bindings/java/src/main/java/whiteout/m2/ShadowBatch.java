// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * Java binding for the native ShadowBatch type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * ShadowBatch allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (ShadowBatch obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class ShadowBatch implements AutoCloseable {
    private static final long BYTES = 12L;

    final MemorySegment handle;
    final boolean owned;

    ShadowBatch(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public ShadowBatch() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2ShadowBatch_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("ShadowBatch allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2ShadowBatch_delete, handle);
        }
    }

    /** @return the flags field of this M2ShadowBatch. */
    public byte getFlags() {
        return handle.get(ValueLayout.JAVA_BYTE, 0L);
    }
    public void setFlags(byte value) {
        handle.set(ValueLayout.JAVA_BYTE, 0L, value);
    }
    /** @return the flags2 field of this M2ShadowBatch. */
    public byte getFlags2() {
        return handle.get(ValueLayout.JAVA_BYTE, 1L);
    }
    public void setFlags2(byte value) {
        handle.set(ValueLayout.JAVA_BYTE, 1L, value);
    }
    /** @return the unknown0 field of this M2ShadowBatch. */
    public short getUnknown0() {
        return handle.get(ValueLayout.JAVA_SHORT, 2L);
    }
    public void setUnknown0(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 2L, value);
    }
    /** @return the submeshId field of this M2ShadowBatch. */
    public short getSubmeshId() {
        return handle.get(ValueLayout.JAVA_SHORT, 4L);
    }
    public void setSubmeshId(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 4L, value);
    }
    /** @return the textureId field of this M2ShadowBatch. */
    public short getTextureId() {
        return handle.get(ValueLayout.JAVA_SHORT, 6L);
    }
    public void setTextureId(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 6L, value);
    }
    /** @return the colorId field of this M2ShadowBatch. */
    public short getColorId() {
        return handle.get(ValueLayout.JAVA_SHORT, 8L);
    }
    public void setColorId(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 8L, value);
    }
    /** @return the transparencyId field of this M2ShadowBatch. */
    public short getTransparencyId() {
        return handle.get(ValueLayout.JAVA_SHORT, 10L);
    }
    public void setTransparencyId(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 10L, value);
    }
    @Override public String toString() {
        return "ShadowBatch(" + "flags=" + getFlags() + ", " + "flags2=" + getFlags2() + ", " + "unknown0=" + getUnknown0() + ", " + "submeshId=" + getSubmeshId() + ", " + "textureId=" + getTextureId() + ", " + "colorId=" + getColorId() + ", " + "transparencyId=" + getTransparencyId() + ")";
    }

}
