// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * MSEC — Mesh section bounds (v0–v1, 80 bytes)
 * 
 * Per-node animated bounding extent used for culling and LOD.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * MeshSection allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (MeshSection obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class MeshSection implements AutoCloseable {
    private static final long BYTES = 76L;

    final MemorySegment handle;
    final boolean owned;

    MeshSection(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public MeshSection() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3MeshSection_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("MeshSection allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3MeshSection_delete, handle);
        }
    }

    /**
     * Index into BONE array
     * @return the nodeIndex field of this M3MeshSection.
     */
    public int getNodeIndex() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setNodeIndex(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /**
     * Animated bounding volume (76 bytes)
     * @return the bounds field of this M3MeshSection.
     */
    public AnimRefM3Extent getBounds() {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3MeshSection_get_bounds, handle);
        return new AnimRefM3Extent(__h, false);
    }
    public void setBounds(AnimRefM3Extent value) {
        NativeCommon.invokeNative(Native.whiteout_m3_M3MeshSection_set_bounds, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    @Override public String toString() {
        return "MeshSection(" + "nodeIndex=" + getNodeIndex() + ")";
    }

}
