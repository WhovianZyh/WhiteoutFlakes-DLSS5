// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.textures;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.textures.internal.Native;

/**
 * Reads a TEX file or byte buffer and decodes it into a Texture.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Parser allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (TexParser obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class TexParser implements AutoCloseable {
    private static final long BYTES = 16L;

    final MemorySegment handle;
    final boolean owned;

    TexParser(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public TexParser() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_TexParser_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("TexParser allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_textures_TexParser_delete, handle);
        }
    }

    /**
     * Parse a TEX file from disk.
     *
     * @param filePath String input.
     * @return a fresh java.util.Optional<Texture> owning a native allocation.
     */
    public java.util.Optional<Texture> parse(String filePath) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment filePath_seg = filePath == null
                ? MemorySegment.NULL
                : arena.allocateFrom(filePath, StandardCharsets.UTF_8);
            MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_TexParser_parse, handle, filePath_seg);
            if (__h == null || __h.equals(MemorySegment.NULL)) return java.util.Optional.empty();
            return java.util.Optional.of(new Texture(__h, true));
        }
    }

    /**
     * Parse a TEX byte buffer.
     *
     * @param buffer byte[] input.
     * @return a fresh java.util.Optional<Texture> owning a native allocation.
     */
    public java.util.Optional<Texture> parse(byte[] buffer) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment buffer_seg = arena.allocate(buffer.length * 1L);
            MemorySegment.copy(buffer, 0, buffer_seg, ValueLayout.JAVA_BYTE, 0, buffer.length);
            MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_TexParser_parse_buffer, handle, buffer_seg, (long) buffer.length);
            if (__h == null || __h.equals(MemorySegment.NULL)) return java.util.Optional.empty();
            return java.util.Optional.of(new Texture(__h, true));
        }
    }

    /**
     * Parse a D4 TEX file from two file paths (metadata .tex + pixel payload).
     *
     * @param texFilePath String input.
     * @param payloadFilePath String input.
     * @return a fresh java.util.Optional<Texture> owning a native allocation.
     */
    public java.util.Optional<Texture> parse(String texFilePath, String payloadFilePath) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment texFilePath_seg = texFilePath == null
                ? MemorySegment.NULL
                : arena.allocateFrom(texFilePath, StandardCharsets.UTF_8);
            MemorySegment payloadFilePath_seg = payloadFilePath == null
                ? MemorySegment.NULL
                : arena.allocateFrom(payloadFilePath, StandardCharsets.UTF_8);
            MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_TexParser_parse_texFilePath_payloadFilePath, handle, texFilePath_seg, payloadFilePath_seg);
            if (__h == null || __h.equals(MemorySegment.NULL)) return java.util.Optional.empty();
            return java.util.Optional.of(new Texture(__h, true));
        }
    }

    /**
     * Parse a D4 TEX file from two byte buffers (metadata + pixel payload).
     *
     * @param texData byte[] input.
     * @param payloadData byte[] input.
     * @return a fresh java.util.Optional<Texture> owning a native allocation.
     */
    public java.util.Optional<Texture> parse(byte[] texData, byte[] payloadData) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment texData_seg = arena.allocate(texData.length * 1L);
            MemorySegment.copy(texData, 0, texData_seg, ValueLayout.JAVA_BYTE, 0, texData.length);
            MemorySegment payloadData_seg = arena.allocate(payloadData.length * 1L);
            MemorySegment.copy(payloadData, 0, payloadData_seg, ValueLayout.JAVA_BYTE, 0, payloadData.length);
            MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_TexParser_parse_texData_payloadData, handle, texData_seg, (long) texData.length, payloadData_seg, (long) payloadData.length);
            if (__h == null || __h.equals(MemorySegment.NULL)) return java.util.Optional.empty();
            return java.util.Optional.of(new Texture(__h, true));
        }
    }

    /**
     * Parse a D4 TEX with hi-res + low-res payloads from file paths.
     *
     * @param texFilePath String input.
     * @param hiResPayloadFilePath String input.
     * @param lowResPayloadFilePath String input.
     * @return a fresh java.util.Optional<Texture> owning a native allocation.
     */
    public java.util.Optional<Texture> parse(String texFilePath, String hiResPayloadFilePath, String lowResPayloadFilePath) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment texFilePath_seg = texFilePath == null
                ? MemorySegment.NULL
                : arena.allocateFrom(texFilePath, StandardCharsets.UTF_8);
            MemorySegment hiResPayloadFilePath_seg = hiResPayloadFilePath == null
                ? MemorySegment.NULL
                : arena.allocateFrom(hiResPayloadFilePath, StandardCharsets.UTF_8);
            MemorySegment lowResPayloadFilePath_seg = lowResPayloadFilePath == null
                ? MemorySegment.NULL
                : arena.allocateFrom(lowResPayloadFilePath, StandardCharsets.UTF_8);
            MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_TexParser_parse_texFilePath_hiResPayloadFilePath_lowResPayloadFilePath, handle, texFilePath_seg, hiResPayloadFilePath_seg, lowResPayloadFilePath_seg);
            if (__h == null || __h.equals(MemorySegment.NULL)) return java.util.Optional.empty();
            return java.util.Optional.of(new Texture(__h, true));
        }
    }

    /**
     * @return true if the last parse produced any issues.
     */
    public boolean hasIssues() {
        return ((int) NativeCommon.invokeNative(Native.whiteout_textures_TexParser_hasIssues, handle)) != 0;
    }

    /**
     * @return accumulated issues from the last parse call.
     * @return the strings, materialised in one native call.
     */
    public java.util.List<String> getIssues() {
        try (Arena arena = Arena.ofConfined()) {
            long __n = (long) NativeCommon.invokeNative(Native.whiteout_textures_TexParser_getIssues_count, handle);
            var __out = new java.util.ArrayList<String>((int) __n);
            for (long __i = 0; __i < __n; __i++) {
                MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_TexParser_getIssues_at, arena, handle, __i);
                __out.add(NativeCommon.takeString(__s));
            }
            return __out;
        }
    }

    @Override public String toString() {
        return "TexParser@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
