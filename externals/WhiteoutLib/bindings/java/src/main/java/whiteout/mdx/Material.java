// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mdx;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mdx.internal.Native;

/**
 * Material definition with rendering properties
 * 
 * Materials define how surfaces are rendered. Each material contains one or more layers that specify textures and blending modes.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Material allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (Material obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class Material implements AutoCloseable {
    private static final long BYTES = 64L;

    final MemorySegment handle;
    final boolean owned;

    Material(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public Material() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxMaterial_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("Material allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxMaterial_delete, handle);
        }
    }

    /**
     * Rendering priority (higher = render last); signed
     * @return the priorityPlane field of this MdxMaterial.
     */
    public int getPriorityPlane() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setPriorityPlane(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /**
     * Material flags
     * @return the flags field of this MdxMaterial.
     */
    public MaterialFlag getFlags() {
        return MaterialFlag.fromInt(handle.get(ValueLayout.JAVA_INT, 4L));
    }
    public void setFlags(MaterialFlag value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value.value);
    }
    /**
     * Shader name (Reforged)
     * @return the shader field of this MdxMaterial.
     */
    public String getShader() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __s = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxMaterial_get_shader, arena, handle);
            MemorySegment __chars = __s.get(ValueLayout.ADDRESS, 0);
            long __len = __s.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__chars == null || __chars.equals(MemorySegment.NULL)) return "";
            byte[] __bytes = __chars.reinterpret(__len).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_CString_free, __s);
            return new String(__bytes, StandardCharsets.UTF_8);
        }
    }
    public void setShader(String value) {
        try (Arena arena = Arena.ofConfined()) {
            byte[] __utf = value == null ? new byte[0] : value.getBytes(StandardCharsets.UTF_8);
            MemorySegment __seg = arena.allocate(__utf.length + 1);
            MemorySegment.copy(__utf, 0, __seg, ValueLayout.JAVA_BYTE, 0, __utf.length);
            __seg.set(ValueLayout.JAVA_BYTE, __utf.length, (byte) 0);
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxMaterial_set_shader, handle, __seg);
        }
    }
    /**
     * Rendering layers
     * @return the layers field of this MdxMaterial.
     */
    public int getLayersCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_mdx_MdxMaterial_get_layers_count, handle);
    }
    public Layer getLayersAt(int index) {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxMaterial_get_layers_at, handle, (long) index);
        return new Layer(__h, false);
    }
    public void resizeLayers(int count) {
        NativeCommon.invokeNative(Native.whiteout_mdx_MdxMaterial_resize_layers, handle, (long) count);
    }
    public java.util.List<Layer> layersView() {
        return new java.util.AbstractList<Layer>() {
            @Override public int size() { return getLayersCount(); }
            @Override public Layer get(int index) { return getLayersAt(index); }
        };
    }
    @Override public String toString() {
        return "Material(" + "priorityPlane=" + getPriorityPlane() + ", " + "flags=" + getFlags() + ", " + "shader=" + getShader() + ")";
    }

}
