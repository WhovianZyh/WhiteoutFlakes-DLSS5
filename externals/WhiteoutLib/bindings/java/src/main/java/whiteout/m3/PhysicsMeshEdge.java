// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * DMME — Physics mesh edge (v0, 20 bytes)
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * PhysicsMeshEdge allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (PhysicsMeshEdge obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class PhysicsMeshEdge implements AutoCloseable {
    private static final long BYTES = 24L;

    final MemorySegment handle;
    final boolean owned;

    PhysicsMeshEdge(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public PhysicsMeshEdge() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3PhysicsMeshEdge_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("PhysicsMeshEdge allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3PhysicsMeshEdge_delete, handle);
        }
    }

    /**
     * Edge type
     * @return the edgeType field of this M3PhysicsMeshEdge.
     */
    public int getEdgeType() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setEdgeType(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /**
     * First vertex index
     * @return the vertexA field of this M3PhysicsMeshEdge.
     */
    public int getVertexA() {
        return handle.get(ValueLayout.JAVA_INT, 4L);
    }
    public void setVertexA(int value) {
        handle.set(ValueLayout.JAVA_INT, 4L, value);
    }
    /**
     * Second vertex index
     * @return the vertexB field of this M3PhysicsMeshEdge.
     */
    public int getVertexB() {
        return handle.get(ValueLayout.JAVA_INT, 8L);
    }
    public void setVertexB(int value) {
        handle.set(ValueLayout.JAVA_INT, 8L, value);
    }
    /**
     * First adjacent face
     * @return the faceA field of this M3PhysicsMeshEdge.
     */
    public int getFaceA() {
        return handle.get(ValueLayout.JAVA_INT, 12L);
    }
    public void setFaceA(int value) {
        handle.set(ValueLayout.JAVA_INT, 12L, value);
    }
    /**
     * Second adjacent face
     * @return the faceB field of this M3PhysicsMeshEdge.
     */
    public int getFaceB() {
        return handle.get(ValueLayout.JAVA_INT, 16L);
    }
    public void setFaceB(int value) {
        handle.set(ValueLayout.JAVA_INT, 16L, value);
    }
    @Override public String toString() {
        return "PhysicsMeshEdge(" + "edgeType=" + getEdgeType() + ", " + "vertexA=" + getVertexA() + ", " + "vertexB=" + getVertexB() + ", " + "faceA=" + getFaceA() + ", " + "faceB=" + getFaceB() + ")";
    }

}
