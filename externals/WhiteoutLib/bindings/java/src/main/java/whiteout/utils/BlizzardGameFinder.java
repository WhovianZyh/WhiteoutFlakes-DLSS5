// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.utils;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.host.internal.Native;

/**
 * Static-method facade around the free functions above so they bind through the class-method codegen path.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * BlizzardGameFinder allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (BlizzardGameFinder obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class BlizzardGameFinder implements AutoCloseable {
    private static final long BYTES = 1L;

    final MemorySegment handle;
    final boolean owned;

    BlizzardGameFinder(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_host_BlizzardGameFinder_delete, handle);
        }
    }

    /**
     * Discover installed Blizzard games. See `findBlizzardGames()`.
     * @return a fresh BlizzardGameInfoList owning a native allocation.
     */
    public static BlizzardGameInfoList findAll() {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_host_BlizzardGameFinder_findAll);
        return new BlizzardGameInfoList(__h, true);
    }

    /**
     * Map a display name to a BlizzardGame enum. See `blizzardGameFromName()`.
     *
     * @param name String input.
     * @return a fresh BlizzardGame owning a native allocation.
     */
    public static BlizzardGame fromName(String name) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment name_seg = name == null
                ? MemorySegment.NULL
                : arena.allocateFrom(name, StandardCharsets.UTF_8);
            return BlizzardGame.fromInt((int) NativeCommon.invokeNative(Native.whiteout_host_BlizzardGameFinder_fromName, name_seg));
        }
    }

    /**
     * Get the canonical display name for a known game. See `blizzardGameToName()`. Returns empty string for Unknown.
     *
     * @param game BlizzardGame input.
     * @return String result.
     */
    public static String toName(BlizzardGame game) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __cstr = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_host_BlizzardGameFinder_toName, arena, game.value);
            MemorySegment __chars = __cstr.get(ValueLayout.ADDRESS, 0);
            long __slen = __cstr.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            String __out = (__chars == null || __chars.equals(MemorySegment.NULL))
                ? "" : __chars.reinterpret(__slen + 1).getString(0L);
            NativeCommon.invokeNative(Native.whiteout_CString_free, __cstr);
            return __out;
        }
    }

    @Override public String toString() {
        return "BlizzardGameFinder@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
