// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * Java binding for the native LodProfile type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * LodProfile allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (LodProfile obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class LodProfile implements AutoCloseable {
    private static final long BYTES = 16L;

    final MemorySegment handle;
    final boolean owned;

    LodProfile(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public LodProfile() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2LodProfile_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("LodProfile allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2LodProfile_delete, handle);
        }
    }

    /** @return the flags field of this M2LodProfile. */
    public short getFlags() {
        return handle.get(ValueLayout.JAVA_SHORT, 0L);
    }
    public void setFlags(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 0L, value);
    }
    /** @return the numLodLevels field of this M2LodProfile. */
    public short getNumLodLevels() {
        return handle.get(ValueLayout.JAVA_SHORT, 2L);
    }
    public void setNumLodLevels(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 2L, value);
    }
    /** @return the lodDistance field of this M2LodProfile. */
    public float getLodDistance() {
        return handle.get(ValueLayout.JAVA_FLOAT, 4L);
    }
    public void setLodDistance(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 4L, value);
    }
    /**
     * Per-LOD particle bone mask index. Only four are stored, but the client drives up to eight LOD levels: it copies [0..3] into its first four slots and then replicates entry [3] into slots 4..7.
     * @return the particleBoneLod field of this M2LodProfile.
     */
    public int getParticleBoneLodCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m2_M2LodProfile_particleBoneLod_size);
    }
    public byte getParticleBoneLodAt(int index) {
        return (byte) NativeCommon.invokeNative(Native.whiteout_m2_M2LodProfile_get_particleBoneLod_at, handle, (long) index);
    }
    public void setParticleBoneLodAt(int index, byte value) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2LodProfile_set_particleBoneLod_at, handle, (long) index, value);
    }
    /**
     * Fixed-point LOD scale, applied as `lodScaleRaw / 2048.0` and only when `flags & 0x08` is set (otherwise the client uses 1.0). This is the pair of bytes previously read as `reserved0` + `lodFlags`; reading it as a u16 explains why the high byte looked like a mirror of flags bit 3, since 0x0800 / 2048 == 1.0.
     * @return the lodScaleRaw field of this M2LodProfile.
     */
    public short getLodScaleRaw() {
        return handle.get(ValueLayout.JAVA_SHORT, 12L);
    }
    public void setLodScaleRaw(short value) {
        handle.set(ValueLayout.JAVA_SHORT, 12L, value);
    }
    /** @return the lodBatchCount field of this M2LodProfile. */
    public byte getLodBatchCount() {
        return handle.get(ValueLayout.JAVA_BYTE, 14L);
    }
    public void setLodBatchCount(byte value) {
        handle.set(ValueLayout.JAVA_BYTE, 14L, value);
    }
    /** @return the reserved1 field of this M2LodProfile. */
    public byte getReserved1() {
        return handle.get(ValueLayout.JAVA_BYTE, 15L);
    }
    public void setReserved1(byte value) {
        handle.set(ValueLayout.JAVA_BYTE, 15L, value);
    }
    @Override public String toString() {
        return "LodProfile(" + "flags=" + getFlags() + ", " + "numLodLevels=" + getNumLodLevels() + ", " + "lodDistance=" + getLodDistance() + ", " + "lodScaleRaw=" + getLodScaleRaw() + ", " + "lodBatchCount=" + getLodBatchCount() + ", " + "reserved1=" + getReserved1() + ")";
    }

}
