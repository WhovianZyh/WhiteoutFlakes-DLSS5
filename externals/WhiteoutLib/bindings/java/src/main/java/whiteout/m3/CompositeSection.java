// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * CMS_ — Composite material section (v0, 24 bytes)
 * 
 * A single section within a composite material, referencing another material index with an animated blend multiplier.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * CompositeSection allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (CompositeSection obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class CompositeSection implements AutoCloseable {
    private static final long BYTES = 28L;

    final MemorySegment handle;
    final boolean owned;

    CompositeSection(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public CompositeSection() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3CompositeSection_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("CompositeSection allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3CompositeSection_delete, handle);
        }
    }

    /**
     * Index into MATM array
     * @return the materialIndex field of this M3CompositeSection.
     */
    public int getMaterialIndex() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setMaterialIndex(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /**
     * Animated blend weight
     * @return the mapMultiplier field of this M3CompositeSection.
     */
    public AnimRefF32 getMapMultiplier() {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3CompositeSection_get_mapMultiplier, handle);
        return new AnimRefF32(__h, false);
    }
    public void setMapMultiplier(AnimRefF32 value) {
        NativeCommon.invokeNative(Native.whiteout_m3_M3CompositeSection_set_mapMultiplier, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    @Override public String toString() {
        return "CompositeSection(" + "materialIndex=" + getMaterialIndex() + ")";
    }

}
