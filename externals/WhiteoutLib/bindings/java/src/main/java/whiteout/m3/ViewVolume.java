// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m3;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m3.internal.Native;

/**
 * VVOL — View volume (v0, 40 bytes)
 * 
 * Animated visibility volume bound to a bone, used for culling decisions.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * ViewVolume allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (ViewVolume obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class ViewVolume implements AutoCloseable {
    private static final long BYTES = 44L;

    final MemorySegment handle;
    final boolean owned;

    ViewVolume(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public ViewVolume() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3ViewVolume_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("ViewVolume allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m3_M3ViewVolume_delete, handle);
        }
    }

    /**
     * Index into BONE array
     * @return the nodeIndex field of this M3ViewVolume.
     */
    public int getNodeIndex() {
        return handle.get(ValueLayout.JAVA_INT, 0L);
    }
    public void setNodeIndex(int value) {
        handle.set(ValueLayout.JAVA_INT, 0L, value);
    }
    /**
     * Animated half-extents (36 bytes)
     * @return the size field of this M3ViewVolume.
     */
    public AnimRefVector3f getSize() {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m3_M3ViewVolume_get_size, handle);
        return new AnimRefVector3f(__h, false);
    }
    public void setSize(AnimRefVector3f value) {
        NativeCommon.invokeNative(Native.whiteout_m3_M3ViewVolume_set_size, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    @Override public String toString() {
        return "ViewVolume(" + "nodeIndex=" + getNodeIndex() + ")";
    }

}
