// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * PLYT — a convex hull, version 3+. Domino's `dmPolytope`.
 * 
 * The chunk stores fixed-size headers and variable-size payloads in two blocks; both halves are folded into this one struct, and the header's counts are recomputed from the vectors on write. The header's four pointer fields are filled in by the client at load time and are zero in every file, so they are not kept.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * PolytopeShape allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (PolytopeShape obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class PolytopeShape implements AutoCloseable {
    private static final long BYTES = 136L;

    final MemorySegment handle;
    final boolean owned;

    PolytopeShape(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public PolytopeShape() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("PolytopeShape allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_delete, handle);
        }
    }

    /**
     * Hull corners.
     * @return the vertices field of this M2PolytopeShape.
     */
    public int getVerticesCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_get_vertices_count, handle);
    }
    public float[] getVertices() {
        long __count = (long) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_get_vertices_count, handle);
        MemorySegment __ptr = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_get_vertices_data, handle);
        if (__count == 0 || __ptr == null || __ptr.equals(MemorySegment.NULL)) return new float[0];
        long __scalars = __count * 3L;
        return __ptr.reinterpret(__scalars * 4L).toArray(ValueLayout.JAVA_FLOAT);
    }
    public void setVertices(float[] values) {
        try (Arena arena = Arena.ofConfined()) {
            long __count = (long) values.length / 3;
            MemorySegment __seg = arena.allocate((long) values.length * 4L);
            if (values.length > 0) MemorySegment.copy(values, 0, __seg, ValueLayout.JAVA_FLOAT, 0, values.length);
            NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_assign_vertices, handle, __seg, __count);
        }
    }
    public void resizeVertices(int count) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_resize_vertices, handle, (long) count);
    }
    /**
     * Outward plane of each face, `xyz` normal and `w` offset.
     * @return the facePlanes field of this M2PolytopeShape.
     */
    public int getFacePlanesCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_get_facePlanes_count, handle);
    }
    public float[] getFacePlanes() {
        long __count = (long) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_get_facePlanes_count, handle);
        MemorySegment __ptr = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_get_facePlanes_data, handle);
        if (__count == 0 || __ptr == null || __ptr.equals(MemorySegment.NULL)) return new float[0];
        long __scalars = __count * 4L;
        return __ptr.reinterpret(__scalars * 4L).toArray(ValueLayout.JAVA_FLOAT);
    }
    public void setFacePlanes(float[] values) {
        try (Arena arena = Arena.ofConfined()) {
            long __count = (long) values.length / 4;
            MemorySegment __seg = arena.allocate((long) values.length * 4L);
            if (values.length > 0) MemorySegment.copy(values, 0, __seg, ValueLayout.JAVA_FLOAT, 0, values.length);
            NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_assign_facePlanes, handle, __seg, __count);
        }
    }
    public void resizeFacePlanes(int count) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_resize_facePlanes, handle, (long) count);
    }
    /**
     * One entry per face: any half-edge bounding it, as the entry point for walking the face through PolytopeHalfEdge::nextEdge.
     * @return the faceFirstEdges field of this M2PolytopeShape.
     */
    public int getFaceFirstEdgesCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_get_faceFirstEdges_count, handle);
    }
    public byte[] getFaceFirstEdges() {
        long __count = (long) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_get_faceFirstEdges_count, handle);
        MemorySegment __ptr = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_get_faceFirstEdges_data, handle);
        if (__count == 0 || __ptr == null || __ptr.equals(MemorySegment.NULL)) return new byte[0];
        long __scalars = __count * 1L;
        return __ptr.reinterpret(__scalars * 1L).toArray(ValueLayout.JAVA_BYTE);
    }
    public void setFaceFirstEdges(byte[] values) {
        try (Arena arena = Arena.ofConfined()) {
            long __count = (long) values.length / 1;
            MemorySegment __seg = arena.allocate((long) values.length * 1L);
            if (values.length > 0) MemorySegment.copy(values, 0, __seg, ValueLayout.JAVA_BYTE, 0, values.length);
            NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_assign_faceFirstEdges, handle, __seg, __count);
        }
    }
    public void resizeFaceFirstEdges(int count) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_resize_faceFirstEdges, handle, (long) count);
    }
    /** @return the edges field of this M2PolytopeShape. */
    public int getEdgesCount() {
        return (int) (long) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_get_edges_count, handle);
    }
    public PolytopeHalfEdge getEdgesAt(int index) {
        MemorySegment __h = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_get_edges_at, handle, (long) index);
        return new PolytopeHalfEdge(__h, false);
    }
    public void resizeEdges(int count) {
        NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_resize_edges, handle, (long) count);
    }
    public java.util.List<PolytopeHalfEdge> edgesView() {
        return new java.util.AbstractList<PolytopeHalfEdge>() {
            @Override public int size() { return getEdgesCount(); }
            @Override public PolytopeHalfEdge get(int index) { return getEdgesAt(index); }
        };
    }
    /**
     * Volume-weighted, not the vertex average.
     * @return the centroid field of this M2PolytopeShape.
     */
    public Vector3f getCentroid() {
        return Handles.wrapVector3f(handle.asSlice(96L, 12L), false);
    }
    public void setCentroid(Vector3f value) {
        if (value == null) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2PolytopeShape_set_centroid, handle, MemorySegment.NULL);
            return;
        }
        MemorySegment.copy(Handles.segmentOf(value), 0L, handle, 96L, 12L);
    }
    /**
     * Hull volume.
     * @return the volume field of this M2PolytopeShape.
     */
    public float getVolume() {
        return handle.get(ValueLayout.JAVA_FLOAT, 108L);
    }
    public void setVolume(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 108L, value);
    }
    /**
     * Hull surface area.
     * @return the surfaceArea field of this M2PolytopeShape.
     */
    public float getSurfaceArea() {
        return handle.get(ValueLayout.JAVA_FLOAT, 112L);
    }
    public void setSurfaceArea(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 112L, value);
    }
    /**
     * The four-byte gaps each count leaves in front of its 64-bit pointer, and the one that trails the header. Uninitialised in the files — some carry fragments of unrelated strings — so they are kept verbatim for writing.
     * @return the padding04 field of this M2PolytopeShape.
     */
    public int getPadding04() {
        return handle.get(ValueLayout.JAVA_INT, 116L);
    }
    public void setPadding04(int value) {
        handle.set(ValueLayout.JAVA_INT, 116L, value);
    }
    /** @return the padding14 field of this M2PolytopeShape. */
    public int getPadding14() {
        return handle.get(ValueLayout.JAVA_INT, 120L);
    }
    public void setPadding14(int value) {
        handle.set(ValueLayout.JAVA_INT, 120L, value);
    }
    /** @return the padding2c field of this M2PolytopeShape. */
    public int getPadding2c() {
        return handle.get(ValueLayout.JAVA_INT, 124L);
    }
    public void setPadding2c(int value) {
        handle.set(ValueLayout.JAVA_INT, 124L, value);
    }
    /** @return the padding4c field of this M2PolytopeShape. */
    public int getPadding4c() {
        return handle.get(ValueLayout.JAVA_INT, 128L);
    }
    public void setPadding4c(int value) {
        handle.set(ValueLayout.JAVA_INT, 128L, value);
    }
    @Override public String toString() {
        return "PolytopeShape(" + "volume=" + getVolume() + ", " + "surfaceArea=" + getSurfaceArea() + ", " + "padding04=" + getPadding04() + ", " + "padding14=" + getPadding14() + ", " + "padding2c=" + getPadding2c() + ", " + "padding4c=" + getPadding4c() + ")";
    }

}
