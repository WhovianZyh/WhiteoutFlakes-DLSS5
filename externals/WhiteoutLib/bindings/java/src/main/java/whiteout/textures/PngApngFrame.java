// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.textures;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.textures.internal.Native;

/**
 * One frame of an animated PNG (APNG), with its display duration.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * ApngFrame allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (PngApngFrame obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class PngApngFrame implements AutoCloseable {
    private static final long BYTES = 16L;

    final MemorySegment handle;
    final boolean owned;

    PngApngFrame(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public PngApngFrame() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_textures_PngApngFrame_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("PngApngFrame allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_textures_PngApngFrame_delete, handle);
        }
    }

    /**
     * Full-canvas frame image (converted to RGBA8 on write).
     * @return the image field of this PngApngFrame.
     */
    public Texture getImage() {
        return new Texture(handle.asSlice(0L, 8L), false);
    }
    public void setImage(Texture value) {
        NativeCommon.invokeNative(Native.whiteout_textures_PngApngFrame_set_image, handle, value == null ? MemorySegment.NULL : value.handle);
    }
    /**
     * Display duration in milliseconds.
     * @return the delayMs field of this PngApngFrame.
     */
    public int getDelayMs() {
        return handle.get(ValueLayout.JAVA_INT, 8L);
    }
    public void setDelayMs(int value) {
        handle.set(ValueLayout.JAVA_INT, 8L, value);
    }
    @Override public String toString() {
        return "PngApngFrame(" + "delayMs=" + getDelayMs() + ")";
    }

}
