// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.m2;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.m2.internal.Native;

/**
 * Java binding for the native ParticleEmitterExtension type.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * ParticleEmitterExtension allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (ParticleEmitterExtension obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class ParticleEmitterExtension implements AutoCloseable {
    private static final long BYTES = 64L;

    final MemorySegment handle;
    final boolean owned;

    ParticleEmitterExtension(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public ParticleEmitterExtension() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_m2_M2ParticleEmitterExtension_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("ParticleEmitterExtension allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_m2_M2ParticleEmitterExtension_delete, handle);
        }
    }

    /** @return the zSource field of this M2ParticleEmitterExtension. */
    public float getZSource() {
        return handle.get(ValueLayout.JAVA_FLOAT, 0L);
    }
    public void setZSource(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 0L, value);
    }
    /** @return the colorMult field of this M2ParticleEmitterExtension. */
    public float getColorMult() {
        return handle.get(ValueLayout.JAVA_FLOAT, 4L);
    }
    public void setColorMult(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 4L, value);
    }
    /** @return the alphaMult field of this M2ParticleEmitterExtension. */
    public float getAlphaMult() {
        return handle.get(ValueLayout.JAVA_FLOAT, 8L);
    }
    public void setAlphaMult(float value) {
        handle.set(ValueLayout.JAVA_FLOAT, 8L, value);
    }
    @Override public String toString() {
        return "ParticleEmitterExtension(" + "zSource=" + getZSource() + ", " + "colorMult=" + getColorMult() + ", " + "alphaMult=" + getAlphaMult() + ")";
    }

}
