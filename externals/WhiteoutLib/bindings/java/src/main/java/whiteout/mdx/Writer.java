// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.mdx;

import java.lang.foreign.*;
import java.lang.invoke.MethodHandle;
import java.nio.charset.StandardCharsets;
import whiteout.common.*;
import whiteout.common.internal.Handles;
import whiteout.common.internal.NativeCommon;
import whiteout.mdx.internal.Native;

/**
 * Writer for MDX/MDL model files
 * 
 * The Writer takes a Model structure and writes it to disk in binary MDX format or text MDL format. It automatically handles chunk serialization and size calculation for MDX, and text formatting for MDL.
 * 
 * The format is selected either by file extension (.mdl → text, .mdx → binary) or by an explicit MDLXFormat parameter.
 * 
 * Uses the PImpl (Pointer to Implementation) idiom to hide implementation details.
 *
 * <p><b>Lifecycle.</b> Instances hold a handle to a native
 * Writer allocation. Always release them with
 * {@link #close()} — try-with-resources is the natural shape:
 * <pre>{@code
 * try (Writer obj = ...) {
 *     // ... use obj ...
 * }
 * }</pre>
 * Borrowed views returned by field getters (e.g. nested struct slices)
 * share the parent's memory and must not outlive it.
 *
 * <p><b>Thread-safety.</b> Instances are not thread-safe. The underlying
 * C++ types make no synchronization guarantees, so callers must coordinate
 * external access if a handle is shared across threads.
 */
public final class Writer implements AutoCloseable {
    private static final long BYTES = 8L;

    final MemorySegment handle;
    final boolean owned;

    Writer(MemorySegment seg, boolean owned) {
        this.handle = (seg == null || seg.equals(MemorySegment.NULL))
            ? seg : seg.reinterpret(BYTES);
        this.owned = owned;
    }

    public Writer() {
        MemorySegment __raw = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxWriter_new);
        if (__raw == null || __raw.equals(MemorySegment.NULL))
            throw new RuntimeException("Writer allocation failed");
        this.handle = __raw.reinterpret(BYTES);
        this.owned = true;
    }

    @Override
    public void close() {
        if (!owned) return;
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxWriter_delete, handle);
        }
    }

    /**
     * Write a model file to disk
     * 
     * The format is detected from the file extension: `.mdl` for text MDL format, `.mdx` (or any other extension) for binary MDX format.
     * 
     * @param filePath  Path where the file should be written @param mdlx      Model data to write @param mdlFormat MDL text dialect (only used when writing .mdl); defaults to WarcraftIII (engine-faithful). @throws std::runtime_error If file cannot be created or written
     *
     * @param mdlx Model input.
     * @param mdlFormat MdlFormat input.
     */
    public void write(String filePath, Model mdlx, MdlFormat mdlFormat) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment filePath_seg = filePath == null
                ? MemorySegment.NULL
                : arena.allocateFrom(filePath, StandardCharsets.UTF_8);
            NativeCommon.invokeNative(Native.whiteout_mdx_MdxWriter_write, handle, filePath_seg, mdlx == null ? MemorySegment.NULL : mdlx.handle, mdlFormat.value);
        }
    }

    /**
     * Write a model to a byte buffer @param mdx       Model data to write @param format    Output format (MDX binary or MDL text, defaults to MDX) @param mdlFormat MDL text dialect (only used when format == MDL); defaults to WarcraftIII (engine-faithful). @return Vector containing the binary MDX data or UTF-8 MDL text
     *
     * @param mdx Model input.
     * @param format MDLXFormat input.
     * @param mdlFormat MdlFormat input.
     */
    public byte[] write(Model mdx, MDLXFormat format, MdlFormat mdlFormat) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment __struct = (MemorySegment) NativeCommon.invokeNative(Native.whiteout_mdx_MdxWriter_write_mdx_format_mdlFormat, arena, handle, mdx == null ? MemorySegment.NULL : mdx.handle, format.value, mdlFormat.value);
            MemorySegment __data = __struct.get(ValueLayout.ADDRESS, 0);
            long __size = __struct.get(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS.byteSize());
            if (__data == null || __data.equals(MemorySegment.NULL)) return new byte[0];
            byte[] __out = __data.reinterpret(__size).toArray(ValueLayout.JAVA_BYTE);
            NativeCommon.invokeNative(Native.whiteout_Bytes_free, __struct);
            return __out;
        }
    }

    @Override public String toString() {
        return "Writer@" + Long.toHexString(handle == null ? 0 : handle.address());
    }

}
