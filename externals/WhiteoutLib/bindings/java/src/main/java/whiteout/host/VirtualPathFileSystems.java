// SPDX-License-Identifier: BSD-3-Clause
// AUTOGENERATED by tools/codegen/emit_java.py - do not edit.
package whiteout.host;

import java.util.Objects;

import whiteout.interfaces.VirtualPathFileSystem;
import whiteout.interfaces.internal.VirtualPathFileSystemBridge;

/**
 * Dispatch helper for consumer bindings that accept any {@link VirtualPathFileSystem}.
 *
 * <p>{@link #resolveNative(VirtualPathFileSystem, Object)} normalises native-backed
 * (Panama-wrapped, implementing {@link NativeHandled}) and pure-Java
 * implementations to a raw C++ {@code VirtualPathFileSystem*} address suitable for
 * a native consumer. Native-backed inputs come through unchanged (zero-cost);
 * pure-Java inputs are wrapped via {@link VirtualPathFileSystemBridge#createPinned}.
 */
public final class VirtualPathFileSystems {

    private VirtualPathFileSystems() {}

    /**
     * Resolve {@code impl} to a raw {@code VirtualPathFileSystem*} address.
     *
     * <p>For pure-Java implementations the resulting handle is pinned by a
     * {@code Cleaner} keyed on {@code owner}; keep a strong reference to
     * {@code owner} (typically the consumer wrapper) until C++ is done
     * with the VirtualPathFileSystem.
     */
    public static long resolveNative(VirtualPathFileSystem impl, Object owner) {
        Objects.requireNonNull(impl, "impl");
        if (impl instanceof NativeHandled nh) {
            return nh.nativeHandle().address();
        }
        return VirtualPathFileSystemBridge.createPinned(impl, owner);
    }
}
