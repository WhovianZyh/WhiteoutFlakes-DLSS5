// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026 Fernando Sahmkow
// AUTOGENERATED by tools/codegen/emit_csharp.py — DO NOT EDIT.

namespace Whiteout.Textures;

/// <summary>Semantic role of a texture in a material.</summary>
public enum TextureKind : int
{
    /// <summary>Unknown or application-specific usage.</summary>
    Other = 0,
    /// <summary>Diffuse / base colour (legacy).</summary>
    Diffuse = 1,
    /// <summary>Tangent-space normal map.</summary>
    Normal = 2,
    /// <summary>Specular intensity / colour.</summary>
    Specular = 3,
    /// <summary>ORM packed texture (R=AO, G=Roughness, B=Metalness, A=Unused).</summary>
    ORM = 4,
    /// <summary>PBR base colour (albedo).</summary>
    Albedo = 5,
    /// <summary>Roughness (single channel).</summary>
    Roughness = 6,
    /// <summary>Metalness (single channel).</summary>
    Metalness = 7,
    /// <summary>Ambient occlusion (single channel).</summary>
    AmbientOcclusion = 8,
    /// <summary>Gloss / smoothness (single channel).</summary>
    Gloss = 9,
    /// <summary>Emissive colour / intensity.</summary>
    Emissive = 10,
    /// <summary>Opacity / alpha mask (single channel, linear).</summary>
    AlphaMask = 11,
    /// <summary>Hard binary mask (0 or 1); alpha-coverage-preserving filter.</summary>
    BinaryMask = 12,
    /// <summary>Smooth transparency mask; alpha-coverage-preserving, continuous values.</summary>
    TransparencyMask = 13,
    /// <summary>Blend weight mask; alpha-coverage-preserving with soft transitions.</summary>
    BlendMask = 14,
    /// <summary>Lightmap or baked light contribution (HDR colour).</summary>
    Lightmap = 15,
    /// <summary>Environment / reflection map (equirectangular, GGX prefiltered).</summary>
    EnvironmentPBR = 16,
    /// <summary>Environment map (equirectangular, spherical Kaiser-filtered).</summary>
    EnvironmentLegacy = 17,
    /// <summary>Packed multi-channel texture where each channel carries a distinct semantic role.  Use setChannelKind() / channelKind() to assign and query the per-channel kinds.  generateMipmaps() will apply a kind-appropriate filter to every channel independently.</summary>
    Multikind = 18,
    /// <summary>Channel is not used and carries no semantic meaning.  Only valid as a per-channel kind on a Multikind texture (set via setChannelKind()). generateMipmaps() applies a plain box filter to Unused channels.</summary>
    Unused = 19,
}
