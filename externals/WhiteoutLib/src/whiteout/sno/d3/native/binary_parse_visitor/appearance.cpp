// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026 Fernando Sahmkow
//
// AUTO-GENERATED by scripts/gen_d3_native.py -- do not edit manually.

#include "../binary_parse_visitor.h"

namespace whiteout {
namespace sno {
namespace d3 {
namespace native {

void BinaryParseVisitor::visit(const layout::AABB& src, AABB& dst) {
    dst.vCenter = src.vCenter;
    dst.vHalfExtent = src.vHalfExtent;
}

void BinaryParseVisitor::visit(const layout::Sphere& src, Sphere& dst) {
    dst.vCenter = src.vCenter;
    dst.flRadius = src.flRadius;
}

void BinaryParseVisitor::visit(const layout::TagMapEntry& src, TagMapEntry& dst) {
    dst.nValueType = src.nValueType;
    dst.dwTagId = src.dwTagId;
    dst.dwValue = src.dwValue;
}

void BinaryParseVisitor::visit(const layout::AppearanceLook& src, AppearanceLook& dst) {
    dst.szName = readChars(reinterpret_cast<const char*>(src.szName), 64);
}

void BinaryParseVisitor::visit(const layout::MaterialColors& src, MaterialColors& dst) {
    dst.vDiffuse = src.vDiffuse;
    dst.vSpecular = src.vSpecular;
    dst.vEmissive = src.vEmissive;
    dst.vAmbient = src.vAmbient;
    dst.flShininess = src.flShininess;
    dst.dwMaterialFlags = src.dwMaterialFlags;
}

void BinaryParseVisitor::visit(const layout::MaterialTextureAnim& src, MaterialTextureAnim& dst) {
    dst.flAmount = src.flAmount;
    dst.flRate0 = src.flRate0;
    dst.flRate1 = src.flRate1;
}

void BinaryParseVisitor::visit(const layout::MaterialTextureEntry& src, MaterialTextureEntry& dst) {
    dst.dwSlotIndex = src.dwSlotIndex;
    dst.dwUnknown04 = src.dwUnknown04;
    dst.snoTexture = AssetRef{src.snoTexture, Group::Textures};
    dst.dwTextureFlags = src.dwTextureFlags;
    dst.vUvRow0 = src.vUvRow0;
    dst.vUvRow1 = src.vUvRow1;
    dst.vUvRow2 = src.vUvRow2;
    dst.vUvRow3 = src.vUvRow3;
    visit(src.tAnimU, dst.tAnimU);
    visit(src.tAnimV, dst.tAnimV);
    visit(src.tAnimRotate, dst.tAnimRotate);
    visit(src.tAnim3, dst.tAnim3);
    visit(src.tAnim4, dst.tAnim4);
    visit(src.tAnim5, dst.tAnim5);
    dst.dwTextureType = src.dwTextureType;
    dst.dwUnknown9C = src.dwUnknown9C;
}

void BinaryParseVisitor::visit(const layout::UberMaterial& src, UberMaterial& dst) {
    dst.snoShaderMap = AssetRef{src.snoShaderMap, Group::ShaderMap};
    visit(src.tColors, dst.tColors);
    {
        size_t n = 0;
        const layout::MaterialTextureEntry* p = nullptr;
        if (locate<layout::MaterialTextureEntry>(src.arTextures, src.arTextures_size, n, p)) {
            dst.arTextures.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arTextures[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::SubObjectAppearance& src, SubObjectAppearance& dst) {
    dst.dwUnknown00 = src.dwUnknown00;
    dst.snoCloth = AssetRef{src.snoCloth, Group::Cloth};
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::TagMapEntry* p = nullptr;
        if (locate<layout::TagMapEntry>(src.arShaderParams + 4, src.arShaderParams_size - 4, n, p)) {
            dst.arShaderParams.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arShaderParams[i]);
        }
    }
    visit(src.tMaterial, dst.tMaterial);
    dst.snoMaterial = AssetRef{src.snoMaterial, Group::Material};
}

void BinaryParseVisitor::visit(const layout::AppearanceMaterial& src, AppearanceMaterial& dst) {
    dst.szName = readChars(reinterpret_cast<const char*>(src.szName), 128);
    {
        size_t n = 0;
        const layout::SubObjectAppearance* p = nullptr;
        if (locate<layout::SubObjectAppearance>(src.arVariants, src.arVariants_size, n, p)) {
            dst.arVariants.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arVariants[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::PRSTransform& src, PRSTransform& dst) {
    dst.qRotation = src.qRotation;
    dst.vTranslation = src.vTranslation;
    dst.flScale = src.flScale;
}

void BinaryParseVisitor::visit(const layout::CollisionShape& src, CollisionShape& dst) {
    dst.dwFlags = src.dwFlags;
    dst.eShapeType = src.eShapeType;
    dst.dwUnknown08 = src.dwUnknown08;
    dst.nLodIndex = src.nLodIndex;
    dst.flScaleX = src.flScaleX;
    dst.flScaleY = src.flScaleY;
    dst.flScaleZ = src.flScaleZ;
    {
        size_t n = 0;
        const u8* p = nullptr;
        if (locate<u8>(src.arPolytopeData, src.arPolytopeData_size, n, p)) dst.arPolytopeData.assign(p, p + n);
    }
    dst.vPointA = src.vPointA;
    dst.vPointB = src.vPointB;
    dst.flRadius = src.flRadius;
}

void BinaryParseVisitor::visit(const layout::PRTransform& src, PRTransform& dst) {
    dst.qRotation = src.qRotation;
    dst.vTranslation = src.vTranslation;
}

void BinaryParseVisitor::visit(const layout::ConstraintParameters& src, ConstraintParameters& dst) {
    dst.szName = readChars(reinterpret_cast<const char*>(src.szName), 64);
    dst.dwFlags = src.dwFlags;
    dst.eConstraintType = src.eConstraintType;
    dst.nBoneIndexA = src.nBoneIndexA;
    dst.nBoneIndexB = src.nBoneIndexB;
    visit(src.tFrameA, dst.tFrameA);
    dst.vUnknown34 = src.vUnknown34;
    visit(src.tFrameB, dst.tFrameB);
    visit(src.tFrameC, dst.tFrameC);
    dst.flLimitLower = src.flLimitLower;
    dst.flLimitUpper = src.flLimitUpper;
    dst.flConeAngle = src.flConeAngle;
    dst.flTwistLower = src.flTwistLower;
    dst.flTwistUpper = src.flTwistUpper;
    dst.flParam05 = src.flParam05;
    dst.flParam06 = src.flParam06;
    dst.flParam07 = src.flParam07;
    dst.flParam08 = src.flParam08;
    dst.flParam09 = src.flParam09;
    dst.flParam10 = src.flParam10;
    dst.flParam11 = src.flParam11;
    dst.flParam12 = src.flParam12;
    dst.szBreakEffect = readChars(reinterpret_cast<const char*>(src.szBreakEffect), 64);
}

void BinaryParseVisitor::visit(const layout::BoneStructure& src, BoneStructure& dst) {
    dst.szName = readChars(reinterpret_cast<const char*>(src.szName), 64);
    dst.nParentIndex = src.nParentIndex;
    visit(src.tBounds, dst.tBounds);
    visit(src.tSphere, dst.tSphere);
    visit(src.tTransform0, dst.tTransform0);
    visit(src.tTransform1, dst.tTransform1);
    visit(src.tTransform2, dst.tTransform2);
    visit(src.tTransform3, dst.tTransform3);
    visit(src.tTransform4, dst.tTransform4);
    dst.dwCollisionShapeCount = src.dwCollisionShapeCount;
    {
        size_t n = 0;
        const layout::CollisionShape* p = nullptr;
        if (locate<layout::CollisionShape>(src.arCollisionShapes, src.arCollisionShapes_size, n, p)) {
            dst.arCollisionShapes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arCollisionShapes[i]);
        }
    }
    {
        size_t n = 0;
        const layout::ConstraintParameters* p = nullptr;
        if (locate<layout::ConstraintParameters>(src.arConstraints, src.arConstraints_size, n, p)) {
            dst.arConstraints.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arConstraints[i]);
        }
    }
    dst.snoParticle = AssetRef{src.snoParticle, Group::Particle};
}

void BinaryParseVisitor::visit(const layout::LookAtData& src, LookAtData& dst) {
    dst.nEnabled = src.nEnabled;
    dst.szBoneName = readChars(reinterpret_cast<const char*>(src.szBoneName), 64);
    dst.flYawMin = src.flYawMin;
    dst.flYawMax = src.flYawMax;
    dst.flPitchMin = src.flPitchMin;
    dst.flPitchMax = src.flPitchMax;
    dst.flAngularVelocity = src.flAngularVelocity;
    dst.flUnknown58 = src.flUnknown58;
}

void BinaryParseVisitor::visit(const layout::FatVertex& src, FatVertex& dst) {
    dst.vPosition = src.vPosition;
    dst.dwNormal = src.dwNormal;
    dst.dwColor = src.dwColor;
    dst.dwAuxColor = src.dwAuxColor;
    dst.dwTexCoord0 = src.dwTexCoord0;
    dst.dwTexCoord1 = src.dwTexCoord1;
    dst.dwTangent = src.dwTangent;
    dst.dwBinormal = src.dwBinormal;
    dst.dwClothVertexIndex = src.dwClothVertexIndex;
}

void BinaryParseVisitor::visit(const layout::Influence& src, Influence& dst) {
    dst.nBoneIndex = src.nBoneIndex;
    dst.flWeight = src.flWeight;
}

void BinaryParseVisitor::visit(const layout::VertInfluences& src, VertInfluences& dst) {
    visit(src.tInfluence0, dst.tInfluence0);
    visit(src.tInfluence1, dst.tInfluence1);
    visit(src.tInfluence2, dst.tInfluence2);
}

void BinaryParseVisitor::visit(const layout::ClothVertex& src, ClothVertex& dst) {
    dst.vPosition = src.vPosition;
    dst.vNormal = src.vNormal;
    dst.vPrevPosition = src.vPrevPosition;
    dst.vVelocity = src.vVelocity;
    dst.flInvMass = src.flInvMass;
    dst.nCollisionProxyVertex = src.nCollisionProxyVertex;
    dst.nPinDistance = src.nPinDistance;
    dst.nMeshVertexIndex = src.nMeshVertexIndex;
    dst.nDrivingBone = src.nDrivingBone;
    dst.vContactNormal = src.vContactNormal;
    dst.nContactFlag = src.nContactFlag;
}

void BinaryParseVisitor::visit(const layout::ClothFace& src, ClothFace& dst) {
    dst.vNormal = src.vNormal;
    dst.flRestArea = src.flRestArea;
    dst.nVertex0 = src.nVertex0;
    dst.nVertex1 = src.nVertex1;
    dst.nVertex2 = src.nVertex2;
}

void BinaryParseVisitor::visit(const layout::ClothStaple& src, ClothStaple& dst) {
    dst.nVertexIndex = src.nVertexIndex;
    dst.nBoneIndex0 = src.nBoneIndex0;
    dst.nBoneIndex1 = src.nBoneIndex1;
    dst.nBoneIndex2 = src.nBoneIndex2;
    dst.flWeight0 = src.flWeight0;
    dst.flWeight1 = src.flWeight1;
    dst.flWeight2 = src.flWeight2;
}

void BinaryParseVisitor::visit(const layout::ClothConstraint& src, ClothConstraint& dst) {
    dst.nVertex0 = src.nVertex0;
    dst.nVertex1 = src.nVertex1;
    dst.flRestLengthSq = src.flRestLengthSq;
    dst.flWeight0 = src.flWeight0;
    dst.flWeight1 = src.flWeight1;
    dst.flStiffnessBlend = src.flStiffnessBlend;
}

void BinaryParseVisitor::visit(const layout::ClothStructure& src, ClothStructure& dst) {
    dst.dwVertexCount = src.dwVertexCount;
    {
        size_t n = 0;
        const layout::ClothVertex* p = nullptr;
        if (locate<layout::ClothVertex>(src.arVertices, src.arVertices_size, n, p)) {
            dst.arVertices.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arVertices[i]);
        }
    }
    dst.dwFaceCount = src.dwFaceCount;
    {
        size_t n = 0;
        const layout::ClothFace* p = nullptr;
        if (locate<layout::ClothFace>(src.arFaces, src.arFaces_size, n, p)) {
            dst.arFaces.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arFaces[i]);
        }
    }
    dst.dwStapleCount = src.dwStapleCount;
    {
        size_t n = 0;
        const layout::ClothStaple* p = nullptr;
        if (locate<layout::ClothStaple>(src.arStaples, src.arStaples_size, n, p)) {
            dst.arStaples.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arStaples[i]);
        }
    }
    dst.dwStretchConstraintCount = src.dwStretchConstraintCount;
    {
        size_t n = 0;
        const layout::ClothConstraint* p = nullptr;
        if (locate<layout::ClothConstraint>(src.arStretchConstraints, src.arStretchConstraints_size, n, p)) {
            dst.arStretchConstraints.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arStretchConstraints[i]);
        }
    }
    dst.dwBendConstraintCount = src.dwBendConstraintCount;
    {
        size_t n = 0;
        const layout::ClothConstraint* p = nullptr;
        if (locate<layout::ClothConstraint>(src.arBendConstraints, src.arBendConstraints_size, n, p)) {
            dst.arBendConstraints.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arBendConstraints[i]);
        }
    }
    dst.dwDrivingBoneCount = src.dwDrivingBoneCount;
    dst.flExternalForceScale = src.flExternalForceScale;
}

void BinaryParseVisitor::visit(const layout::SubObject& src, SubObject& dst) {
    dst.dwVertexFormat = src.dwVertexFormat;
    dst.dwVertexCount = src.dwVertexCount;
    {
        size_t n = 0;
        const layout::FatVertex* p = nullptr;
        if (locate<layout::FatVertex>(src.arVertices, src.arVertices_size, n, p)) {
            dst.arVertices.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arVertices[i]);
        }
    }
    {
        size_t n = 0;
        const layout::VertInfluences* p = nullptr;
        if (locate<layout::VertInfluences>(src.arVertexInfluences, src.arVertexInfluences_size, n, p)) {
            dst.arVertexInfluences.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arVertexInfluences[i]);
        }
    }
    dst.dwIndexCount = src.dwIndexCount;
    {
        size_t n = 0;
        const u16* p = nullptr;
        if (locate<u16>(src.arIndices, src.arIndices_size, n, p)) dst.arIndices.assign(p, p + n);
    }
    {
        size_t n = 0;
        const layout::ClothStructure* p = nullptr;
        if (locate<layout::ClothStructure>(src.arClothData, src.arClothData_size, n, p)) {
            dst.arClothData.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arClothData[i]);
        }
    }
    dst.snoSurface = AssetRef{src.snoSurface, Group::Surface};
    dst.nBoneIndex = src.nBoneIndex;
    dst.flUnknown58 = src.flUnknown58;
    dst.szName = readChars(reinterpret_cast<const char*>(src.szName), 128);
    dst.szMaterialName = readChars(reinterpret_cast<const char*>(src.szMaterialName), 128);
    visit(src.tBounds, dst.tBounds);
    dst.dwCollisionShapeCount = src.dwCollisionShapeCount;
    {
        size_t n = 0;
        const layout::CollisionShape* p = nullptr;
        if (locate<layout::CollisionShape>(src.arCollisionShapes, src.arCollisionShapes_size, n, p)) {
            dst.arCollisionShapes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arCollisionShapes[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::GeoSet& src, GeoSet& dst) {
    dst.dwSubObjectCount = src.dwSubObjectCount;
    {
        size_t n = 0;
        const layout::SubObject* p = nullptr;
        if (locate<layout::SubObject>(src.arSubObjects, src.arSubObjects_size, n, p)) {
            dst.arSubObjects.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arSubObjects[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::Hardpoint& src, Hardpoint& dst) {
    dst.szName = readChars(reinterpret_cast<const char*>(src.szName), 64);
    dst.nBoneIndex = src.nBoneIndex;
    visit(src.tTransform, dst.tTransform);
}

void BinaryParseVisitor::visit(const layout::CollisionCapsule& src, CollisionCapsule& dst) {
    dst.flRadius = src.flRadius;
    dst.flLength = src.flLength;
    visit(src.tHardpoint, dst.tHardpoint);
}

void BinaryParseVisitor::visit(const layout::OctreeCube& src, OctreeCube& dst) {
    dst.vMin = src.vMin;
    dst.flSize = src.flSize;
}

void BinaryParseVisitor::visit(const layout::OctreeNode& src, OctreeNode& dst) {
    visit(src.tCube, dst.tCube);
    dst.nChild0 = src.nChild0;
    dst.nChild1 = src.nChild1;
    dst.nChild2 = src.nChild2;
    dst.nChild3 = src.nChild3;
    dst.nChild4 = src.nChild4;
    dst.nChild5 = src.nChild5;
    dst.nChild6 = src.nChild6;
    dst.nChild7 = src.nChild7;
}

void BinaryParseVisitor::visit(const layout::OctreeLeaf& src, OctreeLeaf& dst) {
    visit(src.tCube, dst.tCube);
    dst.dwFirstPrimitive = src.dwFirstPrimitive;
    dst.dwPrimitiveCount = src.dwPrimitiveCount;
}

void BinaryParseVisitor::visit(const layout::OctreePrimitive& src, OctreePrimitive& dst) {
    dst.nUnknown00 = src.nUnknown00;
    dst.nUnknown04 = src.nUnknown04;
}

void BinaryParseVisitor::visit(const layout::Octree& src, Octree& dst) {
    dst.dwUnknown00 = src.dwUnknown00;
    dst.dwUnknown04 = src.dwUnknown04;
    dst.dwUnknown08 = src.dwUnknown08;
    dst.dwNodeCount = src.dwNodeCount;
    dst.dwLeafCount = src.dwLeafCount;
    dst.dwPrimitiveCount = src.dwPrimitiveCount;
    {
        size_t n = 0;
        const layout::OctreeNode* p = nullptr;
        if (locate<layout::OctreeNode>(src.arNodes, src.arNodes_size, n, p)) {
            dst.arNodes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arNodes[i]);
        }
    }
    {
        size_t n = 0;
        const layout::OctreeLeaf* p = nullptr;
        if (locate<layout::OctreeLeaf>(src.arLeaves, src.arLeaves_size, n, p)) {
            dst.arLeaves.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arLeaves[i]);
        }
    }
    {
        size_t n = 0;
        const layout::OctreePrimitive* p = nullptr;
        if (locate<layout::OctreePrimitive>(src.arPrimitives, src.arPrimitives_size, n, p)) {
            dst.arPrimitives.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arPrimitives[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::StaticLight& src, StaticLight& dst) {
    dst.nType = src.nType;
    dst.dwFlags = src.dwFlags;
    dst.vPosition = src.vPosition;
    dst.vDirection = src.vDirection;
    dst.flIntensity = src.flIntensity;
    dst.dwColor = src.dwColor;
    dst.flAttenuation0 = src.flAttenuation0;
    dst.flAttenuation1 = src.flAttenuation1;
    dst.flAttenuation2 = src.flAttenuation2;
    dst.flUnknown34 = src.flUnknown34;
    dst.flUnknown38 = src.flUnknown38;
    dst.flUnknown3C = src.flUnknown3C;
    dst.flUnknown40 = src.flUnknown40;
    dst.dwUnknown44 = src.dwUnknown44;
    dst.dwUnknown48 = src.dwUnknown48;
}

void BinaryParseVisitor::visit(const layout::Appearances& src, Appearances& dst) {
    dst.dwSnoId = src.dwSnoId;
    dst.eObjectType = src.eObjectType;
    dst.dwBoneCount = src.dwBoneCount;
    {
        size_t n = 0;
        const layout::BoneStructure* p = nullptr;
        if (locate<layout::BoneStructure>(src.arBones, src.arBones_size, n, p)) {
            dst.arBones.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arBones[i]);
        }
    }
    visit(src.tLookAt, dst.tLookAt);
    dst.dwBonePulseCount = src.dwBonePulseCount;
    {
        size_t n = 0;
        const u8* p = nullptr;
        if (locate<u8>(src.arBonePulses, src.arBonePulses_size, n, p)) dst.arBonePulses.assign(p, p + n);
    }
    visit(src.tGeoSet0, dst.tGeoSet0);
    visit(src.tGeoSet1, dst.tGeoSet1);
    visit(src.tCollisionSphere, dst.tCollisionSphere);
    dst.dwCollisionCapsuleCount = src.dwCollisionCapsuleCount;
    {
        size_t n = 0;
        const layout::CollisionCapsule* p = nullptr;
        if (locate<layout::CollisionCapsule>(src.arCollisionCapsules, src.arCollisionCapsules_size, n, p)) {
            dst.arCollisionCapsules.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arCollisionCapsules[i]);
        }
    }
    dst.dwHardpointCount = src.dwHardpointCount;
    {
        size_t n = 0;
        const layout::Hardpoint* p = nullptr;
        if (locate<layout::Hardpoint>(src.arHardpoints, src.arHardpoints_size, n, p)) {
            dst.arHardpoints.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arHardpoints[i]);
        }
    }
    dst.vUnknown108 = src.vUnknown108;
    visit(src.tOctree, dst.tOctree);
    visit(src.tBounds, dst.tBounds);
    dst.dwConstraintCount = src.dwConstraintCount;
    {
        size_t n = 0;
        const layout::ConstraintParameters* p = nullptr;
        if (locate<layout::ConstraintParameters>(src.arConstraints, src.arConstraints_size, n, p)) {
            dst.arConstraints.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arConstraints[i]);
        }
    }
    dst.dwUnknown190 = src.dwUnknown190;
    dst.nUnknown198 = src.nUnknown198;
    dst.dwLookCount = src.dwLookCount;
    dst.dwMaterialCount = src.dwMaterialCount;
    {
        size_t n = 0;
        const layout::AppearanceMaterial* p = nullptr;
        if (locate<layout::AppearanceMaterial>(src.arMaterials, src.arMaterials_size, n, p)) {
            dst.arMaterials.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arMaterials[i]);
        }
    }
    {
        size_t n = 0;
        const layout::AppearanceLook* p = nullptr;
        if (locate<layout::AppearanceLook>(src.arLooks, src.arLooks_size, n, p)) {
            dst.arLooks.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arLooks[i]);
        }
    }
    dst.dwStaticLightCount = src.dwStaticLightCount;
    {
        size_t n = 0;
        const layout::StaticLight* p = nullptr;
        if (locate<layout::StaticLight>(src.arStaticLights, src.arStaticLights_size, n, p)) {
            dst.arStaticLights.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arStaticLights[i]);
        }
    }
    dst.dwUnknown1E0 = src.dwUnknown1E0;
    dst.tmUnknown1E4 = src.tmUnknown1E4;
    dst.tmUnknown1E8 = src.tmUnknown1E8;
    dst.tmUnknown1EC = src.tmUnknown1EC;
    dst.tmUnknown1F0 = src.tmUnknown1F0;
    dst.tmUnknown1F4 = src.tmUnknown1F4;
    dst.dwUnknown1F8 = src.dwUnknown1F8;
    dst.flUnknown1FC = src.flUnknown1FC;
    dst.dwUnknown200 = src.dwUnknown200;
    dst.dwUnknown204 = src.dwUnknown204;
    dst.nUnknown208 = src.nUnknown208;
    dst.dwUnknown210 = src.dwUnknown210;
}

} // namespace native
} // namespace d3
} // namespace sno
} // namespace whiteout
