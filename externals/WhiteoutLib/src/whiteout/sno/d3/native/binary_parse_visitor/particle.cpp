// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026 Fernando Sahmkow
//
// AUTO-GENERATED by scripts/gen_d3_native.py -- do not edit manually.

#include "../binary_parse_visitor.h"

namespace whiteout {
namespace sno {
namespace d3 {
namespace native {

void BinaryParseVisitor::visit(const layout::AccelVectorNode& src, AccelVectorNode& dst) {
    dst.vStart = src.vStart;
    dst.vEnd = src.vEnd;
    dst.flTime = src.flTime;
}

void BinaryParseVisitor::visit(const layout::InterpolationScalar& src, InterpolationScalar& dst) {
    dst.nMode = src.nMode;
    dst.flMin = src.flMin;
    dst.flMax = src.flMax;
}

void BinaryParseVisitor::visit(const layout::InterpolationPathHeader& src, InterpolationPathHeader& dst) {
    dst.nNodeCount = src.nNodeCount;
    dst.flLoopStart = src.flLoopStart;
    dst.flLoopEnd = src.flLoopEnd;
    dst.eDistribution = src.eDistribution;
    visit(src.tDriver, dst.tDriver);
}

void BinaryParseVisitor::visit(const layout::AccelVectorPath& src, AccelVectorPath& dst) {
    visit(src.tHeader, dst.tHeader);
    {
        size_t n = 0;
        const layout::AccelVectorNode* p = nullptr;
        if (locate<layout::AccelVectorNode>(src.arNodes, src.arNodes_size, n, p)) {
            dst.arNodes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arNodes[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::AngleNode& src, AngleNode& dst) {
    dst.flStart = src.flStart;
    dst.flEnd = src.flEnd;
    dst.flTime = src.flTime;
}

void BinaryParseVisitor::visit(const layout::AnglePath& src, AnglePath& dst) {
    visit(src.tHeader, dst.tHeader);
    {
        size_t n = 0;
        const layout::AngleNode* p = nullptr;
        if (locate<layout::AngleNode>(src.arNodes, src.arNodes_size, n, p)) {
            dst.arNodes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arNodes[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::AngularVelocityNode& src, AngularVelocityNode& dst) {
    dst.flStart = src.flStart;
    dst.flEnd = src.flEnd;
    dst.flTime = src.flTime;
}

void BinaryParseVisitor::visit(const layout::AngularVelocityPath& src, AngularVelocityPath& dst) {
    visit(src.tHeader, dst.tHeader);
    {
        size_t n = 0;
        const layout::AngularVelocityNode* p = nullptr;
        if (locate<layout::AngularVelocityNode>(src.arNodes, src.arNodes_size, n, p)) {
            dst.arNodes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arNodes[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::ColorNode& src, ColorNode& dst) {
    dst.dwStartColor = src.dwStartColor;
    dst.dwEndColor = src.dwEndColor;
    dst.flTime = src.flTime;
}

void BinaryParseVisitor::visit(const layout::ColorPath& src, ColorPath& dst) {
    visit(src.tHeader, dst.tHeader);
    {
        size_t n = 0;
        const layout::ColorNode* p = nullptr;
        if (locate<layout::ColorNode>(src.arNodes, src.arNodes_size, n, p)) {
            dst.arNodes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arNodes[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::FloatNode& src, FloatNode& dst) {
    dst.flStart = src.flStart;
    dst.flEnd = src.flEnd;
    dst.flTime = src.flTime;
}

void BinaryParseVisitor::visit(const layout::FloatPath& src, FloatPath& dst) {
    visit(src.tHeader, dst.tHeader);
    {
        size_t n = 0;
        const layout::FloatNode* p = nullptr;
        if (locate<layout::FloatNode>(src.arNodes, src.arNodes_size, n, p)) {
            dst.arNodes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arNodes[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::VectorNode& src, VectorNode& dst) {
    dst.vStart = src.vStart;
    dst.vEnd = src.vEnd;
    dst.flTime = src.flTime;
}

void BinaryParseVisitor::visit(const layout::VectorPath& src, VectorPath& dst) {
    visit(src.tHeader, dst.tHeader);
    {
        size_t n = 0;
        const layout::VectorNode* p = nullptr;
        if (locate<layout::VectorNode>(src.arNodes, src.arNodes_size, n, p)) {
            dst.arNodes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arNodes[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::EmitterParams& src, EmitterParams& dst) {
    visit(src.tShapeExtent0, dst.tShapeExtent0);
    visit(src.tShapeExtent1, dst.tShapeExtent1);
    visit(src.tShapeExtent2, dst.tShapeExtent2);
    dst.eEmitterShape = src.eEmitterShape;
    dst.szDccShapeName = readChars(reinterpret_cast<const char*>(src.szDccShapeName), 132);
}

void BinaryParseVisitor::visit(const layout::IntNode& src, IntNode& dst) {
    dst.nStart = src.nStart;
    dst.nEnd = src.nEnd;
    dst.flTime = src.flTime;
}

void BinaryParseVisitor::visit(const layout::IntPath& src, IntPath& dst) {
    visit(src.tHeader, dst.tHeader);
    {
        size_t n = 0;
        const layout::IntNode* p = nullptr;
        if (locate<layout::IntNode>(src.arNodes, src.arNodes_size, n, p)) {
            dst.arNodes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arNodes[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::TimeNode& src, TimeNode& dst) {
    dst.tmStart = src.tmStart;
    dst.tmEnd = src.tmEnd;
    dst.flTime = src.flTime;
}

void BinaryParseVisitor::visit(const layout::TimePath& src, TimePath& dst) {
    visit(src.tHeader, dst.tHeader);
    {
        size_t n = 0;
        const layout::TimeNode* p = nullptr;
        if (locate<layout::TimeNode>(src.arNodes, src.arNodes_size, n, p)) {
            dst.arNodes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arNodes[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::VelocityVectorNode& src, VelocityVectorNode& dst) {
    dst.vStart = src.vStart;
    dst.vEnd = src.vEnd;
    dst.flTime = src.flTime;
}

void BinaryParseVisitor::visit(const layout::VelocityVectorPath& src, VelocityVectorPath& dst) {
    visit(src.tHeader, dst.tHeader);
    {
        size_t n = 0;
        const layout::VelocityVectorNode* p = nullptr;
        if (locate<layout::VelocityVectorNode>(src.arNodes, src.arNodes_size, n, p)) {
            dst.arNodes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arNodes[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::VelocityNode& src, VelocityNode& dst) {
    dst.flStart = src.flStart;
    dst.flEnd = src.flEnd;
    dst.flTime = src.flTime;
}

void BinaryParseVisitor::visit(const layout::VelocityPath& src, VelocityPath& dst) {
    visit(src.tHeader, dst.tHeader);
    {
        size_t n = 0;
        const layout::VelocityNode* p = nullptr;
        if (locate<layout::VelocityNode>(src.arNodes, src.arNodes_size, n, p)) {
            dst.arNodes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arNodes[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::Particle& src, Particle& dst) {
    dst.dwSnoId = src.dwSnoId;
    dst.eSystemType = src.eSystemType;
    dst.dwFlags = src.dwFlags;
    dst.tmLifetime = src.tmLifetime;
    dst.tmEmissionPeriod = src.tmEmissionPeriod;
    dst.tmPreSimulate = src.tmPreSimulate;
    visit(src.tLifetimeRandom, dst.tLifetimeRandom);
    dst.dwUnknown2C = src.dwUnknown2C;
    visit(src.arSizeScalePath, dst.arSizeScalePath);
    visit(src.arCountPath, dst.arCountPath);
    visit(src.arEffectScalePath, dst.arEffectScalePath);
    visit(src.arParticleLifePath, dst.arParticleLifePath);
    visit(src.arInitialSizePath, dst.arInitialSizePath);
    visit(src.arSpreadAnglePath, dst.arSpreadAnglePath);
    visit(src.arInitialVelocityPath, dst.arInitialVelocityPath);
    visit(src.arInitialVelocityWorldPath, dst.arInitialVelocityWorldPath);
    visit(src.arEmissionRatePath, dst.arEmissionRatePath);
    visit(src.arEmitterRatePathA, dst.arEmitterRatePathA);
    visit(src.arEmitterRatePathB, dst.arEmitterRatePathB);
    visit(src.arUnknownEmitterVectorPath, dst.arUnknownEmitterVectorPath);
    visit(src.arUnknownEmitterFloatPath, dst.arUnknownEmitterFloatPath);
    dst.flUnknown2A0 = src.flUnknown2A0;
    visit(src.tMaterial, dst.tMaterial);
    dst.snoPhysics = AssetRef{src.snoPhysics, Group::Physics};
    dst.flMass = src.flMass;
    dst.nMaxInstances = src.nMaxInstances;
    dst.flBurstZOffset = src.flBurstZOffset;
    dst.flSwayFrequency = src.flSwayFrequency;
    dst.flSwayDamping = src.flSwayDamping;
    dst.flSwayMaxOffset = src.flSwayMaxOffset;
    dst.flSwayGustAmount = src.flSwayGustAmount;
    dst.flSwayBaseAmount = src.flSwayBaseAmount;
    dst.flPhysicsParam6 = src.flPhysicsParam6;
    dst.snoActor = AssetRef{src.snoActor, Group::Actor};
    visit(src.tEmitter, dst.tEmitter);
    visit(src.arColorPath, dst.arColorPath);
    visit(src.arScalePath, dst.arScalePath);
    visit(src.arAlphaPath, dst.arAlphaPath);
    visit(src.arSizePath, dst.arSizePath);
    visit(src.arSize2Path, dst.arSize2Path);
    visit(src.arRotationPath, dst.arRotationPath);
    visit(src.arRotationRatePath, dst.arRotationRatePath);
    visit(src.arRotation2RatePath, dst.arRotation2RatePath);
    visit(src.arRotation2Path, dst.arRotation2Path);
    visit(src.arAxisPath, dst.arAxisPath);
    visit(src.arScalarPathCh07, dst.arScalarPathCh07);
    visit(src.arRatePathCh08, dst.arRatePathCh08);
    visit(src.arSpinRatePathCh09, dst.arSpinRatePathCh09);
    visit(src.arAxis2Path, dst.arAxis2Path);
    visit(src.arScalarPathCh11, dst.arScalarPathCh11);
    visit(src.arRatePathCh12, dst.arRatePathCh12);
    visit(src.arOffsetPath, dst.arOffsetPath);
    visit(src.arVelocityPath, dst.arVelocityPath);
    visit(src.arAccelerationPath, dst.arAccelerationPath);
    visit(src.arOffset2Path, dst.arOffset2Path);
    visit(src.arVelocity2Path, dst.arVelocity2Path);
    visit(src.arAcceleration2Path, dst.arAcceleration2Path);
    visit(src.arRatePathCh13, dst.arRatePathCh13);
    visit(src.arScalarPathCh14, dst.arScalarPathCh14);
    dst.nRenderMode = src.nRenderMode;
    dst.flMaxDistance = src.flMaxDistance;
    dst.flCameraDistScale = src.flCameraDistScale;
    {
        size_t n = 0;
        const layout::MsgTriggeredEvent* p = nullptr;
        if (locate<layout::MsgTriggeredEvent>(src.arTriggeredEvents, src.arTriggeredEvents_size, n, p)) {
            dst.arTriggeredEvents.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arTriggeredEvents[i]);
        }
    }
    dst.dwTriggeredEventCount = src.dwTriggeredEventCount;
}

} // namespace native
} // namespace d3
} // namespace sno
} // namespace whiteout
