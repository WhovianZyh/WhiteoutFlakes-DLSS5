// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026 Fernando Sahmkow
//
// AUTO-GENERATED by scripts/gen_d3_native.py -- do not edit manually.

#include "../binary_parse_visitor.h"

namespace whiteout {
namespace sno {
namespace d3 {
namespace native {

void BinaryParseVisitor::visit(const layout::TriggerConditions& src, TriggerConditions& dst) {
    dst.nChance = src.nChance;
    dst.tmDelayMin = src.tmDelayMin;
    dst.tmDelayRange = src.tmDelayRange;
    dst.tmDelayMinAlt = src.tmDelayMinAlt;
    dst.tmDelayRangeAlt = src.tmDelayRangeAlt;
    dst.flKeyValueMin = src.flKeyValueMin;
    dst.flKeyValueRange = src.flKeyValueRange;
    dst.nSubKey = src.nSubKey;
    dst.dwCondFlags = src.dwCondFlags;
}

void BinaryParseVisitor::visit(const layout::SNOName& src, SNOName& dst) {
    dst.eSnoGroup = src.eSnoGroup;
    dst.dwNameHandle = src.dwNameHandle;
}

void BinaryParseVisitor::visit(const layout::HardpointLink& src, HardpointLink& dst) {
    dst.szName = readChars(reinterpret_cast<const char*>(src.szName), 64);
    dst.nIndex = src.nIndex;
}

void BinaryParseVisitor::visit(const layout::TriggerEvent& src, TriggerEvent& dst) {
    dst.eTriggerType = src.eTriggerType;
    visit(src.tConditions, dst.tConditions);
    dst.ePayloadKind = src.ePayloadKind;
    visit(src.tPayload, dst.tPayload);
    dst.dwUnknown34 = src.dwUnknown34;
    dst.dwFlags38 = src.dwFlags38;
    dst.dwUnknown3C = src.dwUnknown3C;
    dst.dwUnknown40 = src.dwUnknown40;
    visit(src.tHardpoint0, dst.tHardpoint0);
    visit(src.tHardpoint1, dst.tHardpoint1);
    dst.szLookName = readChars(reinterpret_cast<const char*>(src.szLookName), 64);
    dst.szConstraintName = readChars(reinterpret_cast<const char*>(src.szConstraintName), 64);
    dst.dwUnknown14C = src.dwUnknown14C;
    dst.flUnknown150 = src.flUnknown150;
    dst.dwUnknown154 = src.dwUnknown154;
    dst.dwUnknown158 = src.dwUnknown158;
    dst.dwFlags15C = src.dwFlags15C;
    dst.dwUnknown160 = src.dwUnknown160;
    dst.dwUnknown164 = src.dwUnknown164;
    dst.flUnknown168 = src.flUnknown168;
    dst.flUnknown16C = src.flUnknown16C;
    dst.dwUnknown170 = src.dwUnknown170;
    dst.flUnknown174 = src.flUnknown174;
    dst.dwUnknown178 = src.dwUnknown178;
    dst.flVelocity17C = src.flVelocity17C;
    dst.dwUnknown180 = src.dwUnknown180;
    dst.tmDuration = src.tmDuration;
    dst.dwColor188 = src.dwColor188;
    dst.tmColor188Time = src.tmColor188Time;
    dst.dwColor190 = src.dwColor190;
    dst.tmColor190Time = src.tmColor190Time;
}

void BinaryParseVisitor::visit(const layout::BoneName& src, BoneName& dst) {
    dst.szBoneName = readChars(reinterpret_cast<const char*>(src.szBoneName), 64);
}

void BinaryParseVisitor::visit(const layout::TranslationKey& src, TranslationKey& dst) {
    dst.nFrame = src.nFrame;
    dst.vPosition = src.vPosition;
}

void BinaryParseVisitor::visit(const layout::TranslationCurve& src, TranslationCurve& dst) {
    dst.dwKeyCount = src.dwKeyCount;
    {
        size_t n = 0;
        const layout::TranslationKey* p = nullptr;
        if (locate<layout::TranslationKey>(src.arKeys, src.arKeys_size, n, p)) {
            dst.arKeys.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arKeys[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::Quaternion16& src, Quaternion16& dst) {
    dst.nX = src.nX;
    dst.nY = src.nY;
    dst.nZ = src.nZ;
    dst.nW = src.nW;
}

void BinaryParseVisitor::visit(const layout::RotationKey& src, RotationKey& dst) {
    dst.nFrame = src.nFrame;
    visit(src.tRotation, dst.tRotation);
}

void BinaryParseVisitor::visit(const layout::RotationCurve& src, RotationCurve& dst) {
    dst.dwKeyCount = src.dwKeyCount;
    {
        size_t n = 0;
        const layout::RotationKey* p = nullptr;
        if (locate<layout::RotationKey>(src.arKeys, src.arKeys_size, n, p)) {
            dst.arKeys.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arKeys[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::ScaleKey& src, ScaleKey& dst) {
    dst.nFrame = src.nFrame;
    dst.flScale = src.flScale;
}

void BinaryParseVisitor::visit(const layout::ScaleCurve& src, ScaleCurve& dst) {
    dst.dwKeyCount = src.dwKeyCount;
    {
        size_t n = 0;
        const layout::ScaleKey* p = nullptr;
        if (locate<layout::ScaleKey>(src.arKeys, src.arKeys_size, n, p)) {
            dst.arKeys.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arKeys[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::KeyframedAttachment& src, KeyframedAttachment& dst) {
    dst.flFrame = src.flFrame;
    visit(src.tEvent, dst.tEvent);
}

void BinaryParseVisitor::visit(const layout::AnimPermutation& src, AnimPermutation& dst) {
    dst.dwFlags = src.dwFlags;
    dst.szName = readChars(reinterpret_cast<const char*>(src.szName), 68);
    dst.flFramesPerTick = src.flFramesPerTick;
    dst.flUnknown4C = src.flUnknown4C;
    dst.flFramesPerTranslationKey = src.flFramesPerTranslationKey;
    dst.flFramesPerRotationKey = src.flFramesPerRotationKey;
    dst.flFramesPerScaleKey = src.flFramesPerScaleKey;
    dst.nBlendTicksFromOtherAnim = src.nBlendTicksFromOtherAnim;
    dst.nBlendTicksSamePermSwap = src.nBlendTicksSamePermSwap;
    dst.dwSelectionWeight = src.dwSelectionWeight;
    dst.flSpeedScalar = src.flSpeedScalar;
    dst.flUnknown6C = src.flUnknown6C;
    dst.flUnknown70 = src.flUnknown70;
    dst.flUnknown74 = src.flUnknown74;
    dst.dwBoneCount = src.dwBoneCount;
    {
        size_t n = 0;
        const layout::BoneName* p = nullptr;
        if (locate<layout::BoneName>(src.arBoneNames, src.arBoneNames_size, n, p)) {
            dst.arBoneNames.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arBoneNames[i]);
        }
    }
    dst.dwFrameCount = src.dwFrameCount;
    {
        size_t n = 0;
        const layout::TranslationCurve* p = nullptr;
        if (locate<layout::TranslationCurve>(src.arTranslationCurves, src.arTranslationCurves_size, n, p)) {
            dst.arTranslationCurves.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arTranslationCurves[i]);
        }
    }
    {
        size_t n = 0;
        const layout::RotationCurve* p = nullptr;
        if (locate<layout::RotationCurve>(src.arRotationCurves, src.arRotationCurves_size, n, p)) {
            dst.arRotationCurves.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arRotationCurves[i]);
        }
    }
    {
        size_t n = 0;
        const layout::ScaleCurve* p = nullptr;
        if (locate<layout::ScaleCurve>(src.arScaleCurves, src.arScaleCurves_size, n, p)) {
            dst.arScaleCurves.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arScaleCurves[i]);
        }
    }
    dst.flMarkerFrame0 = src.flMarkerFrame0;
    dst.flMarkerFrame1 = src.flMarkerFrame1;
    dst.flMarkerFrame2 = src.flMarkerFrame2;
    dst.flMarkerFrame3 = src.flMarkerFrame3;
    dst.vPoint0 = src.vPoint0;
    dst.vPoint1 = src.vPoint1;
    dst.vPoint2 = src.vPoint2;
    dst.vPoint3 = src.vPoint3;
    dst.flEventFrame = src.flEventFrame;
    {
        size_t n = 0;
        const layout::KeyframedAttachment* p = nullptr;
        if (locate<layout::KeyframedAttachment>(src.arAttachments, src.arAttachments_size, n, p)) {
            dst.arAttachments.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arAttachments[i]);
        }
    }
    dst.dwAttachmentCount = src.dwAttachmentCount;
    {
        size_t n = 0;
        const Vector3f* p = nullptr;
        if (locate<Vector3f>(src.arRootMotion, src.arRootMotion_size, n, p)) dst.arRootMotion.assign(p, p + n);
    }
    {
        size_t n = 0;
        const Vector3f* p = nullptr;
        if (locate<Vector3f>(src.arRootMotionInPlace, src.arRootMotionInPlace_size, n, p)) dst.arRootMotionInPlace.assign(p, p + n);
    }
    dst.vMovementVelocity = src.vMovementVelocity;
    visit(src.tHardpoint, dst.tHardpoint);
}

void BinaryParseVisitor::visit(const layout::Anim& src, Anim& dst) {
    dst.dwSnoId = src.dwSnoId;
    dst.dwFlags = src.dwFlags;
    dst.dwFlags2 = src.dwFlags2;
    dst.snoAppearance = AssetRef{src.snoAppearance, Group::Appearance};
    {
        size_t n = 0;
        const layout::AnimPermutation* p = nullptr;
        if (locate<layout::AnimPermutation>(src.arPermutations, src.arPermutations_size, n, p)) {
            dst.arPermutations.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arPermutations[i]);
        }
    }
    dst.dwPermutationCount = src.dwPermutationCount;
    dst.dwNameHash = src.dwNameHash;
}

void BinaryParseVisitor::visit(const layout::AnimSetTagMapEntry& src, AnimSetTagMapEntry& dst) {
    dst.nValueType = src.nValueType;
    dst.dwTagId = src.dwTagId;
    dst.snoAnim = AssetRef{src.snoAnim, Group::Anim};
}

void BinaryParseVisitor::visit(const layout::AnimSet& src, AnimSet& dst) {
    dst.dwSnoId = src.dwSnoId;
    dst.snoBaseAnimSet = AssetRef{src.snoBaseAnimSet, Group::AnimSet};
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tCoreTagMap + 4, src.tCoreTagMap_size - 4, n, p)) {
            dst.tCoreTagMap.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tCoreTagMap[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmHth + 4, src.tmHth_size - 4, n, p)) {
            dst.tmHth.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmHth[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm1HSwing + 4, src.tm1HSwing_size - 4, n, p)) {
            dst.tm1HSwing.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm1HSwing[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm1HThrust + 4, src.tm1HThrust_size - 4, n, p)) {
            dst.tm1HThrust.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm1HThrust[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm2HSwing + 4, src.tm2HSwing_size - 4, n, p)) {
            dst.tm2HSwing.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm2HSwing[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm2HThrust + 4, src.tm2HThrust_size - 4, n, p)) {
            dst.tm2HThrust.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm2HThrust[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmStaff + 4, src.tmStaff_size - 4, n, p)) {
            dst.tmStaff.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmStaff[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmBow + 4, src.tmBow_size - 4, n, p)) {
            dst.tmBow.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmBow[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmXBow + 4, src.tmXBow_size - 4, n, p)) {
            dst.tmXBow.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmXBow[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmWand + 4, src.tmWand_size - 4, n, p)) {
            dst.tmWand.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmWand[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmDualWield + 4, src.tmDualWield_size - 4, n, p)) {
            dst.tmDualWield.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmDualWield[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmHthWithOrb + 4, src.tmHthWithOrb_size - 4, n, p)) {
            dst.tmHthWithOrb.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmHthWithOrb[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm1HSwingWithOrb + 4, src.tm1HSwingWithOrb_size - 4, n, p)) {
            dst.tm1HSwingWithOrb.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm1HSwingWithOrb[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm1HThrustWithOrb + 4, src.tm1HThrustWithOrb_size - 4, n, p)) {
            dst.tm1HThrustWithOrb.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm1HThrustWithOrb[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmDualWieldSwordFist + 4, src.tmDualWieldSwordFist_size - 4, n, p)) {
            dst.tmDualWieldSwordFist.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmDualWieldSwordFist[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmDualWieldFistFist + 4, src.tmDualWieldFistFist_size - 4, n, p)) {
            dst.tmDualWieldFistFist.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmDualWieldFistFist[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm1HFist + 4, src.tm1HFist_size - 4, n, p)) {
            dst.tm1HFist.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm1HFist[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm2HAxeMace + 4, src.tm2HAxeMace_size - 4, n, p)) {
            dst.tm2HAxeMace.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm2HAxeMace[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmHandXBow + 4, src.tmHandXBow_size - 4, n, p)) {
            dst.tmHandXBow.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmHandXBow[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmWandWithOrb + 4, src.tmWandWithOrb_size - 4, n, p)) {
            dst.tmWandWithOrb.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmWandWithOrb[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm1HSwingWithShield + 4, src.tm1HSwingWithShield_size - 4, n, p)) {
            dst.tm1HSwingWithShield.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm1HSwingWithShield[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm1HThrustWithShield + 4, src.tm1HThrustWithShield_size - 4, n, p)) {
            dst.tm1HThrustWithShield.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm1HThrustWithShield[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmHthWithShield + 4, src.tmHthWithShield_size - 4, n, p)) {
            dst.tmHthWithShield.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmHthWithShield[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm2HSwingWithShield + 4, src.tm2HSwingWithShield_size - 4, n, p)) {
            dst.tm2HSwingWithShield.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm2HSwingWithShield[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm2HThrustWithShield + 4, src.tm2HThrustWithShield_size - 4, n, p)) {
            dst.tm2HThrustWithShield.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm2HThrustWithShield[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmStaffWithShield + 4, src.tmStaffWithShield_size - 4, n, p)) {
            dst.tmStaffWithShield.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmStaffWithShield[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm2HFlail + 4, src.tm2HFlail_size - 4, n, p)) {
            dst.tm2HFlail.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm2HFlail[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tm2HFlailWithShield + 4, src.tm2HFlailWithShield_size - 4, n, p)) {
            dst.tm2HFlailWithShield.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tm2HFlailWithShield[i]);
        }
    }
    {
        // the first 4 bytes of the block hold the element count
        size_t n = 0;
        const layout::AnimSetTagMapEntry* p = nullptr;
        if (locate<layout::AnimSetTagMapEntry>(src.tmOnHorse + 4, src.tmOnHorse_size - 4, n, p)) {
            dst.tmOnHorse.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.tmOnHorse[i]);
        }
    }
}

void BinaryParseVisitor::visit(const layout::AnimTreeLeaf& src, AnimTreeLeaf& dst) {
    dst.eLeafType = src.eLeafType;
    dst.snoAnim = AssetRef{src.snoAnim, Group::Anim};
    dst.dwAnimTag = src.dwAnimTag;
    dst.flPoseTime = src.flPoseTime;
    dst.flPlaybackRate = src.flPlaybackRate;
    dst.nSyncGroup = src.nSyncGroup;
    dst.eAdvanceMode = src.eAdvanceMode;
    dst.eBoneMask = src.eBoneMask;
    dst.dwLayerNameHash = src.dwLayerNameHash;
}

void BinaryParseVisitor::visit(const layout::AnimTreeBlendCase& src, AnimTreeBlendCase& dst) {
    dst.eVariable = src.eVariable;
    dst.nValue = src.nValue;
    dst.flValue = src.flValue;
}

void BinaryParseVisitor::visit(const layout::AnimTreeBlendRamp& src, AnimTreeBlendRamp& dst) {
    dst.flRampInTime = src.flRampInTime;
    dst.flRampOutTime = src.flRampOutTime;
    dst.flDuration = src.flDuration;
    dst.flStartValue = src.flStartValue;
    dst.flHoldValue = src.flHoldValue;
    dst.flEndValue = src.flEndValue;
}

void BinaryParseVisitor::visit(const layout::AnimTreeNode& src, AnimTreeNode& dst) {
    dst.eNodeType = src.eNodeType;
    dst.dwEntryCount = src.dwEntryCount;
    std::memcpy(dst.arChildren, src.arChildren, sizeof(dst.arChildren));
    dst.eVariable = src.eVariable;
    for (size_t i = 0; i < 16; ++i) visit(src.arBlendCases[i], dst.arBlendCases[i]);
    visit(src.tBlendRamp, dst.tBlendRamp);
    dst.nSyncGroup = src.nSyncGroup;
    dst.dwLayerNameHash = src.dwLayerNameHash;
}

void BinaryParseVisitor::visit(const layout::AnimTree& src, AnimTree& dst) {
    dst.dwSnoId = src.dwSnoId;
    dst.dwUnknown0C = src.dwUnknown0C;
    dst.dwFlags = src.dwFlags;
    dst.dwLeafCount = src.dwLeafCount;
    {
        size_t n = 0;
        const layout::AnimTreeLeaf* p = nullptr;
        if (locate<layout::AnimTreeLeaf>(src.arLeaves, src.arLeaves_size, n, p)) {
            dst.arLeaves.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arLeaves[i]);
        }
    }
    dst.dwNodeCount = src.dwNodeCount;
    {
        size_t n = 0;
        const layout::AnimTreeNode* p = nullptr;
        if (locate<layout::AnimTreeNode>(src.arNodes, src.arNodes_size, n, p)) {
            dst.arNodes.resize(n);
            for (size_t i = 0; i < n; ++i) visit(p[i], dst.arNodes[i]);
        }
    }
    dst.dwSyncGroupCount = src.dwSyncGroupCount;
    {
        size_t n = 0;
        const i32* p = nullptr;
        if (locate<i32>(src.arSyncGroups, src.arSyncGroups_size, n, p)) dst.arSyncGroups.assign(p, p + n);
    }
    dst.nRootRef = src.nRootRef;
}

} // namespace native
} // namespace d3
} // namespace sno
} // namespace whiteout
