// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026 Fernando Sahmkow
//
// AUTO-GENERATED by scripts/gen_d3_native.py -- do not edit manually.

#include "binary_parse_visitor.h"

#include <whiteout/sno/d3/native/d3_native.h>

#include <utility>

namespace whiteout {
namespace sno {
namespace d3 {
namespace native {

namespace {

/// Resolve one reference through a provider.  An implementation detail of
/// loadActorModel, not API: a caller with a provider already has parse*.
template <class T, class Fn>
std::optional<T> resolve(const AssetRef& ref, AssetProvider& src, Fn parse) {
    if (!ref.valid()) return std::nullopt;
    auto bytes = src.load(ref.group, ref.id);
    if (bytes.empty()) return std::nullopt;
    return parse(bytes);
}

} // namespace

std::optional<Actor> parseActor(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    Actor out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<Anim> parseAnim(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    Anim out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<AnimSet> parseAnimSet(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    AnimSet out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<Appearances> parseAppearances(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    Appearances out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<Cloth> parseCloth(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    Cloth out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<EffectGroup> parseEffectGroup(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    EffectGroup out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<Particle> parseParticle(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    Particle out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<Physics> parsePhysics(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    Physics out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<PhysMesh> parsePhysMesh(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    PhysMesh out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<ShaderMap> parseShaderMap(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    ShaderMap out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<Shaders> parseShaders(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    Shaders out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<Surface> parseSurface(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    Surface out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<Texture> parseTexture(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    Texture out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<AnimTree> parseAnimTree(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    AnimTree out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<Material> parseMaterial(std::span<const u8> file) {
    BinaryParseVisitor v(file);
    Material out;
    if (!v.read(out)) return std::nullopt;
    return out;
}

std::optional<ActorModel> loadActorModel(std::span<const u8> actorFile,
                                         AssetProvider& src) {
    auto actor = parseActor(actorFile);
    if (!actor) return std::nullopt;
    ActorModel m;
    m.actor = std::move(*actor);
    m.appearance = resolve<Appearances>(m.actor.snoAppearance, src, parseAppearances);
    m.animSet = resolve<AnimSet>(m.actor.snoAnimSet, src, parseAnimSet);
    m.physics = resolve<Physics>(m.actor.snoPhysics, src, parsePhysics);
    return m;
}

} // namespace native
} // namespace d3
} // namespace sno
} // namespace whiteout
