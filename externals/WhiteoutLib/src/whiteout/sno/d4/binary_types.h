// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026 Fernando Sahmkow
//
// AUTO-GENERATED by scripts/gen_d4_binary_types.py — do not edit manually.
// Source: Diablo IV build 3.1.1.72836 (image base 0x140000000,
// container registry 0x14310F730).

#pragma once

#include <whiteout/common_types.h>

#include <cstddef>

namespace whiteout {
namespace sno {
namespace d4 {

/// Root type hash the shipped client uses for a SNO group, or 0 if the
/// group has no container compiled into the client.
u32 rootTypeHashForGroup(i32 group);

/// Format hash the shipped client expects for a SNO group, or 0 if unknown.
/// Matches the CoreTOC of the same build.
u32 formatHashForGroup(i32 group);

/// Root type hash for a format hash the compiled registry does not know —
/// older or variant revisions of the same root type.  Returns 0 if unknown.
u32 rootTypeHashForFormatHash(u32 formatHash);

/// Hash the client stores in place of a field name: h = c + 33*h over the
/// case-sensitive name, masked to 28 bits.  Retail strips field names, and
/// d4data spells the ones it never recovered "unk_<hash>" using exactly this
/// value — which is how the two were matched up.
u32 fieldNameHash(const char* name);

/// Number of SNO groups with a container in the shipped client.
constexpr size_t kBinaryGroupCount = 135;

} // namespace d4
} // namespace sno
} // namespace whiteout
